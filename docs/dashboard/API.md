# API contracts

`/api/v1` endpoints use verified Better Auth sessions. Browser cookie mutations require the configured dashboard Origin. Future iOS/Android clients use the same account's signed bearer session token in `Authorization`; a valid bearer request is allowed without a browser Origin and is still subject to the same verified-email, MFA, role and property authorization checks. Errors use HTTP status and an error message; callers must preserve pending operations on failure.

`lib/api-spec/dashboard.openapi.json` specifies selected shared dashboard contracts, including setup/identity, client and property records, client contacts, field work, assessment availability and booking, integration health, property reads, sales, project and expense records, project-phase and service-request lifecycles, scheduling, commercial intake, agreement work and binary photos. `pnpm --filter @workspace/api-spec codegen:dashboard` generates the isolated dashboard fetch client. The UI consumes its field methods. Other routes currently validate with Zod at the server and still require full OpenAPI coverage.

| Domain                | Routes beneath /api/v1                                                                                                                |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| Initialization/access | /setup, /setup/complete, /me, /staff, /account-mfa-policies, /account-mfa-policies/:id, /invitations, /invitations/accept             |
| Operations            | /clients, /properties, /properties/:id/areas, /projects, /work-orders, /work-orders/:id/status, /work-orders/:id/publish, /field/sync |

`GET/POST /clients`, `POST /clients/:id`, and `POST /properties` are generated contracts. Office client updates require the current version and atomically maintain the primary contact. Client accounts receive a minimized client list; crew have no client-record access. A primary-contact onboarding request requires address and phone and returns its created contact ID; neither client nor property creation creates an invitation, schedule, work order, billing record, or provider action.

The generated work-order contract covers office planning, versioned status transitions, and manager publication alongside the existing read, readiness, rescheduling, and field-sync methods. Only owner, manager, or dispatch can create or transition work; an override reason requires owner or manager authority. Publication requires a reviewed work order and is owner/manager-only. It publishes only eligible non-conflicting notes, checklist entries, and completion events; it does not publish field issues, alter billing, or record payment.

`POST /properties/:id` is an office-only, versioned edit of an operational unarchived property. It requires `{name,address,acreage:null|number,accessInstructions?,version}` and returns `409` for stale or unavailable records. A changed address receives a best-effort server-side coordinate refresh; an unresolved address clears the prior point so the map cannot show an obsolete location, while an unchanged address retains its existing point without another geocoding request. `POST /projects/:id` is owner/manager-only and requires `{name,scope,expectedVersion}`. It atomically advances the project version and returns `409` on a stale or unavailable record. Neither route changes client access, phase state, schedules, work, billing, publication, providers, or payments.

`GET/POST /property-types`, `POST /property-types/:id`, and `DELETE /property-types/:id` are owner/manager-only portfolio-classification administration routes. The property workspace updates its current type through `POST /properties/:id/property-type` with `{propertyTypeId:null|uuid,expectedVersion}`. It uses the property's optimistic version and returns `409` on a stale or unavailable property; a type in use cannot be deleted. Client property reads may include their assigned type name but never receive the classification library or mutation access.
| Project phases | /projects/:id/phases, /project-phases/:id, /project-phases/:id/transitions, /project-phases/:id/publish, history and billing-intent routes |
| Service requests | /requests, /service-requests, /service-requests/:id, transitions, history, conversion preview and conversion receipt routes |
| Scheduling | /assessment-slots, /assessment-slots/:id/book, /recurring-services; operations.ts owns rescheduling/pause routes |

The generated recurring-schedule contract covers office creation and future-generation pause/resume. Owner, manager, and dispatch can configure weekly or monthly cadence, interval, America/New_York local time, assigned crew, and independent fixed-monthly or per-visit billing metadata. The worker remains responsible for creating occurrences; pausing never rewrites existing work orders, sends notifications, or alters billing records.
| Sales | /leads, /estimates, estimate decision/revision and lead conversion routes in sales.ts |
| Financial | /expenses, /billing, /billing/:id/post, /quickbooks/connect, /quickbooks/callback, /quickbooks/import-preview, /quickbooks/import, /quickbooks/invoices |
| Media | POST /files/:id with image body, x-p1-property, x-p1-work, x-p1-classification; GET/POST /profile/avatar; protected content/publication routes in files.ts |
| Communications | Notification, delivery and consent routes in notifications.ts |

## Client onboarding and maintenance

Office roles (owner, manager, dispatch, sales and finance) create a client with `POST /clients`. The dashboard onboarding flow submits a business name, address and phone plus a required primary contact: first name, last name, email, position and phone. The server creates the client and primary contact in one transaction and records audit events for both, so a partial client is never retained when contact creation fails.

`GET /clients` exposes the business details and the active primary contact only to office roles; client-role responses remain minimized to their own ID and name. `POST /clients/:id` updates the business and primary contact together. It requires the client `version`; stale submissions return 409 rather than overwriting another user's edit. When an older client has no primary contact, this edit creates one. Additional site, billing and other contacts remain managed through `GET`/`POST /clients/:clientId/contacts` and `POST /clients/:clientId/contacts/:id`, all with the same office-role check and contact version handling.

Migration 0015 adds the client version and primary-contact detail columns without removing existing data. The legacy minimal client-create payload remains supported for compatibility with existing internal integrations, but the dashboard uses the complete onboarding payload.

Billing creation requires `operationId` (UUID), `propertyId`, `estimateId`, `title`, integer `amountCents`, and `kind` (service/deposit/progress/final). Reuse the same operation ID on an identical retry. Different payload reuse returns 409; a new financial intent requires a new operation ID. The transaction stores actor, canonical validated-payload fingerprint and resulting draft ID. This does not automatically send an invoice or collect payment.

## Project phases (deployed migration 0020)

`POST /projects/:projectId/phases` requires owner or manager and creates an additive normalized phase while preserving the legacy JSON `project.phases` value. An explicit occupied `position` returns409. Owner/manager edits use `PATCH /project-phases/:id` with `expectedVersion` and a required reason. Terminal accepted, cancelled and archived records reject edits.

`POST /project-phases/:id/transitions` requires the current version, target state and a reason. The server validates lifecycle transitions, blocks ready/in-progress states with unmet prerequisites unless an explicit override reason is recorded, and requires all linked work to be reviewed/cancelled/skipped before acceptance. `GET /project-phases/:id/history` returns the append-only event history only to office roles. Client reads are restricted to explicitly published projections; crew reads are restricted to phases with active assigned work.

`POST /project-phases/:id/publish` is an owner/manager action for manager-review or accepted work and records a client-safe summary only. `POST /project-phases/:id/billing-intents` is owner/manager/finance-only. It requires an accepted phase, approved estimate, current phase version and UUID operation ID. The server stores one immutable intent and one draft billing record on an identical retry, enforces the estimate cap, and returns409 for a changed operation replay. It never posts to QuickBooks, sends an invoice, creates a payment link, records a payment, or publishes crew material.

The phase list/detail, create/update, transition, publication, history, and billing-intent endpoints are generated from the shared OpenAPI contract. The generated types keep client publications and crew assignment views minimized; office-only event history and billing-intent records remain server-authorized.

## Project and expense records

`GET/POST /projects` is generated from the shared contract. Owner, manager, dispatch, and finance can list operational-property projects; only owner and manager can create one. A creation writes the legacy project summary and optional legacy phase list only. It does not create normalized project phases, dispatch work, publish client material, create billing, post to QuickBooks, or record a payment.

`GET/POST /expenses` is generated from the shared contract. Owner, manager, and finance can list and record expenses for operational properties. It is an operational job-costing record; QuickBooks remains the accounting system of record. The database returns `amount_cents` as a base-10 string because the source column is PostgreSQL `bigint`; request `amountCents` remains a positive integer. These endpoints do not create a QuickBooks entry, post accounting changes, create a payment link, or record a payment.

## Sales lifecycle

The generated sales contract covers office lead listing and creation, eligible lead conversion, draft estimate creation, staff sending, client approval/decline, revisions, and change orders. The established `listAgreementEstimates` read method remains the shared typed reader for `/estimates`, so agreement callers retain their existing operation name and response model.

Lead conversion makes a client/property linkage only. Estimate creation, revision, and change order creation do not send an estimate, schedule work, dispatch a crew, publish client material, post a QuickBooks invoice, create a payment link, or record a payment. An estimate approval remains a current-revision decision by an authorized client for an accessible property; office roles may only move a draft to `sent`.

## Client service requests (deployed migration 0021)

`GET/POST /requests` remain compatible. A client list response has client-safe status labels and no submitting-user identity; office readers retain the operational details. Creation requires a client-accessible or operational property and writes the first append-only lifecycle event in the same transaction.

`GET /service-requests` and `GET /service-requests/:id` provide the normalized lifecycle read model. Clients can read only properties granted to their account and receive a minimized projection. Crew have no independent request queue. Owner, manager, dispatch, sales and finance can read the operational projection; only owner, manager and dispatch can mutate it.

`POST /service-requests/:id/transitions` requires `{expectedVersion,status,reason}`. It accepts only `triaged`, `scheduled`, `closed`, or `cancelled` targets, locks the request, rejects stale or invalid transitions with409, advances the version, and appends an event. `GET /service-requests/:id/history` is office-only and reads append-only events.

`POST /service-requests/:id/conversion-preview` writes nothing. `POST /service-requests/:id/conversions` requires `{operationId,expectedRequestVersion,title,scope,checklist,prerequisites}`. It permits only triaged or service-planning requests, records a stable fingerprint and one receipt, and returns that receipt on an identical retry; changed reuse, stale state, cancellation and a second operation return409. The work order is always an unassigned, unscheduled, unpublished `draft`. This route never invokes QuickBooks, notifications, payments, files, publication or outbox actions.

The migration and public health/authentication boundaries are production-verified. The lifecycle reads, transitions, history, conversion preview and idempotent draft conversion are generated from the shared OpenAPI contract; broader office-route coverage, invited-client workflow acceptance and the one-crew pilot remain open.

## Inspection report publication

`GET /inspections` is available to owner, manager, dispatch, and client accounts. Office roles receive operational inspections for active properties. A client receives only explicitly published inspections belonging to a client-accessible operational property; its response omits the submitting staff user ID. `POST /inspections/:id/publish` requires owner or manager, locks the active-property inspection, and records `inspection.published` in audit history the first time it becomes client-visible. Repeating publication is safe and does not create another audit event. Publishing does not alter a work order, file, invoice, payment, notification, or underlying property condition data.

`GET /properties/:id/timeline` combines the latest 200 field events and inspection reports in chronological order. Clients see inspection entries only after explicit publication; crew still see only their assigned field work and do not receive inspection entries. Timeline inspection payloads retain report findings and are a historical view, not an inferred property-health score.

## Cancellation-review implementation candidate (unmerged)

The isolated agreement correction candidate extends the generated OpenAPI contract with `GET /agreement-charges/:id/review`, `POST /agreement-charges/:id/review-preview`, `POST /agreement-charges/:id/reviews`, `GET /agreement-charges/:id/reviews`, and `GET /service-agreements/:id/charges`. They require an owner, manager or finance actor and always return `Cache-Control: no-store`. Preview writes nothing. The record operation requires a UUID operation ID and returns a historical receipt on an identical retry; a changed reuse or stale snapshot returns 409.

The server builds and hashes the cancellation, charge and full billing-draft snapshot. `keep_due` is valid only for that exact snapshot; `correction_required` is sticky. Neither result posts, changes the invoice amount, creates a credit, releases an approved cap, sends an invoice or records a payment. See [AGREEMENT_CORRECTION_PROPOSAL.md](AGREEMENT_CORRECTION_PROPOSAL.md) for the precise contract. This candidate is not deployed.

Field submissions require unique operation IDs, target work order, base version, captured timestamp, kind and validated payload. Accepted acknowledgments permit local removal; conflicts remain for review. Reassignment/authorization rejection also leaves the device copy intact. File uploads use their stable operation UUID and immutable content-derived object keys.

## Commercial inquiry contract (implemented and staging accepted)

Reviewed backend checkpoint66338b0 adds migration0010 and separate raw-body service ingress. Public browsers continue submitting once to Core. The receiver validates the versioned contract, HMAC signature, source/submission identity, fingerprint and bounded payload before durable receipt; browser-authenticated or Origin-bearing ingress is rejected. Exact wire contract and sender retry rules are canonical in `platform/p1-core/docs/contracts/p1-commercial-handoff.md`. No receipt automatically provisions client identities, portal access, properties or QuickBooks records.

Staff endpoints under `/api/v1` require owner, manager or sales:

- `GET /commercial-inquiries`: `{items,nextCursor}`, default50/max200. Optional `status`, `ownerId` (user ID or `unassigned`) and `overdue=true`. Cursor is opaque and bound to filters; reset it whenever filters change. Ordering is created_at DESC/id DESC with microsecond precision.
- `GET /commercial-inquiries/:id`: full lead detail plus submission_id, received_at and raw_intake. Raw intake is untrusted business content and must never be interpreted as HTML or instructions.
- `PATCH /commercial-inquiries/:id/follow-up`: `{expectedVersion,ownerId:null|string,nextAction,nextActionDueAt:null|ISO,status}`. Status is new/contacted/qualified/proposal/won/lost. Next action is trimmed1–2000 characters. Assign only active owner/manager/sales users. A stale version returns409; success advances version and audit history. Re-fetch detail and first page because the result omits receipt metadata.

The legacy lead list filters commercial inquiries out for dispatch/finance, and operational conversion advances the same optimistic version. See COMMERCIAL_INBOX.md for UI behavior and regression steps. Commercial and schedule generated contracts are implemented in883de4e and pending review/release; remaining office routes still require coverage. The implemented service schemas remain authoritative for current wire validation.

## Assessment availability (deployed checkpoint4079996)

`GET /assessment-availability` returns `{config,blackouts,timeZone}`. `POST /assessment-availability` requires `{version,durationMinutes,bufferBefore,bufferAfter,windows:[{day,start,end}]}`; weekday 0 is Sunday and times are HH:mm in America/New_York. Duration 15–240 minutes and buffers 0–120 minutes; overlapping windows or windows shorter than duration+buffers fail 400. Current version required; stale 409. Rules retire only future, unbooked, generated slots. Booked and manually created appointments remain.

`POST /assessment-availability/generate` takes `{from,through}` ISO dates, inclusive 1–90 days, and returns `{created}`. Generation is explicit after saving rules; retries preserve existing slots. Appointment and travel occupancy must fit the configured window. Nonexistent/cross-transition local appointment times are skipped; local 9am stays 9am as UTC offset changes.

`POST /assessment-availability/blackouts` accepts UTC `{startsAt,endsAt,reason}`; `/assessment-availability/blackouts/:id/archive` removes a blackout through archival. A blackout overlapping a confirmed appointment or its buffers fails 409. Availability reads exclude blackouts; booking checks the same condition transactionally. The office form clearly labels blackout entry with the device timezone; weekly windows always use New York.

All availability management routes require owner/manager/dispatch on the server. Client booking still requires property access. Config, generation, booking, manual slots and blackout mutations share the calendar advisory transaction lock; all mutations are audited. Manual availability may reuse a retired unbooked start but never overwrite a booking. Migration 0009 is additive; application rollback can retain the new columns/tables.

Seven-test combined suite plus migration replay passed. Assessment coverage includes six-role management endpoint matrix, repeated generation, spring offset change, stale config, retained bookings, blackout travel buffers, booking/blackout race, and retired-slot reoffer. Browser fixture of the real component exercised duration saving, generation and blackout creation. Physical-device and live authenticated acceptance remain outstanding.

## Date-range scheduling (implemented locally; deployment pending)

`GET /schedule?from=YYYY-MM-DD&through=YYYY-MM-DD` returns `{items,nextCursor}` in100-row pages for up to32inclusive New York calendar days. Follow `cursor` until null. UTC cursor timestamps preserve PostgreSQL microseconds so equal-time appointments cannot repeat or disappear at page boundaries. `unscheduled=true` pages the office backlog separately; clients cannot use that mode. Client/crew row isolation matches `/work-orders`, and client responses omit internal scope, assignments and versions.

`POST /work-orders/:id/reschedule` accepts `{scheduledAt,assignedTo?,version,reason}`. Omitted assignment preserves the current person; explicit null unassigns. Only owner/manager/dispatch can edit. The work row is locked, current version required, and only draft/scheduled/delayed jobs can move. Non-client active staff are validated with a row lock. The change increments version and audits reason and before/after values; recurrence identifiers and occurrence date are not changed. Started/completed/closed work fails409. This does not implement crew duration/capacity conflict detection.

The calendar loads all pages for the chosen date range and the authorized unscheduled backlog, displays load/retry errors, and allows eligible office users to edit a selected visit. It stops with an explicit error after100pages rather than silently truncating. Datetime editing clearly uses the device timezone; calendar grouping uses New York. Browser fixture verified moving a visit to the next day at9am and clearing its assignment. Tests cover506appointments with identical microsecond timestamps, client/crew projections and route isolation, competing edits, invalid client assignment, occurrence preservation and started-work rejection.

Scheduling review follow-up: `GET /work-orders/:id` authorizes the same client/crew boundary and returns the same safe client projection. Calendar job navigation fetches this detail instead of depending on the legacy list; the selected job is re-fetched during refresh and cleared on sign-out. Isolation tests traverse every page and assert the exact authorized505-record set for both client and crew. A full-app synthetic browser fixture with an empty legacy list verified calendar → detail card → field-entry dialog for a separately loaded job.

### Shared identity contract for mobile

`getDashboardMe` and `DashboardMe` are generated from `/api/v1/me` in the dashboard OpenAPI contract. A verified session may return `role: null` when its business profile is missing or inactive. `avatarUrl` is either `null` or the private same-origin path for that account; it is never an object-storage URL. `twoFactorEnabled` is optional and nullable because the handler forwards the identity provider value without normalization; consumers must compare it with `true`. `mfaRequired` reports whether the current session is blocked by its configured policy; `ownerMfaRequired` is its deprecated compatibility alias. Reading this endpoint does not create session assurance or authorize operational data access.

`GET /profile/avatar` and `POST /profile/avatar` require the current active account. Reads return only that account's transformed WebP image with `Cache-Control: private, no-store`; a known URL never authorizes another account to read it. Uploads accept JPEG, PNG, or WebP only, validate their signatures after authentication, limit request bytes to 5 MiB and decoded pixels to 16 million, convert to a 512px WebP, and allow only four in-flight avatar uploads per dashboard process. The server records the update in the account audit history and best-effort removes the prior private object after the new reference commits. These routes do not create a user, modify a business profile, grant access, or expose storage credentials.

`listAccountMfaPolicies` and `updateAccountMfaPolicy` are owner-only generated contracts. The list includes all active account roles, including clients, while `/staff` remains role-minimized for its existing office readers. The update and its audit record share one transaction. If an owner requires their own unassured current session, the client must re-read `/me` and show enrollment immediately.

`listDashboardProperties`, `getPropertyTimeline`, and `listPropertyFiles` share the existing authorized read projections with native clients. Internal property fields are optional because they are omitted for clients; acreage is a nullable PostgreSQL numeric string. Timeline reads contain the latest 200 events and retain historical JSON payloads. File metadata omits object keys and bucket URLs. These endpoints remain online snapshots rather than a complete synchronization feed.

`listPropertyAreas` and `createPropertyArea` are generated contracts for `GET/POST /properties/:id/areas`. The read requires server-side access to an operational property: office roles can read it, clients need their account grant, and crew need currently assigned active work. Creation is owner/manager/dispatch-only and requires an operational property with a client. Area acreage returns as a nullable PostgreSQL numeric string; creation accepts a nonnegative number. These routes only store or read property-area records: they do not create assets, schedule or dispatch work, publish material, create billing, change QuickBooks, create a payment link, or record payment.

### Binary photo transport

`uploadFieldPhoto(id, blob, headers, options)` declares required `x-p1-property` and `x-p1-work` headers and optional classification from OpenAPI. Set the Blob MIME or an explicit supported Content-Type to `image/jpeg`, `image/png`, or `image/webp`. The generated transport passes the original Blob directly, preserving supplied `Headers` (including native authorization) and keeping typed target headers authoritative. `getPrivateFileContent` returns an authenticated Blob, never a bucket URL. Device-level file handling still requires native acceptance.

Orval 8.9.1 incorrectly serializes binary image requests using JSON.stringify. The scoped `fix-dashboard-binary.mjs` generation hook corrects only this operation after asserting the expected generator shape. Generation fails when that shape changes, requiring review rather than silently shipping corrupted uploads. Run dashboard code generation through its configured hook and the shared transport regression test after generator changes. Do not hand-edit generated models.

### Website form concurrency

`PUT /marketing/cms/forms/{id}` requires `expectedUpdatedAt`, copied from the saved form's `updatedAt` (`null` only for a legacy null timestamp). Core applies this precondition atomically with the update and returns `409` when it no longer matches, or `400` when omitted/invalid. The retained `/api/admin/forms/{id}` update has the same requirement. Clients must retain the local draft on conflict and reload explicitly; do not silently replace the precondition and retry. Form creation does not require a precondition. Deploy the updated editors and Core handler together. No database migration is needed.

#### Website event attendance

`GET /marketing/cms/events/{eventId}/attendees` lists minimized attendee records. `PUT /marketing/cms/events/{eventId}/attendees/{id}/checkin` accepts exactly `{ "attended": boolean }`. Both require the Events capability and enabled website Events feature. The atomic attendance write is scoped to the event and attendee; missing or mismatched records return 404. Repeated `true` preserves the existing check-in timestamp; `false` clears it. No payment, registration-status, email or waitlist changes occur. Payment-provider IDs and local website account links are never included in these responses. Large lists remain subject to the CMS transport response-size bound; failures must not be presented as complete attendee exports.

### Versioned agreement template library

The retained `/agreement-templates` API now supports `msa`, `scope`, `cost` and `package` templates. `GET` defaults to published MSA versions so existing recurring-estimate pickers retain their behavior; optional `kind` (`all` or a type) and `state` (`published`, `draft`, `archived`, `all`) filter the library. Sales, Agreements or template-maintenance grants permit published reads. Non-published lists and all mutations require `revenue.agreement-templates.manage`; a single draft read is also restricted to template managers. Archived versions remain readable for historical references.

Typed `POST` requests include `kind` and create a draft. The original `{name,body}` create request remains an immediately published MSA for compatibility. Responses retain the original fields and add `kind`, `status`, `description`, `payload`, `edit_version`, `family_id` and `source_template_id`. Exact structured payloads and generated clients are defined in `dashboard.openapi.json`; cost quantities support at most two decimal places, integer cents, and separate one-time/monthly/per-visit bases.

`PUT /:id` edits drafts only and requires `expectedEditVersion` plus content. `POST /:id/publish`, `/archive`, and typed `/revise` require the same edit-version precondition; `/duplicate` also accepts a new name. Stale requests return 409. Publication fixes that version’s content and archives its prior published sibling. Revision copies a published version into a new draft in the same family, leaving the current version usable until publication. Only one draft per family may exist. Duplication starts a separate draft family. Published/archived content cannot be edited in place. A package names exact published MSA/scope/cost IDs when published; subsequent source archival does not rewrite it. The legacy body-only `/revise` contract still immediately publishes a new MSA revision.

All mutations write audit events. Template editing/selection does not send messages, onboard clients, change existing estimate snapshots, schedule work, create invoices or collect payment. The existing recurring estimate path accepts published MSA templates only; component/package composition is a separate implementation track and is not implied by these library endpoints.

### Private client agreement composition drafts

`POST /agreement-drafts` requires Sales and an `operationId` UUID, title, client/inquiry context, and optional package/component selection and dates. The operation ID is scoped to the actor; repeating an identical request returns the same draft, while reusing the key for different input returns 409. Creation copies exact source IDs, versions, names, terms and structured payloads, gives client scope/cost rows independent IDs, and stores a snapshot of verified context fields. It never changes source templates or creates clients, access grants, estimates, jobs, invoices or notifications.

`GET /agreement-drafts` and `GET /agreement-drafts/:id` permit Sales or Agreements reads. Listing supports client/inquiry filters, `limit` (1–100) and an opaque cursor. Its `{items,nextCursor}` response preserves full database timestamp precision and an ID tie-breaker. `PUT /:id` requires Sales, `expectedVersion`, title, content and dates; only private drafts may be edited. `POST /:id/context` separately version-checks explicit client/property/inquiry/source-estimate context changes. Mismatched or archived context is rejected. An inquiry-only draft may retain null client/property fields until explicit onboarding, without inventing operational records. Internal creation fingerprints and keys are omitted from reads.

`POST /agreement-drafts/:id/templates/review` requires Sales, `expectedVersion`, a package/component `selection`, and unique nonempty `sections` (`terms`, `scope`, `costs`, `packageNotes`). It returns current/proposed raw content, the proposed resolved preview, source snapshots and a consistency token without updating the draft. Every requested section needs its corresponding published source; scope/cost notes travel with their rows. `POST /:id/templates/apply` accepts that same request plus `reviewToken`, resolves sources again under transaction locks, and rejects stale draft versions or changed/archived selections. Only reviewed sections change; dates, context, other content and earlier source snapshots remain intact. Applied sources append to the immutable source history, and a privileged audit event records sections, source versions and the draft version transition. The consistency token binds review content; it is not a replacement for authorization. Concurrent/repeated apply requests have one version-check winner; after a lost response, read the saved draft before trying another replacement. Review/apply row IDs may differ because newly copied client scope/cost rows receive independent identities; their reviewed content and pricing remain the same.

`POST /agreement-drafts/:id/template-export` requires both template-maintenance and Sales-or-Agreements access. `{expectedVersion,kind}` (`msa`, `scope`, `cost`) returns a reusable candidate without writing a template or changing the source. It omits client/inquiry/property/source identifiers, uses a generic name, replaces known saved/current context and linked contact/organization details, removes UUIDs/email addresses and unsupported placeholders, and gives rows new IDs. Supported placeholders remain; cost quantities reset to 1 and prices to zero while billing bases remain explicit. Preparation is version-checked. This is known-value redaction, not exhaustive personal-data detection; users must review arbitrary prose and negotiated terms. The native editor requires review acknowledgement (invalidated by edits) before saving through the existing typed unpublished-template creation path. Publishing remains a separate library action. Reviewed/published components can be assembled into a package through the existing package editor.

`POST /agreement-drafts/:id/pricing/review` requires Sales and `{expectedVersion,allocations}`. This read-only financial review locks the saved draft against concurrent edits during calculation and rejects stale/prepared drafts. Each nonempty billing basis has exactly one allocation with explicitly selected `scopeRowIds`; all saved scope rows must be covered, and intentional sharing is allowed. Cost rows are grouped by their saved basis and cannot be omitted by the caller. A one-time allocation uses its exact rounded row total. A fixed-monthly allocation supplies every calendar period as `{startsOn,endsOn,amountCents,reviewReason?}`; it reuses service-agreement term validation and requires a reason for partial periods or an amount different from the monthly rate. A per-visit allocation supplies a positive `maximumVisits` (at most 10,000), with a finite saved agreement term. It is an explicit financial allowance, not a computed schedule count.

The response includes separate rates, row references, proposed allocation ceilings, blockers, unallocated scope IDs and `pricingValid`. `authorizedAmountCents` is null when blocked; otherwise it is the sum of finite allocation ceilings, never the sum of unlike rates. Exact integer arithmetic enforces the existing $100,000,000 estimate ceiling per allocation and overall. Missing terms/scope/costs, unresolved placeholders, unsupported terms, incomplete allocations and unreviewed monthly amounts block the review. The native editor clears results after input changes and retains inputs after errors. Calculation inputs remain unsaved until the separate pricing-save action succeeds; closing discards unsaved inputs. This review endpoint does not persist allocations, create estimates, send messages, approve work, or certify current client/property/schedule/recipient eligibility. Preparation must revalidate those conditions and pricing in its own transaction before an immutable proposal can be issued.

`POST /agreement-drafts/:id/pricing` accepts the same request and requires Sales. It recomputes the complete financial review under `FOR UPDATE`, rejects unresolved blockers (422) and stale/prepared drafts (409), then increments the draft version and saves `pricing_plan` with schema/source versions, normalized allocations and reviewed limits. The returned object is the updated private draft; saving does not create an estimate or send anything. Concurrent/repeated saves of the same expected version have one winner; after a lost response, reload the draft to determine whether the plan was saved. Audit records the version transition, bases and proposed authorized maximum without copying agreement prose.

Migration `0032_agreement_draft_pricing.sql` adds the nullable plan and a database invalidation trigger. Content, title, dates, party/context links/snapshot and template-history changes clear the plan, including writes through older handlers. A version increment that reuses the old plan also clears it; an explicitly replaced plan must bind both its envelope and review to the resulting draft version. The native editor reopens saved scope/period/visit inputs, clears results after edits, and warns that subsequent content/context saves invalidate the plan. Agreements readers can see the saved proposed limits but cannot review/save new allocations. Apply the additive migration before deploying the writer; rollback retains the column and invalidation trigger to prevent stale plans. Financial plan persistence is not proposal preparation: the later preparation transaction must validate current eligibility, scheduling, recipients and allocation-specific operational/billing safeguards, then snapshot the reviewed agreement into the existing Sales workflow.

The allocation boundary is implemented by additive migration `0033_estimate_allocations.sql`. One estimate retains the overall customer authorization. Its `estimate_allocation` rows identify the one-time, fixed-monthly and per-visit portions, each with approved scope, exact ceiling, row references and recurring configuration. There is at most one allocation per basis. Issuing an allocated estimate requires the ceilings to sum exactly to its amount and captures the allocation rows in the same immutable document snapshot; issued allocations cannot be changed, deleted or moved. No historical estimates are backfilled with inferred allocations.

Jobs, recurrences, service agreements and billing drafts carry nullable `estimate_allocation_id`. Composite foreign keys bind allocations to the correct estimate. Operational writes validate the property, approved parent and matching billing basis; an allocated record cannot switch authorizations. The original one-recurrence-per-estimate index remains for unallocated estimates, while allocated estimates support one recurrence per component. Recurring visits retain their recurrence link; agreement charges derive allocation identity from the approved service agreement.

Both `POST /billing` and project-phase billing intents accept optional nullable `estimateAllocationId`, required for an estimate with allocations. The shared helper and database trigger enforce the component and global remaining amounts, including every existing billing draft regardless of posting status. Missing or foreign allocations are rejected. Charges cannot be moved to another authorization to free capacity. Every allocated billing writer serializes on the parent estimate, including concurrent writes through different paths. Recurring agreement creation/activation uses the selected basis's approved scope; approved dates, billing mode, rate and monthly period charges must match its immutable configuration. Activation also checks the reviewed recurrence cadence/interval/time. Its financial previews report component-approved/billed amounts, with remaining capacity bounded by both caps. The separate `maximumVisits` in per-visit configuration must reconcile exactly with rate times authorized ceiling; it remains a financial allowance, not a promise that a schedule contains that many visits.

Apply this migration before allocation-aware API code. The current composition workspace does not yet create these estimate allocation rows: transactional preparation, composed public/PDF rendering, approval fan-out and revision/change-order handling remain pending. Historical unallocated estimate behavior stays intact. Rollback after allocated records exist must retain the table, columns, guards and replacement unique indexes; restoring the older single-estimate recurrence index would conflict with legitimate multiple components. Quiesce new allocated writers and use a compatible reader/writer during rollback. Do not drop or flatten allocations into legacy records.

`GET /estimates/:id/billing-allocations` requires Billing capability, an operational property and an approved estimate. It returns the parent approved/billed/remaining cents plus up to three component choices (`id`, `title`, `basis`, approved/billed/remaining cents). The short read transaction uses the same parent lock and capacity calculation as billing writers. No contract prose, source-row references, configuration, contacts or unrelated billing drafts are returned. Sales/Agreements grants alone do not authorize this financial read. This is a current snapshot, not a reservation; every write rechecks both limits.

The general billing dialog and project-phase preparation now use a shared native allocation picker. Loading/error states block preparation with a retry action, allocated estimates require an explicit component, exhausted choices are disabled, and older unallocated estimates show their ordinary remaining authorization without an extra selection. Changing the estimate clears the old choice and ignores late responses. Project changes clear the phase billing context and ignore superseded phase-list loads. The selected component's remaining amount is shown separately from the overall estimate amount.

Phase billing retains the exact operation ID, version, amount, estimate and allocation after a failed or lost response. Details and project switching stay fixed until retry succeeds or the user explicitly starts a different request after checking existing drafts. Navigation warns while an unresolved request is held. This in-memory retry state does not survive a hard reload; reload/recovery must inspect existing drafts before starting another request. A successful retry clears the prior error and closes the form. General billing continues to use its existing retained operation ID. Neither form posts invoices or contacts clients.







Read responses include raw draft content, immutable source snapshots and a derived preview. Supported placeholders are `client.name`, `client.billing_address`, `property.name`, `property.address`, `agreement.prepared_on`, `agreement.starts_on` and `agreement.ends_on`, written as `{{name}}`. Unknown/missing values remain visible in `unresolvedPlaceholders`. Substitution is single-pass plain text; this endpoint does not render HTML. Context refresh updates only snapshot values, not manually edited text. Costs use exact integer arithmetic with per-line half-up cent rounding and separate `one_time`, `fixed_monthly` and `per_visit` totals; unlike billing bases are never combined into a contract total.

All newly selected components must be published, including the fixed components of a selected package. An archived component makes that package unavailable for new draft creation until it is revised; saved client drafts retain their copies unchanged. Draft-to-estimate preparation and customer publication are not implemented by these endpoints and must separately validate placeholders, operational context and supported billing allocation.


### Estimate document rendering

Customer estimate JSON now includes the estimate's saved `agreement_template_snapshot` alongside its additional `terms`. Customer review and PDF render both explicitly, preserving newlines. PDF generation uses pagination, wrapped text, embedded fonts and page numbers; the former single-page writer has been removed. Both views use the shared `formatEstimateExpiry` formatter to identify the same expiry date/time in UTC, rather than silently displaying different dates in different time zones.

Both send paths validate the captured document against the PDF font repertoire before committing status or queuing recipients/messages. An invalid document rolls back the send and snapshot together. Unsupported code points return 422 with their Unicode codes; they are not silently omitted or substituted in legal content. Existing sent records still undergo this check when downloading. Adding coverage requires an appropriately licensed font and updated coverage metadata. New sends freeze client/property labels and complete document content as described below. Composed-agreement preparation remains separate.


### Issued estimate document snapshots

Migration `0031_estimate_document_snapshots.sql` adds nullable `estimate.document_snapshot`. The database captures schema version 1, capture time, party labels/address, scope, terms/MSA, prices, ordered line items, expiry, template references and recurring configuration during the first draft-to-sent transition. Readers overlay this content on live lifecycle status, approval timestamps and revision availability. Current client/property edits therefore cannot change an issued document. Public review now includes the captured property address and shows the estimate revision, matching PDF context.

After capture, database triggers reject content/snapshot changes, deletion, returning to draft, and all line-item mutations. Line writers lock their parent estimates in stable order so a send includes earlier committed edits and rejects later writes. The existing revise endpoint creates a new row with no snapshot, preserving the older issued document and invalidating its public link through the existing `is_current` check. The revised row captures its own document when sent.

Older issued rows remain nullable; the migration does not invent historical party details. Office document reads report `document_snapshot_state` as `draft`, `frozen` or `legacy_live` and `document_captured_at` when known. Use `estimate-document-reconciliation.sql` to inventory missing snapshots in the authorized rehearsal. Reconcile against retained source evidence before retirement; do not label a current-record capture as the originally issued document.

Deployment order: apply the additive migration, then deploy the snapshot-aware reader/writer together. Rollback must preserve the snapshot column/data and protection triggers and use a compatible reader. An older reader that rejoins current party labels would violate the new document guarantee. No production migration has been applied by this implementation task.

### Composed proposal preparation boundary (internal, not an endpoint)

`prepareComposedEstimate` requires Sales capability and consumes a draft's current saved pricing review plus `expectedVersion`, validity days (1–90), optional project ID and exactly one schedule per recurring billing basis. A schedule specifies weekly/monthly cadence, interval, local time and a first visit within the saved term. It never derives maximum billable visits from recurrence cadence or flattens monthly/per-visit rates into one-time prices. Active client and operational property linkage are required; inquiry drafts must complete explicit onboarding first. Changed party details block preparation until context refresh and repricing.

Migration `0034_composed_estimate_preparation.sql` adds the composed estimate kind, immutable preparation snapshot, priced-line component/source references and private request fingerprint. Assembly is transactional and deferred database checks require a complete seal with reconciled caps and exact component cost rows. Request retries with the same normalized body recover the already-linked proposal. The draft version advances once, with its pricing plan rebound to that version; the immutable estimate retains the original reviewed source version.

Apply 0033 before 0034 and 0034 before any code calling the preparation service. Do not expose that service before composed public/PDF projections, approval conversion and revision handling are available: legacy one-time/recurring consumers must not interpret mixed-basis proposals. Releasing only this foundation does not authorize enabling preparation or production migration. Rollback should disable creation/entry points and retain additive columns, guards and any prepared records for reconciliation; do not coerce composed estimates into one-time estimates or drop their snapshots/allocations. No historical data backfill is included.

### Composed customer document projection

Estimate document reads and public token reads now add `composition_document` for composed proposals. This schema-version-1 projection contains dates, shared contractual text, authorization maximum and ordered components with scope descriptions, price rows, rates, charge limits, explicit monthly periods/maximum visits and reviewed schedules. It deliberately excludes internal source row IDs, template provenance, actors, pricing review explanations and preparation fingerprints. Legacy estimates omit this field. Public HTML and PDF use the same ordered formatter; the full internal preparation snapshot is never included in the public response.

Migration 0035 follows 0034 and runs its issuance trigger after standard document/allocation capture. It rejects sending when the property/client is archived, nonoperational or assigned to a different client, and captures reviewed party labels rather than later display-name changes. A client-portal document read requires current property access and access to the original prepared client. Do not enable preparation solely because document rendering is available: approval must create every authorized component and revision/change-order consumers must preserve the composition before enabling that endpoint. Rollback retains the frozen snapshots and guards and disables composed entry points; there is no historical backfill or destructive down migration.

### Composed approval conversion

The existing public-token and client decision endpoints now support prepared composed estimates. Approval locks the parent and creates one unscheduled one-time job (when allocated) and one paused recurrence/draft service agreement per recurring basis, all in the same transaction. Recurrences use the reviewed first visit rather than the agreement start date, and agreements preserve exact authorized terms, allocation scope, MSA and accepting contact. No automatic activation or billing occurs. Approval receipts include `components` with allocation/basis and created job/recurring-service/agreement IDs in addition to the existing primary `jobId` and `recurring` fields. A repeat after the winning decision retains existing unavailable-link/conflict behavior and never converts twice.

Composed decisions recheck that the active operational property still belongs to the prepared client and that the contact or portal actor retains that client's authority. Persisted component scope/configuration/caps must match the customer-reviewed snapshot. Component/approval/audit failures roll back the entire conversion. Deploy after migrations 0033–0035; this support alone does not enable the still-internal preparation endpoint or remove the requirement to complete revisions and operational scheduling constraints.

### Allocated recurring visit limits (migration 0036)

New allocated recurring work is linked through `work_order.service_agreement_id`, separately from one-time estimate allocation references. Database functions resolve the unique effective allocated agreement for an occurrence and check its active/cancelled term, first authorized visit, current client/property and reviewed recurrence configuration. The scheduled date must also remain within the effective term. Existing linked visit provenance is immutable. The generator filters before its 50-service limit and rechecks after acquiring recurrence locks; ordinary inserts and reopening released visits use the same allowance boundary.

An uncharged cancelled/skipped visit releases capacity. Every other reserved visit, and any visit with a charge, consumes capacity. Unlinked historical visits in the applicable term (or lacking an occurrence date) conservatively consume capacity without receiving invented provenance. Before cutover, reconcile these records and expose remaining authorization in the operational UI. A retained legacy recurrence becomes governed when an allocated agreement takes effect; an effective earlier unallocated agreement keeps its original term behavior.

Deploy 0036 after 0033–0035 and before the updated generator. Disable the generator before rolling application code back; retain the additive reference, index and guards rather than dropping history or allowing an old worker to repeatedly hit exhausted authorization. No production migration or automatic historical mapping is authorized by this implementation.

`GET /recurring-jobs` additionally returns `generation_status`, `visit_allowance`, `visits_reserved` and `visits_remaining`. The count fields are nullable: null means there is no per-visit allowance for the next occurrence, not zero authorization. The endpoint remains restricted to `operations.recurring`. The status/count query uses one database statement and includes no new financial figures or private pricing configuration. Results are observational; writes and the generator recheck authorization independently.

### Internal composed revision lineage (migration 0037)

`agreement_composition_draft.revises_estimate_id` and `revises_estimate_revision` distinguish a true replacement from ordinary contextual `source_estimate_id` linkage. They must be supplied together, identify a positive revision and match the source estimate ID. Internal revision creation requires Sales access, a current unapproved composed estimate and a stable operation ID; it creates editable independent rows without copying pricing authorization. It leaves the original proposal current until successful replacement preparation.

Preparing a replacement locks and verifies the source, preserves client/property and series lineage, increments the estimate revision and supersedes the source in the same transaction as all replacement allocations, lines and snapshots. Approval of the source prevents replacement; a structured change order is required instead. Deploy 0037 before this code is enabled. Rollback must retain original/replacement snapshots and lineage and disable new revision preparation rather than resetting current flags or coercing composed documents through legacy flat revision endpoints. Native entry points and complete change-order integration remain pending.

### Internal additional-work change orders (migration 0038)

`agreement_composition_draft.change_order_estimate_id` records an approved parent for a new additive authorization. It must match `source_estimate_id` and cannot coexist with `revises_estimate_id` on the same draft. The internal Sales-only creator requires the expected current approved revision and a stable operation ID. It preserves reviewed terms/history but initializes empty new scope/cost rows and dates, with no pricing plan. Approval of the new proposal does not replace the base agreement or authorize its work a second time.

Preparation retains client/property, defaults to the parent's project, and creates a separate estimate series with `change_order_for`. The customer `composition_document.changeOrder` contains only the parent title and revision; source IDs remain internal. This is additional-work authorization, while existing cancellation/successor procedures govern replacements or reductions of active recurring commitments. Apply 0038 before enabling the creator, and retain all additive lineage/snapshots on rollback. Native entry points remain pending.

#### Composed proposal preparation and related drafts

`POST /agreement-drafts/:id/prepare` requires `revenue.sales`, a current saved pricing review, `expectedVersion`, and explicit schedules for each recurring billing basis. It creates a sealed draft estimate (201), or returns the same result for an exact retry (200). It does not send the proposal or create operational work. Changed preparation input or stale versions conflict. Optional `validForDays` defaults to 30; optional project membership is validated server-side.

`POST /estimates/:id/agreement-revision` requires Sales, `operationId` and `expectedRevision`; it opens a repricing-required composition draft from a current unapproved composed proposal. Preparing the replacement atomically advances its series revision.

`POST /estimates/:id/agreement-change-order` requires Sales, `operationId`, `expectedRevision` and `title`; it opens an additional-work draft from a current approved composed proposal, retaining reviewed terms but clearing scope, costs and dates. Its prepared estimate is a separate authorization and does not alter the original approved estimate. Both draft-creation endpoints return 201 for creation or 200 for an exact operation retry.

`POST /recurring-jobs/:id/activate` retains its Operations capability and assignee/date/time input contract. It now locks through the shared service-agreement context, checks the submitted date is within the accepted term, and rejects allocated proposals whose submitted first visit or local time differs from the approved allocation. Successful activation changes both the accepted draft agreement and paused recurrence atomically. The UI prefills and locks those approved values; server-side checks remain authoritative.

`GET /user-management/users/:id/history` remains Owner-only and now accepts `limit` (1–100, default 50) and an opaque account-bound `cursor`. It returns `{ items, nextCursor }`; null marks the final page. Events are ordered newest first by exact timestamp and ID. Newer concurrent events appear on refresh rather than shifting older pages. Invalid or cross-account cursors return 400. Consumers should append pages by event ID and retain the current page/cursor when a request fails.

`GET /user-management/invitations` now accepts `limit` (1–100, default 50) and an opaque invitation `cursor`, returning `{ items, nextCursor }`. All invitation lifecycle states remain available; null marks the final page. Ordering uses exact creation time plus ID. The endpoint remains Owner-only and excludes token hashes. Invalid cursor/query input returns 400. Resend/revoke consumers should refresh the first page to show current lifecycle state.

#### Careers bridge

The `/marketing/cms/careers` API retains Core Careers storage and feature gating. `GET/POST /jobs`, `GET/PUT/DELETE /jobs/:id`, `GET /applications`, `GET/PUT /applications/:id` and `GET /applications/:id/resume` require `marketing.content.careers`. Application updates support status and notes. Resume responses use the bounded binary transport and attachment disposition. `GET/PUT /settings` require Owner access; integration secrets are not available to ordinary Careers users. Job author fields are assigned from the linked actor, with original authorship retained on updates. OpenAPI documents the typed clients. This does not enable public recruiting or migrate its data.

Careers settings treat blank credential fields as unchanged; nonblank values replace the stored encrypted credential. Settings are persisted in one transaction. Reads stay redacted. Explicit credential removal controls remain part of the pending Owner settings UI work.

Careers job updates accept optional `expectedUpdatedAt` (ISO timestamp). Native dashboard edits always send the saved `updatedAt`; a changed job returns 409 and retains the existing record. Timestamp versions advance monotonically at millisecond precision for JSON round trips. Legacy callers remain compatible when the field is absent. The Careers editor requires a loaded version for existing jobs and preserves local edits after failed saves.

Careers application updates accept optional `expectedUpdatedAt` and a note of at most 50,000 characters. Native review always sends the loaded timestamp. Status and its note are saved in a single locked transaction; a stale request returns 409, a missing application returns 404, and note-write failures roll back the status update. Note-only writes advance the version. Repeating an old version cannot duplicate its note. Refresh application detail after saving to retrieve the current chronological notes.

### Careers settings concurrency

`GET /api/v1/marketing/cms/careers/settings` returns a fresh, redacted snapshot with an opaque `version`. Business Center PUT requests must include that loaded version: missing/malformed versions return 400 and stale versions return 409 without writing settings. Reload and reconcile local changes before retrying. Blank credentials retain their existing stored values. The Owner and Careers feature gates still apply.

Deploy the updated Core service before enabling this versioned dashboard editor. The editor disables saving when an older service omits the version. Non-federated legacy callers may omit it for compatibility; their writes still invalidate later versioned saves. No migration is required.

Careers settings PUT also accepts `clearCredentials`, an explicit array containing any of `googleServiceAccountJson`, `indeedApplySecret`, `zipRecruiterApiKey` or `genericWebhookSecret`. Removal requires the current version, the corresponding integration flag false, and no nonblank replacement for the same credential. Invalid combinations return 400; stale versions return 409. The integration flag and selected empty encrypted values commit together. Unselected blank fields continue to preserve stored credentials. Responses remain redacted settings snapshots and do not include removal instructions. Release the updated Core contract before the dashboard removal controls.

### Careers deletion and recruiting history

`DELETE /api/v1/marketing/cms/careers/jobs/:id` requires JSON `{ "expectedUpdatedAt": "<loaded ISO timestamp>" }`. Missing/malformed versions return 400, stale versions or jobs with applications return 409, missing jobs return 404. Jobs with applications must be archived through the ordinary versioned update; the retained deletion service also applies this history protection to non-federated callers. The gateway forwards a DELETE JSON body only for this allowlisted operation. Deploy Core history protection and gateway body forwarding before exposing the native delete control.

### Native Sales inquiry notes

`GET /api/v1/leads/:id/notes` and `POST /api/v1/leads/:id/notes` require a fresh authenticated account with `revenue.sales`; customer/crew portal membership and former staff role names do not grant access. Responses are private/no-store. Reads return `{items:[{id,body,createdAt,authorName,imported}],nextCursor}`. Default page size is 30, maximum 100; use the returned opaque cursor with the same inquiry. Exact UTC microseconds and ID order prevent losing notes that share a timestamp. Null historical authors remain visible through a left join; source identity details stay server-side.

Creation requires `{id: <new UUID>, body: <1–10000 characters after trimming>}`. Generate the UUID once per intended note and preserve it plus the exact text for any uncertain-response retry. One successful insert returns 201; an identical same-author/same-inquiry replay returns 200. Conflicting reuse returns 409. Unknown fields, including browser-supplied authors and provenance, return 400. Only the authenticated actor becomes the author. The note and one `lead_note.created` audit event commit atomically. Notes do not overwrite lead status, ownership or follow-up fields. No edit/delete endpoint exists; database triggers reject updates and deletes.

Apply additive migration `0039_lead_notes.sql` before releasing the API/UI. The existing customer-note table is unchanged. Nullable historical authors require source instance/note provenance; source IDs are unique within an instance. The native API cannot set those migration columns. The CRM importer and completed-task migration are still pending. For application rollback retain the table and its history; do not drop it or disable its append-only trigger. The previous application can run with this added table, but does not display its notes. Back up and reconcile added history before any later retirement or schema removal.

### CRM follow-up tasks

Task endpoints use `/api/v1/leads/:id/tasks` for Sales (`revenue.sales`) and `/api/v1/clients/:id/tasks` for Customers (`customers.clients`). Each request checks the current account and its separate area grant; clients and crew cannot use either office API. Task IDs never override the parent or area scope. Archived clients return 404. Responses are private/no-store.

- `GET` the collection with `state=open|completed|all` (default open), optional `limit` (default 30, maximum 100) and the returned `cursor`. Results contain `items` and `nextCursor`. Cursors bind the parent, area and filter and retain exact timestamp/ID ordering. Refresh when tasks change state during paging; pages are not a frozen snapshot.
- `GET /assignees` returns only IDs/names of active Owners or staff with the relevant area grant. New assignments are checked again inside the write transaction, with profile/access rows locked. An unchanged historical assignment may remain while a task is completed or edited.
- `GET /:taskId` returns the saved task including version, creator, assignee and timestamps; unavailable historical names remain null rather than dropping the record. Source identity fields and creation replay payloads are not exposed.
- `POST` requires `{id,title,dueAt,assignedToId,completed}`. Generate the UUID once per intended creation. `title` is trimmed and limited to 2,000 characters; due date is an offset-bearing ISO timestamp or null, assignment is an eligible account ID or null. First creation returns 201; exact normalized same-actor/same-parent retries return 200, including after later edits. Conflicting reuse returns 409. Retries never revert later task edits.
- `PATCH /:taskId` requires `{expectedVersion,title,dueAt,assignedToId,completed}`. Stale versions return 409. Reload and review after stale or unconfirmed saves rather than replaying an old edit. Genuine changes increment the version; unchanged values do not create a new revision. Creation and edit schemas reject unsupported author, source and parent fields.
- `GET /:taskId/history` returns `items` and `nextBeforeVersion`; pass `beforeVersion` plus optional `limit` (maximum 100) to continue. Each revision retains title, due date, completion, assignment and changing actor. `recordedAt` is the time that revision was recorded, not an inferred historical completion time.

Migration `0040_crm_follow_up_tasks.sql` adds one task store with exactly one lead/client parent and an append-only revision store. Source IDs are unique per instance and source entity type. Native creation records an immutable normalized request for retry recognition. Task origins cannot be changed, versions must advance by one, and triggers retain revisions. Audit insertion, task state and revision insertion share one transaction; failures roll back together. No task deletion endpoint exists and database triggers protect history. Completed tasks can be reopened through a versioned edit. These follow-ups do not create work orders, calendar appointments, notifications, onboarding or billing.

Apply the migration before the API/UI release. Older application code can coexist but will not show native tasks; retain both tables on rollback and back up their new history. Do not drop tables or disable preservation triggers to roll back the interface. The Core payload importer is still pending: validate source field lengths, timestamps, author/assignee mappings and original completion state without truncating or silently substituting identities. Source timestamps/provenance are retained separately from newly recorded revision times.

### Complete customer-note history and provenance

The existing customer note store now supports `GET /api/v1/clients/:id/notes`, guarded by `customers.clients`. It returns `{items,nextCursor}` using the existing `ClientNote` projection, with nullable `author_name` and an `imported` indicator. Missing historical accounts no longer hide notes: both workspace preview queries use left joins too. Previews retain their 30-note limit; the customer Notes & tasks screen uses the full paginated endpoint. Default page size is 30, maximum 100. Reuse the opaque cursor only with the same client; exact timestamp/ID ordering retains sub-millisecond records. Responses are private/no-store, and archived clients return 404.

`POST /api/v1/clients/:id/notes` retains `{body,propertyId?}` compatibility and accepts optional `id` (UUID). The native editor always generates one ID per intended creation and keeps the same ID, text and property scope for uncertain retries. First creation returns 201; identical same-actor/same-client/same-property retries return 200 with the original ID. Conflicting reuse returns 409. A retry of an already saved note remains valid after its property is archived; creation of a new note still requires an active operational property belonging to that client. Legacy callers omitting `id` keep server-generated IDs and do not gain automatic retry protection.

New notes use the authenticated actor. Unknown request fields—including authorship and source metadata—are rejected. Client/property eligibility is checked under transaction locks; note insertion and its audit entry commit atomically. The existing append-only protection is unchanged.

Apply `0041_customer_note_provenance.sql` before the updated API. It extends `client_note` with source instance/note/author IDs, enforces source-note uniqueness and permits null local authors only with complete source provenance. Existing native rows remain valid without rewriting their contents. No second CRM customer-note store is introduced. The importer must still reconcile source content, lengths and timestamps without truncation or invented authors. No import runs as part of this migration.

Rollback should retain the new columns, indexes, constraints and history. Do not restore `author_id NOT NULL` after imported null-author records exist. An older server's inner-join previews would hide those records; retain the updated read path if rolling back the frontend, or explicitly account for that visibility limitation in release recovery. Do not delete historical records to make an older application appear compatible.

### General inquiry follow-up

`GET /api/v1/leads/:id/follow-up` requires `revenue.sales` and returns `{lead,owners}`. The lead projection includes only its name, follow-up/status/version fields and existing conversion IDs; owner choices expose only IDs/names for active Owners and staff with Sales access. Responses are private/no-store. Missing inquiries return 404.

`PATCH` uses the existing `CommercialFollowUp` request: `{expectedVersion,ownerId,nextAction,nextActionDueAt,status}`. Owner and due date may be null, next action is required (trimmed, 1–2,000 characters), and supported stages are new/contacted/qualified/proposal/won/lost. Owner eligibility is checked under profile/access-row locks. A matching lead version is updated once together with its audit event; stale or missing leads return 409, invalid owners/requests return 400. Unsupported fields cannot alter conversion IDs or intake content. On an uncertain response, reload the saved state before another edit.

General and commercial endpoints now use one service and the same lead version. The older `/commercial-inquiries/:id/follow-up` remains commercial-only and retains its response shape and `commercial.followup_updated` audit action. General inquiry updates record `lead.followup_updated`; commercial rows retain the commercial audit action even through the general endpoint. No schema migration is needed.

Changing stage to Won only records the pursuit outcome. It does not create/link clients, properties, portal access or billing. Explicit create/link-customer onboarding is now available through the separate workflow documented below; changing the stage alone still performs no onboarding.

### Full Sales inquiry history

`GET /api/v1/sales/inquiries` requires `revenue.sales` (or active Owner). It returns `{items,nextCursor}` with private/no-store caching. Optional filters: `q` (literal case-insensitive name, email, phone, company, location or owner search; maximum 200 characters), `status` (`new`, `contacted`, `qualified`, `proposal`, `won`, `lost`), `ownerId` (or `unassigned`), `kind` (`all`, `general`, `commercial`), and `overdue=true` (past due excluding Won/Lost). `limit` defaults to 30 and is bounded to 1–100. Exact timestamp/UUID keyset cursors are bound to the filter set; changed filters require starting a new list. Results include commercial leads without ingress receipts and legacy general leads. Search input is escaped as literal text and passed as a query parameter. No raw intake payload or user-security record is projected.

The existing `GET /leads` array contract remains unchanged for compatibility; the native Sales screen now uses this paginated endpoint. Creation-date ordering avoids moving rows when follow-up fields change. Concurrent additions and changes to filter membership appear on explicit refresh; this is not a frozen database snapshot. Additive migration `0042_inquiry_history_index.sql` supports the history ordering. It has no data transformation. Rollback may remove that index after rolling back the application, without deleting inquiry records. No production migration was executed.

### Inquiry contact corrections and history

`GET /api/v1/leads/:id/details`, `PATCH /api/v1/leads/:id/details` and `GET /api/v1/leads/:id/details/history` require current `revenue.sales` access (or active Owner) and return private/no-store responses. Read/update projections contain only `id`, `version`, `name`, `email`, `phone`, `location`, `description`, and `reported_company_name`. Updates require `expectedVersion` and all six editable fields. Name is trimmed and required (200 characters maximum); email is nullable/empty or a valid address (320 maximum); phone/company allow null/empty (100/300 maximum); location/message allow empty strings (2,000/10,000 maximum). Unknown fields are rejected. This does not verify email ownership.

A row lock and the shared inquiry version serialize detail/follow-up/context changes. Stale saves return 409; missing inquiries return 404. Unchanged normalized detail values return the current version without adding history or audit entries. Changes advance the version and record changed field names (not contact values) in the audit event. Detail changes, history and audit commit atomically. Linked client/property records, stage, ownership, source attribution, commercial receipts and saved agreement snapshots are not modified.

History returns `{items,nextBeforeVersion}`; each item contains `version`, `kind`, `fields`, `recorded_at`, and nullable `actor_name`. Optional `beforeVersion` and `limit` (1–100, default 30) page the append-only history. Missing actor identities remain visible. Versions may have gaps because other inquiry edits share the counter. Migration `0043_lead_detail_history.sql` records a `baseline` for existing records, explicitly representing migration-time values rather than an original submission. New records receive `created` entries, and detail changes receive `edited` entries. Legacy creation paths without an identified transaction actor retain null authorship. Commercial source receipts remain the original intake source.

Apply 0043 before deploying the new detail editor. Back up and rehearse the baseline insert against the production inventory before release. Application rollback can retain the additive history table/functions/triggers; do not delete collected history during rollback. Detail-changing SQL must advance the inquiry version by one. No production migration was executed as part of implementation.

### Imported CRM history

`GET /api/v1/leads/:id/crm-archive` requires `revenue.sales`; `GET /api/v1/clients/:id/crm-archive` requires `customers.clients`. Current active Owners have access to both. Crew/client identities and other area grants do not grant archive access. Each request rechecks access and returns private/no-store responses. No mutation routes exist.

Lists return `{items,nextCursor}`. Items contain source instance, collection, source ID, content SHA-256 and microsecond import time only. `limit` defaults to 20 (1–50). Keyset cursors use the full source-instance/collection/source-ID tuple and are bound to parent type and ID. Refresh starts a new list; new imports sorting before the cursor appear on refresh. Archived records themselves are immutable.

`GET` on either path plus `/record` requires `sourceInstanceId`, `collection` and `sourceId` query parameters. The parent ID and capability remain mandatory and cannot be replaced by knowing a source key. Missing parents or mismatched source/parent identities return 404; malformed IDs, query keys or cursors return 400. Detail adds `sourceJson`, the original archived JSON rendered as text by PostgreSQL, preserving numeric precision without JavaScript JSON-number parsing. Values are historical and are not overlaid onto current operational fields. Internal review/operator metadata is not projected.

Native Sales inquiry/commercial views and Customers → Notes & tasks expose a lazy read-only history viewer. It renders source text literally, supports pagination and retry, and preserves neighboring unsaved notes. There is no public portal/crew exposure or automatic link navigation from archived text.

Apply archive migration 0044 before this feature; additive 0045 adds parent/source-order indexes for bounded list queries. Neither migration is a production import. Application rollback can retain these indexes and the archive. Full source-field editing/native workflow parity, reviewed live import, backup/restore rehearsal and retirement remain separate gates.

### Won inquiry customer onboarding

`GET /api/v1/leads/:id/onboarding` and `POST` on the same path require both current `revenue.sales` and `customers.clients` access (or active Owner). Responses are private/no-store. GET returns inquiry ID/version/stage and its linked customer ID/name/archive status. The Sales inquiry list and commercial detail offer this workflow only to users with both grants.

POST accepts exactly `{operationId,expectedVersion,customer}`. `customer` is either `{existingId}` or `{create:{name,email,phone}}`. Name is explicitly reviewed, trimmed and required (300 characters); email is nullable or a valid address (320), phone nullable or nonempty text (100). Source contact details are not silently copied or used for fuzzy matching. Existing customers must be active. New operations require current inquiry version, Won status and no existing converted customer/property. Known organization/contact/property associations must be active and cannot contradict the selected customer. Unassigned prospect associations remain preserved without being silently promoted into operational records.

The operation locks the inquiry and relevant associations, then commits customer creation/link, the inquiry version, an immutable replay receipt and audit together. It leaves stage Won and original intake/contact values intact. No property, portal grant, accounting customer, agreement or invoice is created. New-customer duplicates are not automatically merged: the user must review the existing customer list before choosing creation. Shared inquiry version conflicts require refresh. Operation UUIDs are serialized across inquiries; exact actor/input replays return the original receipt without creating another customer, even if that customer was subsequently archived. Changed actor/input or a changed saved customer link returns 409. The UI retains the exact submitted operation for unconfirmed-result retries and freezes its inputs until resolved.

Migration 0046 adds `lead_customer_onboarding`, one immutable receipt per inquiry, with original operation, customer, actor, request fingerprint, mode, version and timestamp. Apply before deploying this workflow. Retain receipts and linked customers on application rollback; deleting history or unlinking customers is not a rollback mechanism. Production migration has not run.

The old property-creating conversion button is removed from the inquiry list. The compatibility `/leads/:id/convert` API remains for existing callers: a previously onboarded customer cannot be replaced, must be active, and explicit property creation preserves Won status. Its preexisting prospect-context guard remains. Prospect context creation/editing stops once a customer is onboarded to prevent a conflicting reassociation; previously completed context-operation replay remains unchanged. Explicit operational adoption of prospect property/contact/organization context and full agreement-to-operations rehearsal remain open work.
