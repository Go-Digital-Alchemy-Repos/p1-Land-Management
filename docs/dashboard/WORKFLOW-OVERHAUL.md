# Dashboard workflow overhaul: review record

The approved direction is a single light-theme dashboard release. Existing colorful icons, business rules, stored records, permission boundaries and public website styling remain in place. The [clickable synthetic preview](../../artifacts/p1-dashboard/preview/workflows.html) is a design-review artifact; it does not connect to the API or stand in for authenticated staging acceptance. Staging alone may expose it at `/design-preview/` when `DASHBOARD_DESIGN_PREVIEW=enabled`; both HTML and script return 404 without that explicit flag. Production must leave the flag unset.

## Route and role audit

The canonical route inventory is `DASHBOARD_PAGES` in `artifacts/p1-dashboard/src/dashboard-routes.ts`. Every listed route was opened on the live Owner dashboard during this review. Dynamic screens can still load after their initial shell, so the route sweep establishes reachability and page structure rather than asserting that every asynchronous state was visually accepted. The following groups need final desktop, tablet and phone inspection against the integrated build:

| Workspace | Routes reviewed | Finding and action |
| --- | --- | --- |
| Workspace | `/`, `/my-day`, `/profile` | Overview and My day have distinct task surfaces. Generic heading descriptions elsewhere were replaced with screen-specific copy. Verify keyboard navigation, recovery and offline states in staging. |
| Customers | `/properties`, `/clients`, `/requests`, client and property deep links | Preserve the approved map and record layouts. Client overview links its internal Sales-origin inquiry only when the actor has Sales access. Account sections are deep-linkable navigation. A narrow-granted request/project operator now receives only client-scoped property IDs and names needed to create work; inaccessible portfolio and agreement summaries are hidden. Crew property lists omit private access instructions and notes. Review dense record tabs and mobile tables in staging. |
| Operations | `/schedule`, `/recurring`, `/projects`, `/inspections`, work-order deep links | Project phase fetch failures now show Retry instead of claiming no phases, and client-visible phase notes and inspection reports require explicit review before publication. Recurring now uses date-grouped visits and expandable service records on phones, with the permitted activation action in the record. Schedule now opens only the selected work record beneath the calendar; its phone month view is a compact agenda. Month reads split the 42-day calendar into API-supported ranges. Inspect task density and error states at three breakpoints. |
| Revenue | `/sales`, `/sales/pipeline`, `/sales/pipeline-settings`, `/agreements`, `/agreements/drafts`, `/agreements/templates`, `/billing`, `/expenses` | Sales Overview and Pipeline previously exposed duplicate inline inquiry forms. They now open one direct-linkable profile. Pipeline colors now use valid light-theme tokens. On phones, a stage selector replaces the six stacked columns; the first populated stage opens initially. Agreement and draft deep links show retryable failures instead of silently refetching or rendering an unrelated list. Billing/Expenses suppress false empty states after a global refresh failure. Billing and Expenses now have searchable, expandable records; QuickBooks posting retains an explicit review, retryable error and duplicate-click guard. Provider-backed staging checks remain open. |
| Marketing | Content, Design, Website System and Reporting routes listed in `DASHBOARD_PAGES` | Existing CMS authoring, versions and grants remain intact. Route-specific headings replace generic copy. Website Menus now separates load failure from a truly empty list and offers Retry. Complex editor visual/accessibility and configured-provider checks remain open. |
| Settings | `/settings/people`, `/settings/security`, `/settings/integrations`, `/settings/preferences`, `/settings/term-libraries` | Headings now state each setting's purpose. User Manager distinguishes a failed directory load from no accounts, blocks invitations until recovery, and labels its dialog. Owner-only access and read/write grants must be exercised in staging with representative accounts. |

The role review covers Owner, Sales, Dispatch, Finance, Crew and Client. The UI route guard is tested for direct links; the server integration tests exercise staff grants and portal isolation using synthetic accounts. Final acceptance still needs real browser sessions for each role, including an attempt to visit a forbidden deep link and a permitted action on its own screen. No live customer's account should be used for these checks.

Marketing/Settings actions still use some generic editor patterns; those and populated Billing/Schedule records need further staging visual review. Billing and Expenses still share a broad dashboard refresh; the current failure state is explicit and retryable, but per-resource recovery remains open.

## Sales interaction contract

Sales Overview is the priority list, Pipeline is the stage scan, and `/sales/leads/:id` is the single inquiry record. The tabs are Overview, Follow-up, Inquiry details, Activity, commercial Assessment when available, and Client handoff when the actor has both Sales and customer access. Contact corrections and follow-up edits keep their version checks and explicit save/cancel controls. A failed refresh hides stale editable profile content.

Won records the Sales result. It does not imply an executed agreement. The handoff remains an explicit, replayable operation after Won. It creates or links a client without rewriting the original inquiry, creating a property, sending a portal invitation, or posting accounting work. The internal client overview links back to its source inquiry for Sales-authorized staff; the client portal receives no Sales history.

## Validation and release gates

- Dashboard typecheck and build pass. The build reports existing large chunks; evaluate code splitting against measured load before changing bundle boundaries.
- The dashboard React test config resolves one React runtime across retained Core components. All 184 UI tests pass. The 45 Node route and utility tests passed separately before the latest UI refinements. Five focused Schedule/date checks also pass, including the 32-day API range boundary.
- Isolated PostgreSQL/API integration checks pass (149 tests, migration replay) for onboarding replay, linked client data minimization, stale lead corrections, follow-up concurrency, Sales grants and the integrated standard-template installer. These checks create only synthetic local records.
- In the isolated local browser, a synthetic Sales account created an inquiry, opened its direct profile link, assigned an owner, saved Won, reviewed and created a linked synthetic client, and followed the client profile's internal Sales-history link back to the original inquiry. The profile and client workspace had no document overflow at a 390px phone width; the lead profile was also visually reviewed at desktop width. The local Owner account requires MFA by policy, so Owner-only UI acceptance remains open rather than bypassing that protection.
- A broad-granted synthetic staff account reached Workspace, Customers, Operations, Revenue, Marketing content/design/reporting and non-Owner Settings routes. At 390px, these reachable static routes had no document-level horizontal overflow. This is a route/overflow sweep, not a complete visual or assistive-technology acceptance. Owner-only routes correctly rejected the staff account. The isolated Marketing design pages displayed saved-settings load errors; their data-backed interaction and visual acceptance needs a configured staging backend.
- Phone visual review found that primary page actions were squeezed into narrow two-line buttons beside headings (for example, Expenses). The shared heading now stacks its action beneath the title at the mobile breakpoint with a full-width 44px minimum target. The Expenses and Properties screens were rechecked at 390px; the action labels are legible and the pages do not overflow.
- A second phone pass found Project Phases and Service Agreements actions reading as bare text. Their scoped controls now have defined touch targets and visual grouping. A synthetic local agreement deep link returned an explicit 404 with Retry rather than repeated fetching or an empty list, and a missing draft deep link showed Back and Retry. These checks are local visual evidence, not staging acceptance.
- At 390px, the revised Schedule month mode showed its agenda without the former 42-day API rejection. Billing's empty state and action fit without overflow. Populated Billing and Schedule records still need staging visual acceptance.
- The first staging upload failed in its build because the release package omitted retained Core presentation CSS imported by Dashboard Marketing. The allowlist was corrected; the resulting package completed an isolated Linux/amd64 Docker build. The failed upload never replaced the active staging revision. Staging deployment `72b0e221-1ba9-4d5b-9f2d-23672d435bba` reached SUCCESS after the corrected preview route was uploaded. The staging database volume backup `b87f0919-967e-488b-87f1-f89d1e24edf5` was created before the migration run. This is a staging checkpoint, not the production release.
- The [staging design preview](https://p1-dashboard-staging-dashboard-staging.up.railway.app/design-preview/) has clickable journeys and role selection. Its HTML and script return 200 without redirects; the unsuffixed path redirects once. A browser walkthrough opened the Sales lead profile and focused follow-up panel, verified that its save action reports no record write, and switched between Client and Sales views. The Client view did not expose the lead profile. The preview's phone and tablet widths do not overflow. Phone testing found that the preview menu opened with icon-only links; the corrected build `1d7541ab` reached staging SUCCESS as deployment `e180cebf-ebd7-4950-9b69-4ce26644b5a7`, where labeled links opened their target and closed the menu. Owner design review is pending. The preview uses synthetic content and is not authenticated staging acceptance.
- Staging `/health` returned 200 and anonymous `/api/v1/leads` returned 401 with `no-store`. Authenticated staging acceptance is still blocked by the staging login's unconfigured Owner setup; do not infer role/action success from synthetic local tests or the preview.
- Still required before the one production release: complete integrated staging walkthroughs of every role and route, visual comparison at phone/tablet/desktop, keyboard and screen-reader acceptance, synthetic end-to-end lead intake through Won and client link, backup/release validation, and post-deploy revision/live verification.

Do not describe the preview as a deployed staging build or the local test results as production acceptance.
