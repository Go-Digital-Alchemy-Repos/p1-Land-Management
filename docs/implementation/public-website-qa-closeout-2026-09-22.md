# Public website QA closeout — September 22, 2026

This slice completes the public website visual, mobile, accessibility, and form review without changing approved imagery or marketing copy. A live read-only sweep covered all 54 linked public routes at desktop and 390 px mobile widths: each had a single H1, substantive main content, no document overflow or broken loaded images, and none of the previously reported editorial annotations. Representative service, location, contact, article, map, desktop navigation, and mobile navigation interactions were also inspected.

The same-tab form navigation regression is already tracked in `artifacts/p1-website/tests/public-form-navigation.spec.tsx`; all seven cases pass, including A→B→A late callbacks, in-flight submit completion, and fresh idempotency keys. Shared form controls now announce required and invalid states, focus the first invalid input (including across pages), and make image choices keyboard-operated buttons. Client-side route changes focus the new page heading after lazy content loads. The map's keyboard instructions and expanded-menu button state are associated with their controls. The contact form uses native selects, preserving its choices and fitting the initial JavaScript budget.

Validation: website and Core typechecks; website production build/prerender; 54-route public QA and content regression; 98 website server tests; seven navigation tests; five focused form validation tests; FAQ schema, layout, navigation, media loading, image budget, and responsive image projection checks. The public QA script verifies the Owner-approved removal of Service Areas from the Services menu. FAQ validation now supplies the responsive image virtual module used by production builds. The FAQ check reports existing template-source warnings but validates all 44 emitted FAQPage blocks.

The local contact preview cannot complete Cloudflare Turnstile, so no production lead was submitted during this slice. This review does not establish inbox receipt or Search Console indexing. A separate controlled production inquiry and recipient confirmation remain part of lead-delivery acceptance.
