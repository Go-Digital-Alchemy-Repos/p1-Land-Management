# Business Center consolidation implementation evidence

Status: in progress. This is not a release candidate and must not be deployed as a completed consolidation.

## Branch and source preservation

Implementation worktree: `p1-business-center-consolidation`, branch `codex/business-center-consolidation`, based on published `origin/main` at `2848855`. The original shared checkout and its content-overhaul changes remain untouched. Blythe is read-only reference material; no Blythe change is included.

The reporting implementation from `codex/admin-google-analytics` was integrated through four clean cherry-picks: `3a7c875`, `5105e17`, `c0c0737`, `0d4a7d1`. This preserves the reporting code, not a claim that Google credentials are connected or that the new destinations exist yet.

## Implemented foundation

- Shared explicit leaf capability catalogue for Workspace, Customers, Operations, Revenue, Marketing Content/Design/Reporting, and business Settings. Group selection expands into current leaves. Owner alone receives automatic known-tool access.
- Additive migration `0028_business_access.sql`; reviewed grants, profile names, optimistic version, notification subscriptions, invitation grants/revocation, and plain `member` role. Existing staff receive no automatic grants. Legacy role suggestions are review-only.
- Owner-only User Manager APIs and UI: account search, invite/resend/revoke, names, suspension, grouped permissions, MFA policy controls, session revocation and audit history. Stale edits preserve the draft and explicit reload fetches fresh server values.
- Access updates atomically version grants, audit, and invalidate sessions. Owner access cannot be edited or suspended through this manager. Client invitations retain separate client-access boundaries.
- `/me` returns capabilities. Dashboard navigation now uses explicit office grants, with profile fallback for ungranted accounts. Existing client and assigned crew portal routes remain distinct.
- Sales API follow-through: legacy leads/estimates, current estimate creation/document/recipient/send paths and commercial inbox require explicit Sales or the applicable financial read grant. Template selection and maintenance are separate. Commercial owner assignment checks active Sales grants rather than legacy titles. Direct HTTP tests prove denial for ungranted Manager/report-only accounts and access for explicitly granted members.
- Initial service migration: sales revision/conversion, commercial assessment/prospect context, agreement management/read/activation preview, billing charge review and queue require explicit grants. Billing reads remain separated from general agreement details.
- OpenAPI and generated client cover the new identity field and User Manager endpoints. Codegen now preserves its handwritten index and legacy estimate adapter.

## Validation to date

- `pnpm run typecheck:libs`: passed.
- API and dashboard typechecks passed after actor typing and generated-client integration fixes.
- `node scripts/test-dashboard.mjs`: 47/47 passed, plus migration replay, using disposable PostgreSQL and synthetic records.
- `node scripts/test-service-agreements.mjs`: lifecycle/charge/schema/review tests, worker preparation test and real HTTP test passed; no live providers.
- Dashboard production build: passed; existing large MapLibre chunk warning remains.
- Dashboard navigation tests: 3/3 passed after explicit-grant navigation change.
- User Manager browser regression passed in headless Chrome: stale draft retention, fresh reload/version, isolated Analytics grant, invitation submission, and mobile dialog containment.

The baseline property-workspace test omitted nullable structured-address and property-type fields already returned by the baseline API. Its expectation was aligned with the verified response; production behavior was not changed to satisfy that assertion.

## Required remaining work

1. Complete the API capability matrix and migrate all remaining legacy role gates, including projections, shared lookups, assignment choices, notifications and exports. Initial domain changes and navigation are not full enforcement. Replace broad frontend role-based preloading/action conditions. Preserve crew/client record scope and offline queues.
2. Complete User Manager recovery and Core form-notification selection, existing-account linkage, history/pagination and migration reconciliation. Review crew grants versus scoped field workflow before cutover.
3. Complete module-by-module visual/accessibility verification of the adopted Core admin theme, including the remaining Marketing and template screens. Shared light/dark/system styling and sampled browser verification are implemented.
4. Mount the retained CMS route modules under Marketing and build the allowlisted, user-scoped Core boundary. Separate Google Analytics and Search Console grants on all legacy and new server routes. Preserve original media/publishing/intake behavior.
5. Merge CRM notes/tasks/history and client identity through explicit source mappings, repeatable import/reconciliation, and explicit Won onboarding.
6. Implement reusable MSA/scope/cost/package templates, immutable published versions and client composition integrated into existing estimate/approval/activation workflows. Preserve legal-content and billing constraints in the accepted plan.
7. Feature inventory/parity verification, restore rehearsal, redirects/retirement tooling, production builds and final release review. No legacy removal or production cutover has occurred.

Canonical scope: `docs/proposals/p1-business-center-consolidation.md`. The active goal remains the full implementation, not just this foundation.

## Next integration notes

The reporting authority contract and separate source grants are implemented (see checkpoints below). The CMS migration is now extending that user-scoped boundary. Other Core areas still use legacy local admin/editor gates and must be migrated before retirement; local administrators must never implicitly become canonical Owners.

The web app now loads datasets from leaf grants and uses a typed `/workspace/references` endpoint for minimized selection fields. Grants are checked independently for record workspace tabs and source sections. The main client, property, schedule, recurring, project, inspection, sales and billing action controls use capabilities; agreement selectors no longer fetch full Properties records. Failed or obsolete refreshes cannot repopulate business datasets after an identity/grant change.

## Customer and operations enforcement checkpoint

- Customer/contact/property/type reads and writes require the corresponding Customers grant. Nested client/property sections query only permitted source areas; general workspace activity is Owner-only. Request attachments are excluded from generic property file listings and remain available through their own authorized tool.
- Schedule, recurring programs, project phases, inspections, expenses, billing and QuickBooks financial actions use explicit capabilities. Integration credential configuration remains Owner-only. Requests-to-work conversion requires both Requests and Schedule. Project phase billing intents require Billing.
- My Day-only ordinary members receive assignment-scoped work reads and field submission access. Office completion review requires Schedule, including inside the transition policy itself. Existing client publication and crew assignment boundaries remain.
- Workflow notification recipients are selected by tool grants; global manual delivery administration is Owner-only. Historical notification access and delivery-time revocation for already queued messages still need follow-through.
- Disposable database suite passed 47 tests plus migration replay. Added direct unauthorized HTTP checks across Customers, Operations, Revenue and minimal references, minimized record-section assertions, and My Day member assignment checks. After removing the final string-role completion-review fallback, its targeted policy suite passed 5 tests. API/dashboard typechecks and agreement lifecycle/preparation/HTTP tests passed. No live providers used.

Remaining access follow-through includes server-side assignee eligibility on every write path, notification history/delivery revocation, agreement-only editing versus billing projections, isolated-tool browser scenarios and the Core federation authority contract. Do not treat these checkpoints as complete system authorization coverage.

## Unified admin appearance checkpoint

The dashboard now uses the Core admin light/dark tokens from `platform/p1-core/client/src/index.css` through dashboard-owned `theme.css`. Earthtone feature styles use semantic surfaces, text, borders, actions and status colors. Decorative page illustrations are no longer mounted. Core/public branding files are unchanged. The light muted-text token is slightly darker than Core's source to meet 4.5:1 on the admin canvas.

The header exposes Light/Dark/System for every signed-in workspace user, independently of business-settings permission. Selection is device-local under `p1-business-center-theme`; system changes and cross-tab storage changes are observed. A same-origin pre-render initializer prevents the wrong initial mode, and the offline shell caches that initializer. Storage failure retains a working in-session preference. App manifest colors and QR-code contrast were aligned. Ordinary modal layering now clears the mobile header.

Validation: dashboard typecheck and production build passed (existing MapLibre chunk warning). `scripts/test-dashboard-theme-browser.cjs` passed in Chrome against mocked synthetic client data: light/dark/system, reload persistence, OS changes, key text/action contrast in both modes, absence of decorative backgrounds, client editing dialog, mobile containment and title hit testing. Light desktop, dark dialog and mobile screenshots were visually inspected. Existing User Manager conflict/invite/mobile browser regression passed with the new shared theme loaded.

This proves the shared theme and sampled workflows; final visual acceptance still requires the remaining Marketing/report/template screens and the broader module-by-module parity pass. Production remains unchanged.

## Marketing reporting authority and transport checkpoint

Federation introspection now negotiates `include_capabilities: true`; older callers receive the original strict DTO unchanged. Core accepts the plain `member` role and receives current leaf grants on every introspection. Authenticated requests retain that verified canonical identity separately from the linked local Core user. Local admin/editor or CRM permission alone cannot authorize reporting. Analytics and realtime require `marketing.analytics.view`; Search Console requires `marketing.search-console.view`, including the legacy report APIs.

Dashboard reporting reads now have three fixed `/api/v1/marketing/reporting/` operations: `analytics`, `realtime`, and `search-console`. They authorize the caller, create a 60-second user/session-bound grant, call the fixed Core reporting ingress, and delete the grant afterward. Core introspects the grant and enforces current canonical permissions plus the existing linked Core account's active/revoked status. No shared admin browser session or unrestricted proxy is used. The existing provider services still own queries, caches and Google credentials. Report responses are bounded and private/no-store; arbitrary upstream errors/headers are not forwarded. OpenAPI and generated report DTOs/client functions are included.

Release ordering: deploy the additive dashboard introspection extension before the upgraded Core consumer (the old dashboard rejects the new request field). Provision a separate random base64url service key in dashboard `CORE_MARKETING_SERVICE_KEY` and Core `DASHBOARD_MARKETING_SERVICE_KEY`; configure dashboard `CORE_MARKETING_ORIGIN` as the exact HTTPS Core origin, with no path/query/credentials. Existing federation confidential-client configuration and explicit identity links are also required. No keys were created, exposed, or configured by this change. Private Core service ingress is mounted after the API rate limiter and before browser Origin checks; it rejects Cookie, Origin and Fetch Metadata headers and authenticates the service key before any identity/provider call. Ordinary browser routes retain their Origin checks.

Focused Core tests exercise the actual permission middleware, independent sources, malformed/browser-shaped ingress and suspension; the disposable Core federation integration exercises linked accounts and immediate reporting grant changes/revocation. The dashboard suite includes minimized request construction, bounded/sanitized responses and direct unauthorized report reads. This checkpoint does not yet mount the reporting UI under Marketing or claim a live Google connection. Full CMS migration and reporting browser parity remain required.

Owner instruction retained: push the finished, validated branch to GitHub when the full goal is complete. No interim push or production cutover is authorized by this checkpoint.

Reporting checkpoint validation: dashboard suite 50/50 plus migration replay; Core route/auth suite 19/19; disposable Core federation suite 15/15; Core, API and dashboard typechecks passed. A pure transport test initially imported the authenticated router and caused asynchronous BetterAuth initialization to outlive the test's pool cleanup; transport/configuration were separated from route/authentication dependencies, and the standalone transport tests now pass without opening a database or triggering that warning. The generated client reflects source-specific report shapes and sanitized unavailable responses. The transport currently rejects report bodies above 8 MiB rather than silently truncating them; include large-report parity in final reporting validation.

## Marketing reporting screens checkpoint

Marketing is now a navigation group immediately above Settings. `/marketing/reporting/analytics` and `/marketing/reporting/search-console` are independently permissioned, lazy-loaded report screens using the generated dashboard reporting client. Search Console-only navigation never loads Analytics/realtime, and Analytics-only navigation never loads Search Console. Changing user/grants/source remounts the workspace; requests are aborted on navigation/range changes and failed historical refreshes clear previous report data.

The adaptation retains overview KPIs/prior-period comparisons, daily trends and metric selection, channels/source/campaigns, pages/landing pages, geography/devices/browsers, events, and realtime totals/countries/devices. Device breakdowns retain donut presentation for count metrics. Search Console includes finalized Web totals, daily trends, queries/pages/countries/devices, prior comparisons, and Pacific-day presets. Both include date presets/custom dates, refresh, loaded-row search/sort/pagination, formula-safe filtered CSV export, source identity/freshness and sampling/threshold/truncation disclosures. Missing daily values remain gaps and missing metrics show unavailable rather than invented zero. External provider links use the returned property identity. No public tracking was added.

Reused the existing project chart library version (Recharts 2.15.4) and adapted the prior report formatting functions. Reporting is a separate lazy bundle; it does not expand startup with chart code. Mobile chart-tooltip overflow found in browser testing was confined to the chart surface. The newer major Recharts version was not introduced as part of this migration; package tooling flags the existing v2 series deprecated, which remains a future dependency-maintenance consideration.

Browser verification uses synthetic intercepted API responses only: source isolation, table pagination/search, formula-safe downloaded CSV contents, independent realtime failure, historical-source failure clearing stale rows, revoked grants, dark mode and mobile containment. Screenshots were visually inspected. This is frontend/reporting implementation evidence, not proof of a live Google connection. CMS Content/Design/Website System still require their destination modules and allowlisted operations; this checkpoint does not retire `/admin/`.

Final screen checkpoint validation: dashboard suite 53/53 plus migration replay, dashboard typecheck, production build and reporting browser regression passed. The existing MapLibre bundle-size warning remains. The revocation browser assertion was corrected to await the established Profile redirect rather than the transient unavailable heading; it confirms no reporting request occurs after revoked grants are loaded. No production or GitHub push occurred.

## Retained CMS API checkpoint

Added explicit dashboard `/api/v1/marketing/cms` method/path operations for Pages (including publishing, scheduling, duplication, revisions, preview-link and relationship actions), Sections, Galleries, Menus, Sidebars, SEO/robots/redirects/audit, and Team. The operation registry in `artifacts/api-server/src/dashboard/marketing-cms.transport.ts` is the current transport contract. Record identifiers are single segments; only the original gallery search/status/sort and page force parameters are accepted. Core remains the only owner of CMS records and mutation validation. No CMS database copy or alternate publishing engine was introduced.

Each request checks canonical dashboard grants and forwards a short-lived user/session grant to the fixed Core `/api/integrations/business-center/cms` service ingress. Core independently resolves current identity, suspension and grants, then invokes the existing route handlers. The service key authenticates the dashboard process only; it cannot confer user permissions. Ordinary browser cookies, Origin and Fetch Metadata headers are rejected at ingress. Both reporting and CMS share that service authentication implementation. Request/response bodies are bounded at 8 MiB, redirects and automatic mutation retries are disabled, and upstream cookies/headers/internal failures are not exposed. Domain validation and conflict payloads are retained.

The retained CMS routes now require their exact Marketing leaf grants even through `/api/admin/cms`. Media routes likewise require Media access instead of accepting blanket Content/Design/CRM. SEO governs redirects, robots and the existing summarized cross-content audit. Public published reads are unchanged; authenticated draft preview now also requires Pages and its existing preview token. Page title/slug changes still synchronize derived menu links. Explicit removal of linked navigation items requires both Pages and Menus. Cross-tool picker references must be implemented deliberately as the editors move, rather than weakening the maintenance grants.

This checkpoint does not mount CMS editors in the dashboard. Multipart/binary Media transport, Website/Blog/Forms/Events/Careers, Design, Owner-only Website System, editor locks, minimized selector references, typed frontend clients, comprehensive OpenAPI and editor parity remain required. The initial capability list omitted the feature-gated Events/Careers areas; include them in the remaining inventory so disabled tools are preserved without enabling them in production. No legacy UI retirement or live CMS mutation has occurred.

Validation: dashboard suite 56/56 plus disposable migration replay, 26 focused Core route/upload tests, API and Core typechecks, and dashboard API production bundle passed. Tests invoke retained handlers over HTTP with synthetic storage and fresh identity fixtures, cover every exposed method/path denial on both surfaces, independent tool reads, page publication identity, linked-menu removal, revoked/suspended/crew/client/unattested Owner access, draft preview gating, transport path/query/body limits and existing upload protections. Production provider/storage operations were not invoked. Complete CMS editor browser parity and live release rehearsal remain outstanding.

## Editor reservation migration checkpoint

Editor-lock reads, listings, acquisition, heartbeat and release now require the canonical grant matching the resource: Pages, Blog, Events, Forms, Sections, Menus or Sidebars. Website documentation and email-template locks require an active, attested Owner. Local Core admin/editor roles alone no longer authorize lock operations. This check applies to retained body-scoped legacy routes as well as new path-scoped service routes. The dashboard exposes a fixed method/path entry for each resource/action; it does not let a body select a more privileged lock type. Responses are private/no-store.

The existing Core lock store, linked-user ownership, conflict responses, 30-second heartbeat and five-minute expiry remain intact. Revoked grants prevent renewal/release; expiry makes an abandoned reservation available again. These remain the existing advisory editor reservations, not a new transactional write-lock guarantee. Existing save-conflict behavior and editor UI still need their full integration verification.

Events and Careers now have distinct leaf checkboxes in the canonical permission vocabulary. No existing non-Owner receives them automatically, and no site feature flag was enabled. Their full tools remain in the destination inventory.

The dashboard service connection now supports all nine existing lock resource types. Lock operations are mounted independently of the CMS feature flag because Docs/email templates and feature-gated Events/Forms are not CMS page tools. The destination editor still needs the retained component tree, media/picker connections, route adaptation and browser parity before it can be considered migrated. No CMS screen has been declared complete by this reservation checkpoint.

Validation: 17 Core reservation/service-ingress tests and the 56-test disposable dashboard suite plus migration replay passed. Core, API, dashboard and shared-library typechecks passed, as did the dashboard API production bundle. Tests exercise actual lock handlers and service logic with synthetic storage: ownership collisions, both route formats, resource isolation, Owner attestation, revocation and expiry recovery. Initial typechecks identified the new Events/Careers leaves missing from the OpenAPI enum and stale shared build declarations; the specification/generated client and shared build were synchronized before the successful final checks. No production data, Blythe files or release settings were changed.

## Native Marketing menu editor checkpoint

`/marketing/content/menus` is now a lazy-loaded dashboard screen, protected by `marketing.content.menus`. Marketing navigation has Content and Reporting section labels above the main Settings area. The menu UI adapts the existing menu editor behavior to the dashboard's semantic theme and uses the original Core CRUD handlers and records. It supports all current and legacy location assignments, custom links, page links with synchronized or custom labels, form popups and their titles/descriptions, new-tab behavior, up to three nested levels, drag/keyboard ordering, promotion to the top level, creation and deletion. Assignment to an occupied location explicitly identifies the menu being replaced.

An independently authorized `/menu-references` response supplies only page ID/title/slug/status and form ID/name/slug. Menus-only users can populate selectors without reading full page drafts, form definitions or submissions. Typed OpenAPI menu/reference/reservation DTOs and generated API functions are used by the screen; no Core credentials or separate sign-in are present in the browser. The remaining CMS operations still need their comprehensive typed contracts as their screens move.

Existing-menu edits acquire and renew the retained reservation, recheck it before save, retain the draft on conflict and release on unmount. Internal navigation and browser unload offer unsaved-change protection; canonical access revocation overrides it and unmounts the editor. Read requests are aborted at identity/route unmount. Saving still follows the original Core menu semantics and is not claimed to add transactional optimistic concurrency. The original page builder and other CMS screens remain to be integrated; this screen does not retire `/admin/`.

Validation: native-menu browser test passed with synthetic intercepted responses for creation, nested page/form composition, promotion/reordering, minimized request sources, unsaved navigation cancellation, lock conflict draft retention, mobile containment and revoked access. The existing reporting browser regression also passed after shared navigation changes. Mobile render was inspected after waiting for the sidebar transition. Dashboard suite 56/56 plus migration replay, 10 Core CMS tests including reference projection, Core/API/dashboard/shared typechecks, and dashboard production build passed. The existing MapLibre size warning remains. No production CMS writes, deployment or GitHub push occurred.

## Native P1 Website editor checkpoint

Marketing → Content → Website now exposes the existing manifest-bounded P1 content editor natively at `/marketing/content/website`, including direct component links, catalog/field search, draft save, explicit saved-revision publication, history and restore-as-draft. Generated dashboard contracts call the retained Core workflow through six fixed service operations. Core remains the content owner; expected-revision conflicts retain local edits and do not retry writes. Both legacy and service Website routes require `marketing.content.website`; local admin/content-editor roles alone confer no access. Navigation/unload guards protect unsaved edits.

Source conflict resolved in navigation/copy: the public P1 site reads published client-site-content snapshots, including shared `site-chrome` navigation and page SEO. Generic CMS menus/SEO settings are separate retained tools; changing them must not be represented as updating primary P1 navigation/SEO. The Website editor preserves this publication boundary. Generic sitemap previews must not be labeled as the actual P1 runtime sitemap. Further generic CMS migration remains outstanding.

Preview adaptation retains source-window, exact origin, stack/route/component identity and revision checks. The public site additionally accepts the exact `https://dashboard.p1landmanagement.com` parent. Only recognized public `cmsPreview` documents allow that frame ancestor; ordinary pages retain SAMEORIGIN/self restrictions. Preview responses remain private/no-store and noindex; analytics excludes them. The iframe uses the manifest's HTTPS preview URL, scripts/same-origin sandbox and exact target origin. No production content or framing configuration was changed. Production preview requires the separately authorized public-site release; the local UI browser fixture mocks its iframe.

Validation: 33 focused Core route/workflow/preview tests, 56 dashboard tests with disposable migration replay, 29 public runtime tests and two analytics tests passed. Core/API/dashboard/public-site typechecks and dashboard/public-site builds passed. Website editor browser checks cover direct links, unsaved-navigation cancellation, stale-write recovery, exact revision payloads, publication, restore without republishing, mobile containment and revoked access. Desktop/mobile screenshots were inspected. Existing public build sourcemap warnings and MapLibre chunk-size warning remain. No full consolidation completion, live cross-origin preview claim, legacy retirement, GitHub push or production cutover.

## Media transport checkpoint

The fixed CMS operation registry now includes Media list, metadata/alt PATCH, delete, multipart upload/replacement and binary source reads. Browser authorization precedes raw upload parsing; the dashboard bounds multipart envelopes at 11 MiB and Core retains its original 10 MiB file limit, type/extension checks, image decoding/optimization, upload freeze policy and storage ownership. Fresh canonical identity is independently checked at Core ingress. No browser cookies, destinations or arbitrary headers pass through. Source streams are bounded at 11 MiB and returned private/no-store, nosniff and attachment; only PNG/JPEG/WebP/GIF retain image MIME types, other sources become octet-stream. Metadata stays JSON. No mutation retries were introduced.

This is transport groundwork for the native Media library and pickers; those interfaces, crop/replace parity, typed client/OpenAPI coverage and end-to-end real-storage rehearsal remain outstanding. No live files were uploaded, replaced or deleted. Transport tests use synthetic byte buffers; service-route positives mock the storage-producing helper/download. Existing independent file-validation and upload-freeze tests exercise the real safeguards.

During inspection, the dashboard's production CSP was found to inherit `default-src self` for frames, which would block the newly moved Website preview despite the public site's correct frame-ancestor policy. It now explicitly allows only `https://www.p1landmanagement.com` as frame source, retaining `frame-ancestors none`. A production-server integration assertion covers that header; the earlier Vite browser fixture could not detect this server-only restriction.

Validation for this checkpoint: 58 dashboard tests plus disposable migration replay passed, including production CSP and all added Media method/grant denials; 32 focused Core tests passed across service ingress, actual upload validation, freeze policy and usage discovery (13 ingress tests after the final positive fixture was added). Core and API typechecks passed. Media UI is not yet present, so no Media browser parity is claimed.

## Native Media library and Website image picker checkpoint

Marketing → Content → Media now mounts a native, independently permissioned library. It retains filename/title/alt/caption/description search, image/document and live/draft/no-known-reference filters, eight sort orders, multi-file uploads with the existing 10 MiB limit, metadata/SEO/social fields, URL copy, authenticated downloads, explicit deletion confirmation and crop/replace. Cropping supports pointer selection and keyboard-accessible percentage coordinates, bounded image coordinates, WebP output capped at 1200 pixels and explicit replacement confirmation; it adds no new package dependency. Unsaved metadata has navigation/unload protection. Failed batch uploads retain earlier successful files and reload the library.

Typed OpenAPI/generated clients now cover Media list, metadata, upload, replace, delete and binary source reads. Previews and crops use authenticated source endpoints, never arbitrary browser-selected fetch destinations. The Website editor offers the shared picker only with the independent Media grant; unsupported MIME types and image origins are unavailable for selection, and the retained server manifest validation still governs saves. A selected image only changes the local Website draft. Publication remains explicit.

Usage discovery now includes current draft and published Website snapshots, separately identified by snapshot field on the actual content record. It returns reference metadata rather than content bodies. This closes a pre-existing gap where assets referenced by the primary P1 Website could appear unused. Usage remains advisory: external references and unsaved edits are not a deletion guarantee. Existing generic content usage discovery and storage ownership remain intact.

Validation: 59 dashboard tests plus disposable migration replay passed; generated Media client tests verify paths, multipart filename/bytes, metadata and Blob responses. Focused Core usage/ingress tests passed (17 tests) and Core typecheck passed. Dashboard typecheck/build passed, retaining the existing MapLibre size warning. Native browser fixtures verify type/search filtering, metadata draft protection and save, crop multipart replacement, uploads, delete cancellation/confirmation, mobile containment, revoked access, Website picker selection, disallowed origins and removal of the picker without Media authority. Desktop/mobile screenshots were inspected with synthetic image data. Browser fixtures mock storage and provider calls; real-storage release rehearsal and full consolidation parity remain outstanding. No production assets or deployments were changed, and no GitHub push occurred.

## Native Team management and CMS rich-text editor checkpoint

Marketing → Content → Team now lists, searches, filters, creates and updates website Team profiles with the canonical Team grant. Name/title, portrait URL and alt, excerpt, rich biography and draft/published/archived state retain their Core contracts. Publication has an explicit confirmation, archival retains the record, and unsaved edits are guarded. Team profiles do not create staff accounts or grant access. Member ordering remains owned by each CMS Team section; it is not a new global staff order. Core keeps record storage, biography sanitization and activity attribution to the linked local actor.

The CMS biography editor uses the retained Core TipTap 3.20.1 engine, with visual/HTML modes, headings, emphasis, lists, quotes, alignment, links/new-tab attributes, embedded images/alignment, and undo/redo. The dashboard's older plain-text editor is unchanged. Media pickers for portraits and embedded images require separate Media access; manual URLs and stored HTML remain available without Media authority. Relative image previews resolve to the P1 public origin without rewriting stored HTML. Dashboard image CSP adds only that exact public origin; other external image URLs can be retained but are not guaranteed to preview under the restricted dashboard policy. Final cross-origin asset parity remains part of release review.

Dependency decision: reuse Core's existing MIT-licensed TipTap family rather than add a competing editor. Direct and transitive TipTap packages are pinned to the same 3.20.1 release; the first install exposed newer incompatible transitive peers, resolved with explicit family overrides. `pnpm peers check` then reported no peer issues. The editor loads with the Team chunk (approximately 388 kB uncompressed / 123 kB gzip), rather than the initial dashboard bundle.

The existing public HTML sanitizer stripped the editor's text alignment. It now preserves only `text-align: left|center|right` on paragraphs/headings. Other style properties, executable markup and unsafe URLs remain stripped; regression tests cover this narrow policy extension. Visual inspection also corrected selected-toolbar contrast.

Validation: native browser checks cover rich formatting/HTML round-trip, portrait selection/alt, canceled unsaved discard, publish confirmation, archive/reopen, independent Media denial, revoked Team access, and mobile containment. Desktop/mobile views were inspected. The initial development-browser run hit a Vite dependency-optimization reload error; a fresh run after optimization passed without page errors. Focused Core Team contract/rendering/service tests passed (25), followed by 11 sanitizer/security tests after the alignment change. Dashboard/Core typechecks passed. All fixtures use synthetic records/assets and mocked browser API responses; no public Team records were changed. Full consolidation completion and GitHub push remain outstanding.

Final checkpoint checks: 59 dashboard tests and disposable migration replay passed after the image CSP assertion; the dashboard production build passed with the existing MapLibre warning. The Team browser fixture also verifies embedded-image alignment and preservation of its original relative URL through HTML/visual modes.

## Blog service and typed contract checkpoint

The dashboard CMS allowlist now exposes all retained Blog post CRUD, taxonomy CRUD, comment moderation and comment-settings operations under `marketing.content.blog`. The same leaf is enforced inside the retained Blog router and at its legacy admin mount; broad Content or local admin roles do not bypass it. Blog remains behind its existing independent `blogEnabled` flag and is mounted before the general CMS feature gate. No disabled feature was enabled. Scheduling still rejects past dates, forces scheduled posts to unpublished with no published timestamp, and clears scheduling when publishing. Comment filters accept only one known scalar status, and static references/comment routes are registered before post-ID routes.

A Blog-only reference endpoint returns sidebar IDs and names without sidebar content or maintenance authority. Existing post author names remain content fields, not staff-account lookups. Taxonomy reads retain their existing legacy-name reconciliation behavior. An explicit `parentId: null` now detaches a category; omitted parent fields retain the old parent. Previously null-coalescing prevented detachment. This distinction is covered by a retained-handler test.

OpenAPI/generated clients cover posts (including SEO, focal points, article/podcast/external content fields, categories/tags, sidebar and publication dates), category/tag administration, moderation records/status/filtering, and all comment settings. Partial update contracts preserve explicit nulls. Core remains the owner of records, validation, scheduled publication and public comments. The native Blog editor/settings/moderation screens and their end-to-end parity are still outstanding; this checkpoint does not make generic Core Blog posts the primary P1 public site's content source or change its manifest publication workflow.

Validation: 18 focused Core service-route tests passed, covering every Blog operation denial on service and retained routes, publication scheduling, feature isolation, minimized selectors, moderation, taxonomy conflicts and parent detachment. Core/API/shared typechecks passed. No live posts, categories, comments, provider settings or production deployments were changed.

Final dashboard checks: 61 tests plus disposable migration replay passed, including generated Blog-client verification of scheduling nulls, category detachment and moderation query serialization. Semantic comparison of the OpenAPI document confirmed that existing schemas and paths are unchanged; the new Blog definitions are additive.

## Native Blog workspace checkpoint

Marketing → Content → Blog now mounts a lazy native workspace behind the Blog capability. It includes searchable post lists, draft/published/scheduled filters, post create/edit/delete, article/podcast/external fields, author, excerpt, cover and focal-point preview, categories/tags, sidebar selection, SEO/social fields, noindex and explicit publication/scheduling. Existing posts acquire and heartbeat the retained advisory editor reservation; saves verify ownership. Unsaved edits use the dashboard navigation/unload guard. Reservation writes are still advisory, not transaction-enforced locking.

The shared retained TipTap editor adds Blog heading 1, code, code block, divider, emoji and published-gallery shortcode controls while preserving visual/HTML modes. Blog references now return published gallery IDs/titles alongside sidebar IDs/names; draft galleries and gallery bodies remain excluded. Media selection retains its separate grant. Arbitrary external images remain subject to the existing restricted preview CSP.

Separate Blog panels expose category/tag names, slugs, hierarchy, order and deletion; comment body/note editing, moderation states and deletion; and every retained comment setting. Updating one comment preserves unsaved drafts in other cards. Category parent changes reject descendant cycles server-side, in addition to filtering invalid parent choices in the UI. This closes an existing unsafe hierarchy edge case. Taxonomy deletion retains Core's reference cleanup behavior.

Validation: 61 dashboard tests and disposable migration replay passed. All 19 focused Core service tests passed, including descendant-cycle rejection and minimized published-gallery selectors. Dashboard and Core typechecks passed; the dashboard production build passed with the existing MapLibre size warning. Synthetic browser checks passed for new post creation, HTML/gallery shortcode persistence, future scheduling, explicit category detachment, independent comment draft preservation, moderation, settings and mobile containment. Browser fixtures mock API calls and do not change live records. The new post editor still needs broader visual/accessibility and failure/revocation parity review; generic Blog public-source reconciliation and the rest of the consolidation remain outstanding. No GitHub push or production deployment occurred.

## Native SEO workspace checkpoint

Marketing → Content → SEO now provides native global defaults, social/organization fields, robots override/reset, redirect create/edit/activate/deactivate/delete, and the retained CMS metadata audit. The SEO leaf controls navigation and all server operations; image pickers require the separate Media leaf. Defaults and robots saves preserve their separate contracts so saving defaults cannot accidentally clear a custom robots override. Unsaved edits have navigation/unload protection. Enabling global noindex and applying/resetting robots content require explicit confirmation.

The Sitemap and robots panel links to the actual P1 public sitemap/robots endpoints. It deliberately does not reproduce the legacy client-side sitemap prediction, which combined generic CMS records and unrelated static routes. The panel explains the source distinction; the audit remains a metadata-only report over retained CMS pages, Blog posts and Events. Primary P1 page SEO continues through the Website manifest publication flow. Public-source reconciliation and broader cross-module edit links remain in the final parity work.

Additive OpenAPI/generated contracts cover settings, robots, redirects and audit projections. No persistent schema or public publication behavior changed. Validation: 61 dashboard tests and disposable migration replay passed; 20 focused Core bridge tests passed, including custom robots normalization/reset and audit body minimization with independent page denial. Dashboard/shared typechecks and production build passed; existing MapLibre chunk warning remains. Synthetic browser checks cover defaults saves, dirty-navigation cancellation, independent Media denial, robots override/reset, redirect creation/deactivation/deletion, minimized audit calls, mobile containment and revoked SEO access. Mobile output was visually inspected. No live settings or redirects were changed; no GitHub push or production deployment occurred.

## Native Sidebars checkpoint

Marketing → Content → Sidebars now provides native listing/search, create/edit/delete, description/default selection, all eight retained widget types, widget titles/settings, removal and keyboard-accessible ordering. Form/newsletter selectors preserve their original eligibility rules. Existing widget settings remain intact when editing neighboring values. Custom HTML stays editable text; the dashboard does not execute it. Saving the default uses the retained Core behavior and storage.

Existing sidebar edits acquire/heartbeat/verify the retained advisory reservation; a conflict leaves the user's draft visible and recoverable. Unsaved navigation is guarded. The reservation remains advisory rather than a transactional concurrency guarantee, matching the existing editor's contract. Final cutover concurrency/restore rehearsal remains outstanding.

A Sidebar-specific form reference endpoint returns only ID/name/slug/kind under the Sidebar capability. It does not require broader Forms access or expose rules, recipient settings or submissions. Typed sidebar CRUD/reference/reservation clients and the dashboard bridge allowlist are additive; Core still owns validation and persistence.

Validation: 61 dashboard tests with disposable migration replay, 21 Core bridge tests, Dashboard/Core/shared typechecks and the production dashboard build passed. The existing MapLibre size warning remains. Synthetic browser checks exercise widget fields/order, HTML persistence, default selection, exclusion of application forms, unsaved-navigation cancellation, reservation conflict/recovery with draft preservation, mobile containment, deletion and revoked access. Mobile output was visually inspected. No live sidebars or forms changed. Full consolidation and final GitHub push remain outstanding; no production deployment occurred.

## Gallery contract and Sidebar reference preservation checkpoint

Additive gallery OpenAPI/generated clients now describe list filters, create/read/update/delete, duplicate/publish/unpublish, five layouts, all retained display settings and item metadata (image/media, alt/title/caption, links/CTA, tags and ordering). The native Gallery workspace remains outstanding. Retained gallery validation now rejects slugs that normalize to empty or separators before querying or writing storage; valid slugs and public routes are unchanged.

Sidebar form selectors now keep a saved reference visible when changing widget type makes that form ineligible for new selection. Previously the underlying reference survived but its select appeared empty. The fix uses the same eligibility predicate for choices and saved-reference fallback, preserving explicit user control.

Validation: Sidebar browser regression passed, including switching a Contact form widget to Newsletter and back without losing the saved reference. Dashboard/Core/shared typechecks passed. All 22 focused Core route tests passed, including punctuation/separator-only slug rejection. An initial test invocation from the repository root failed the existing Website manifest lookup because that suite depends on the Core working directory; rerunning from Core passed. No native gallery UI or end-to-end gallery parity is claimed at this checkpoint. No live records, deployment or GitHub push changed.

## Native Gallery editing checkpoint

Marketing → Content → Galleries now exposes searchable/filterable/sortable galleries, draft creation, all five layouts and retained display settings, item URLs/Media selection, alt/title/caption/link/CTA/tags, keyboard ordering and removal, draft/published/archived state, saved-record publish/unpublish/duplicate and deletion. Item order is serialized explicitly. Changing an image URL detaches its old Media ID. Media selection requires independent Media access. Publication and deletion require confirmation; unsaved navigation is guarded, and saved-record actions are unavailable until the draft is saved. Core remains the storage/publication owner.

Validation: native browser checks passed for layout settings, metadata/order preservation, dirty guard, independent Media denial, publish/unpublish, duplication, deletion and mobile containment. Synthetic image data and mocked API calls were used; mobile output was visually inspected. Dashboard typecheck and production build passed, with the existing MapLibre warning. All 61 dashboard tests plus disposable migration replay passed.

This is an editing checkpoint, not full Gallery parity: the retained full GalleryRenderer preview and bulk image-upload interaction remain to be ported and verified, along with permission-revocation/failure coverage and cross-origin preview review. No live gallery or Media records were changed. Full consolidation, GitHub push and production release remain outstanding.

## Gallery draft preview and bulk upload checkpoint

The Gallery workspace now includes a local draft preview for grid, masonry, carousel, slider and featured layouts. It adapts the retained renderer's settings and behavior without fetching public content: responsive column counts, image limits/ratio/crop/radius, caption/title position, hover/slide effects, arrow colors, thumbnail/slide selection and lightbox navigation. The lightbox uses a native dialog with focus handling and Escape/arrow-key controls; reduced-motion preferences disable animation. CTA labels expose their destination as text without navigating away from unsaved edits. Custom class names are retained, but public-only CSS and arbitrary external image origins still need final cross-site parity review.

Gallery editors with the separate Media capability can upload multiple PNG/JPEG/WebP/GIF images through the existing bounded Media endpoint. Successful assets append to the current draft with Media ID and metadata; a later failure retains earlier successful assets and explains that behavior. Uploads use an AbortController canceled on editor unmount. No upload publishes or saves the gallery automatically.

Validation: expanded synthetic browser checks passed for slider navigation, keyboard lightbox navigation/Escape, partial bulk-upload failure preserving the first asset, and the existing gallery editing/publication/mobile checks. Draft preview output was visually inspected. Dashboard typecheck and production build passed with the existing MapLibre warning. The preview is an adaptation of the retained renderer, not a claim of pixel-identical public rendering; full visual/asset parity and remaining consolidation gates remain outstanding. No live uploads, publication, GitHub push or deployment occurred.

## Shared block catalog and Sections contract checkpoint

Moved the four data-only block-registry modules into Core's shared/cms-builder directory. The retained admin paths re-export the canonical modules, preserving consumers and tests. Byte-for-byte comparison confirmed the moved definitions/defaults/helpers are unchanged. This provides one source of truth for retained and Business Center block editors instead of a second registry.

A Sections-scoped builder endpoint now returns that catalog and legacy aliases plus minimized page/form/gallery/team selectors. Only published galleries/team profiles are offered, and bodies, biographies, gallery items, form fields/recipient settings and submissions are excluded. Sections permission does not grant access to the underlying editors. Typed contracts cover section CRUD, unchanged arbitrary block payload preservation, starter refresh result counts, builder property definitions and advisory reservations. Starter refresh retains its original behavior, including refreshing/removing system starter records; no refresh was executed against live data. The native visual Sections editor is still outstanding.

Validation: 23 service tests and 2 registry tests passed; retained Section editor and all-block inspector regression tests passed (2). Core/Dashboard/API/shared typechecks passed. Core client/server production build passed with existing PostCSS and chunk-size warnings. Semantic OpenAPI comparison confirmed previously existing paths/schemas are unchanged. An initial root-level `npm run check` had no matching script; checks were then run from the correct Core directory. No production settings/content, GitHub push or deployment changed.

## Native Section property editor checkpoint

Marketing → Content → Sections now mounts a native list/search/category workspace and retained section CRUD with advisory reservations. The editor consumes the canonical block catalog, adds defaults by deep copy, preserves legacy type aliases, orders/duplicates/removes blocks and edits nested array items, rich text, basic fields, page/form/gallery/team references and Media-gated images. Unknown blocks and properties remain in the payload when neighboring values change. Saved references absent from current selector choices remain visible. Rich-text editing respects the reservation/busy state as well as disabled native controls.

Starter refresh is exposed with an explicit explanation/confirmation of its retained overwrite/removal behavior; no starter refresh was run on live data. Saving Sections does not publish pages. The visual page-builder canvas, full block rendering previews, conditional button/link field behavior and comprehensive all-block interaction parity are still outstanding; this checkpoint is property editing, not completion of that migration.

Validation: synthetic browser checks passed for nested-property editing, unknown data/block preservation, missing form references, ordering, deep duplication with new IDs, canceled dirty navigation and mobile containment. Mobile output was inspected. Dashboard typecheck and build passed. The initial dashboard chunk now slightly exceeds 500 kB uncompressed (about 149 kB gzip), alongside the existing larger MapLibre chunk; final bundle review remains required. No live section changes, GitHub push or deployment occurred.

Checkpoint regression suite: all 61 dashboard tests and disposable migration replay passed.

## Shared conditional block fields checkpoint

Extracted the retained button-action and rich-text field rules into a data-only shared helper consumed by both editors. The dashboard now interprets legacy `url` actions as internal/custom links without rewriting their stored value, offers page references for internal button links, switches link/form-modal fields according to the selected action, applies the retained dynamic labels, and uses rich-text editing for subtitle/subheading/answer fields. Hidden alternative values remain in the block payload. Nested array inspectors use the same rules.

Validation: the expanded Sections browser regression passed, including legacy action display, internal page selection, switching to a form modal and preserving the hidden saved URL. Five focused shared-helper/retained-editor tests passed. The first retained-editor run found an inspector-grouping dependency on the moved controller helper; exporting/importing that helper restored the existing inspector, and Core typecheck plus all focused regressions then passed. Dashboard typecheck and build passed with existing size warnings. The full visual canvas, preview, fallback editor details and remaining consolidation requirements are still outstanding. No public content or deployment changed.

## Legacy block compatibility editor checkpoint

Moved the retained fallback block-definition inference into a shared helper, preserving its old import/export entry point. Native Sections now offers the same compatibility controls for unknown block types instead of leaving those records entirely read-only. Text, booleans, numbers, image/link fields and long/HTML text use the retained inference rules. Unsupported objects, arrays and null values remain in the payload unchanged; this does not invent a new block schema.

Validation: the Sections browser fixture now edits a legacy scalar field and verifies the unknown nested object survives alongside all prior ordering/reference/duplication checks. Shared inference and retained all-block inspector tests passed (2). Core/Dashboard typechecks and dashboard production build passed with existing chunk warnings. A first attempt to write the test used a root-relative path while in Core and created no file; the test was then written at the correct path and explicitly run. Native canvas/full rendering and remaining consolidation work are still outstanding. No production writes or GitHub push occurred.

## Builder preview transport checkpoint

Added a shared, versioned draft-preview envelope for the planned Core-rendered Marketing canvas. Acceptance requires an exact canonical HTTP(S) origin, the expected window identity, a per-frame UUID channel and an increasing revision. Draft blocks retain unknown fields and legacy properties. Validation rejects duplicate block IDs, unsafe object keys, cycles, getters, non-JSON values, excessive nesting/node counts, oversized sparse arrays and payloads over 1 MiB; previews are limited to 200 blocks. No storage read, save or publication authority is conferred by this message.

Validation: two focused protocol tests passed using the installed Core Vitest, and Core TypeScript checking passed. An earlier root invocation used an ephemeral Vitest version; the installed Core runner was subsequently used successfully. This checkpoint does not expose a preview endpoint or connect a renderer. The retained renderer has HTML insertion points and dynamic components, while the normal Core app starts account/analytics integrations. Renderer integration therefore still needs sanitization, interaction isolation, explicit frame-origin policy and runtime verification before enabling the canvas. No production headers, live data, GitHub push or deployment changed.

## Shared preview sanitization checkpoint

Moved the existing public rich-HTML sanitizer into a shared module, preserving the server import entry point and its policy unchanged. The retained builder preview now sanitizes a memoized rendering copy before passing blocks to the public renderer. The preview adapter also covers nested `subtitle` values rendered by SectionHeading. The original editor data, unknown properties and save payload remain unchanged. This prepares the same HTML policy for the consolidated preview without introducing a second sanitizer implementation or dependency.

Validation: the sanitizer/draft-preservation test, retained preview-document test and eight existing server security regressions passed. Core TypeScript checking and the client/server production build passed. A repeatable synthetic Chrome test bundles the sanitizer for the browser, inserts its output, checks that scripts/event attributes and unsupported positioning styles are removed, verifies supported formatting and original draft preservation, and confirms no page errors or click-triggered script execution. The existing PostCSS and chunk warnings remain; the Core vendor chunk is now approximately 1.07 MB uncompressed/362 kB gzip, requiring the planned final bundle review.

This is HTML sanitization, not complete preview isolation. The cross-origin receiver, frame headers, origin configuration, disabled publication/submission/navigation behavior and full native canvas remain outstanding. No live content, production configuration, deployment or GitHub push changed.

## Core preview receiver checkpoint

Added a renderer-only preview receiver that uses the retained PublicPageRenderer and shared sanitization. It listens before issuing an exact-origin ready handshake, accepts only the configured parent window/channel and increasing revisions, clears drafts when the session changes, and removes its listener on unmount. A revision-keyed error boundary lets a subsequent corrected draft recover from a malformed block. The rendered subtree is inert, with click/submit capture guards, so it cannot be activated through normal pointer or keyboard interaction.

Added the dedicated preview fetch policy for the forthcoming isolated entry point. It allows only enumerated public-content GET endpoints, strips caller headers/cookies, omits credentials, rejects redirects and cross-origin requests, and renders account lookups as an anonymous local result without requesting the Core account. Explicit fetch calls in retained dynamic components require this runtime policy in addition to QueryClient defaults.

Validation: five receiver, message-boundary and request-policy tests passed; the receiver test renders the actual retained raw-HTML block and checks sanitization, inert state, stale messages and channel replacement. The first fixture used an unsupported block name and was corrected to the actual registry key; the corrected test passed. Core TypeScript checking passed. The route/bootstrap configuration, network-policy installation, CSP/frame headers, dashboard frame sender and real-browser end-to-end verification are still outstanding. Neither the receiver nor fetch policy is exposed as a live route at this checkpoint. No production data/configuration, GitHub push or deployment changed.

## Isolated Core preview route checkpoint

Core now has a dedicated `/cms-preview/builder` document with an isolated client entry. It mounts the retained renderer, public branding and a separate query cache after installing the anonymous public-read policy. It never mounts the normal App/setup/auth/analytics/consent managers. Trusted parent configuration is injected by the server; the session channel stays in the fragment. Standalone or malformed sessions show an unavailable message.

The route is disabled by default. Enabling `CORE_BUILDER_PREVIEW_ENABLED=true` requires valid existing federation configuration; the frame ancestor is exclusively the validated `DASHBOARD_FEDERATION_ISSUER`. Request Origin/Host/query values cannot change it. Only this document receives its frame policy: private/no-store, noindex, no referrer, sandboxed scripts, no nested frames, no forms, and restricted script/connect origins. Existing admin framing remains unchanged. Static production and Vite development document handlers inject the same configuration.

Release preparation must route `/cms-preview/builder` to Core through the existing gateway, preserve Core asset/public-API routing, and enable it only after the dashboard sender/origin configuration and final browser review are complete. This code does not change gateway or production configuration. The new path is outside the legacy `/admin/` control area; existing compiled Core asset URLs remain under their current prefix until the retirement rollout.

Validation: 17 focused receiver/request-policy/security-header tests passed, including disabled/misconfigured behavior, forged-origin rejection and unchanged admin framing. Core typechecking and production build passed with existing PostCSS/chunk warnings. The new repeatable Chrome fixture serves the built artifacts across two synthetic local origins using the actual header middleware, completes the handshake, renders sanitized drafts and the dynamic recordings block, rejects stale revisions and submission fetches, and verifies public reads omit cookies and never call account/setup/analytics/purchase endpoints. No production writes, deployment or GitHub push occurred. The dashboard frame sender and remaining consolidation work are outstanding.

## Native Sections preview integration checkpoint

The Sections editor now opens the isolated Core renderer with desktop (1280), tablet (834) and mobile (430) viewports. It sends current unsaved blocks only after the exact frame/origin/channel/version handshake; updates carry increasing revisions, and retry creates a fresh session. Closing or navigating away removes listeners/timers. Payload failures and connection timeouts leave the editor draft intact and display an actionable status. Missing preview configuration does not block editing or saving. Frame scrolling stays inside the panel on narrow screens.

The Sections-scoped builder projection now exposes an optional nullable preview URL only when Core's isolated route is enabled, using the validated Core APP_URL. The OpenAPI/client contract is additive. Dashboard frame-src retains the public Website origin and adds only the fully configured exact CMS connection origin. Wildcard hosts are rejected for preview framing and federation issuer/origin configuration. Deployment must align Core APP_URL with the reachable origin permitted by the dashboard's CORE_MARKETING_ORIGIN and route the isolated preview path before enabling it. No production configuration changed.

Validation: 62 dashboard tests and disposable migration replay passed; 24 Core bridge tests and two preview-policy tests passed; Core/Dashboard/API/shared typechecks and the dashboard production build passed. Existing chunk warnings remain. The expanded synthetic Sections browser check covers the handshake, live edits, device widths, a fresh retry session, existing save/draft/legacy-field behavior and mobile containment with the preview open. Mobile output was inspected. This sender fixture complements the prior built-Core cross-origin browser test; production gateway/cutover verification remains a release gate. The visual canvas and broader page-builder parity, remaining modules, migration and full consolidation validation are still outstanding. No GitHub push or deployment occurred.

## Saved section insertion checkpoint

The native Sections editor now includes a searchable/category-filtered saved-section library and a position selector shared by new-block and saved-section insertion. Inserting a section copies its block data with fresh IDs and preserves unknown/nested properties; subsequent changes do not link back to the saved source. The library uses the existing Sections capability/API, cancels loading on unmount, reports load errors without changing the draft and disables insertion while the reservation or editor state prevents writes. Searching with Enter does not submit the outer editor form.

Extracted the retained dynamic-system-starter exclusion and block-copy operation into shared helpers used by both editors. Dynamic classification comes from each editor's canonical catalog rather than bundling another registry in the dashboard. The retained editor now also deep-copies nested source data when assigning fresh IDs. Inspection confirmed undo/redo belongs to the existing rich-text editor and is already present; no speculative document-history feature was added.

Validation: four shared-helper/retained-editor tests passed, Core/Dashboard typechecks and dashboard production build passed with existing chunk warnings. Expanded synthetic browser coverage passed for source filtering, insertion before the first block, fresh IDs, retained unknown/nested data, source preservation and existing preview/save/mobile behavior. The initial browser run exposed an ambiguous select label; explicit accessible labels corrected it and the rerun passed. The saved-library UI was visually inspected. Full canvas interaction and remaining consolidation requirements remain outstanding. No schema, production data, GitHub push or deployment changed.

## Generic Pages contract and builder-reference checkpoint

Added typed dashboard contracts/clients for generic CMS page list/create/read/update/delete, duplication, publish/unpublish/schedule, linked-menu inspection/removal, preview links, revision listing/restoration and page reservations. Arbitrary retained JSON content remains unknown/nullable rather than being coerced into a new block schema. Existing OpenAPI paths and schemas were compared semantically and remain unchanged.

The new Pages-scoped builder endpoint returns the canonical block catalog and minimized page/form/gallery/team/sidebar references. It shares the same projection service as Sections, while sidebar references are included only for Pages. No form fields/submissions, gallery items, biographies or sidebar widgets are disclosed. Reading these selectors does not grant access to the related management endpoints. CMS slug validation now reserves the isolated preview path and checks reserved prefixes after leading slashes, closing the rooted-path bypass for existing reserved routes.

Implementation note for the native Pages UI: these generic CMS records are distinct from primary Website manifest drafts. Saving a published generic page updates the same content record, and revision restoration updates title/content while retaining the current status and other metadata. The UI must make published-save/restore effects explicit; it must not imply that revisions restore SEO/template/sidebar metadata or create a separate unpublished working copy. Existing preview links and public routing need the final gateway/source reconciliation before retirement.

Validation: 26 Core bridge tests passed, including minimized Pages selectors, independent capability denial and reserved-slug rejection. The first slug fixture mistakenly supplied its payload as request headers; it was corrected to send JSON and then passed. All 62 dashboard tests and disposable migration replay passed. Core/Dashboard/API/shared typechecks passed. This checkpoint supplies contracts and data access; the native Pages workspace is still outstanding. No schema migration, live page publication, GitHub push or deployment occurred.

## Native generic CMS Pages checkpoint

Marketing → Content → CMS Pages now mounts under its independent Pages grant. The native workspace lists/searches/filters pages and provides draft creation, title/slug/type/layout/sidebar and SEO metadata editing, the shared block property editor, draft previews, duplication, publishing, scheduling, moving to draft, draft archival, deletion, linked-menu review/removal and revision restoration. Saved-record actions are disabled while edits are dirty. Published saves and content restores explicitly confirm their immediate effect; deletion and unpublication fetch fresh menu references and require confirmation before sending a force override. Menu removal requires the separate Menus grant. Media and saved-section insertion retain their independent grants.

Extracted the existing native block controls into CmsBlockEditor, consumed by both Pages and Sections. Reordering, deep duplication, compatibility fields, unknown/nested data preservation and saved-section insertion use the same implementation. Pages preserve unknown top-level content fields when updating blocks and keep unsupported legacy content shapes unchanged during metadata edits; legacy conversion is explicitly unavailable rather than silently clearing that content. The existing advisory reservation lifecycle verifies before writes and retains the draft on conflict. Capability changes remount the workspace.

Validation: native Pages browser checks passed for create/edit, retained fields and missing sidebar references, dirty-navigation cancellation, reservation conflict/recovery, published-save confirmation, publication/scheduling, menu-force review, revision restoration, duplication/deletion, archive/unarchive, mobile containment and revoked access. Separate Sections browser regression passed after extracting shared controls. Mobile Pages output was visually inspected. Dashboard typecheck/build and all 62 dashboard tests with disposable migration replay passed. Initial validation caught a missing view decoration mapping and stale section-specific preview copy; both were corrected and checks rerun. Existing bundle warnings remain (initial dashboard chunk about 504 kB uncompressed/149 kB gzip).

This is the native editing/publication checkpoint, not full Pages parity. Legacy format conversion, retained page starter-template selection, shareable preview-link/public-route reconciliation, all visual-canvas interactions and broad release/restore checks remain outstanding along with the rest of the consolidation plan. No live pages, production release, GitHub push or deployment changed.

## CMS page starter layouts checkpoint

CMS Pages now offers all five retained starter layouts through a lazy-loaded picker. Applying a starter changes only the unsaved block collection, preserves page metadata and unknown top-level content fields, asks before replacing existing blocks, respects the editor reservation, and participates in the existing discard/navigation guards. Starter copy, metrics, testimonials and links are explicitly identified as examples requiring verification before publication. No example was published.

Moved the existing template definitions and landing generator into a canonical shared module with a compatibility re-export for the retained admin. Template block creation now deep-copies registry defaults so nested edits cannot mutate later starter instances or shared defaults. The dashboard downloads that catalog only when the picker opens (about 62 kB uncompressed/14 kB gzip); the initial chunk remains about 504 kB uncompressed/150 kB gzip, with the existing bundle warnings.

Validation: the shared starter test passed for retained-export identity, actual block counts, known block types, fresh IDs and nested data independence. Core and Dashboard typechecks passed, and the dashboard production build passed. Expanded synthetic Pages browser checks passed for replacement cancellation, no implicit writes, discard, starter persistence, independent grants and mobile containment, alongside the existing publication/reservation/revision checks. The first browser run identified an ambiguous select label; an explicit accessible label fixed it. The mobile starter view was inspected. No migration, production data change, push or deployment occurred.

Further inspection confirmed the retained page parser does not actually convert arbitrary legacy content: it accepts block arrays, merges adjacent legacy join blocks, and otherwise returns empty content. The native editor deliberately preserves unsupported data rather than reproducing that clearing behavior. Any conversion requires an inventoried source format and lossless mapping. Landing generator UI, visual-canvas parity, public-route reconciliation and the broader consolidation requirements remain outstanding. GitHub push remains deferred until the entire goal is complete and validated, as requested.

## Landing page generator checkpoint

CMS Pages now includes the retained landing-page composition workflow in the lazy-loaded starter workspace: campaign goal/headline/introduction, audience selection, recommended or individually selected blocks, call-to-action text/link, and draft preview before applying. The native UI uses the shared generator and the existing isolated BuilderPreview instead of introducing another renderer. Changing a goal refreshes its recommended selections; chosen block order is shown, and normal page block controls remain available after applying.

Headline and explicit CTA values are required before progressing, audience and block selections gate later steps, and Enter in generator inputs cannot submit the enclosing page form. Applying uses the same replacement confirmation, draft-only update, metadata preservation, discard guard and reservation restrictions as starter layouts. No server write occurs until the normal Save action. Existing audience/template copy is identified as examples; none was published or asserted as verified P1 proof.

Validation: two shared tests pass, including all offered generator goals/block types, chosen order, fresh IDs and headline/CTA propagation. Expanded synthetic Pages browser checks pass for required-step gating, Enter without saving, replacement cancellation, applying without saving, saved generated content, unchanged page title, mobile containment and existing Pages behavior. Mobile generator output was inspected. Dashboard typecheck and production build pass with existing bundle warnings. No schema, production data, push or deployment changed. Full visual-canvas parity, legacy/source reconciliation, remaining CMS and User Manager modules, CRM/agreement integration and final migration/release validation remain part of the active goal.

## Save block as reusable section checkpoint

The shared native block editor now supports saving an individual block as a reusable section from both Pages and Sections. This control requires the independent Sections capability and uses the existing capability-checked creation endpoint. Opening the panel snapshots the block with independent nested data and a fresh ID, preserving unknown properties. Name, description and category are editable; saving creates a new section without saving or changing the source editor. The saved-section browser is closed after creation so its next open fetches fresh records.

Failures keep the snapshot and entered metadata available for retry. Empty names and pending/disabled states prevent submission, and Enter in panel inputs cannot submit the enclosing editor form. Opening the panel focuses its name field. Existing editor reservations still disable these controls when editing is unavailable.

Validation: expanded synthetic Sections browser checks pass for no implicit source write, empty-name prevention, Enter handling, failed-save retry, copied nested data and independent IDs, alongside existing editor checks. Pages browser regression passes and verifies that a Pages-only user has no save-as-section control. Dashboard typecheck and production build pass; 26 Core bridge tests and two shared section-copy tests pass. No schema, production data, GitHub push or deployment changed. The broader consolidation goal and remaining builder/module/migration work remain active.

## User Manager password recovery checkpoint

Added an Owner-only password-recovery action to the native User Manager and its typed API contract. The server revalidates Owner access, locks the target profile, requires an active existing account, resolves the saved email itself, and invokes the same Better Auth reset flow already used by dashboard sign-in. The callback is fixed to the configured dashboard origin. It records `account.password_recovery.requested` without the recovery link/token and enforces a one-minute per-account limit under the target lock. The response contains only success, never a password or recovery token. MFA requirements and permissions are unchanged; session revocation remains the explicit existing action.

The UI confirms the saved recipient, keeps unsaved profile/access edits intact, disables recovery for inactive saved accounts, and displays delivery-queue confirmation inside the open editor. No real email was sent during implementation. Password tokens and email outbox records continue to be owned by the canonical authentication flow; its database work is separate from the surrounding management audit transaction. A database failure after auth queues an email could therefore require audit reconciliation; the recovery action does not claim distributed atomic delivery.

Validation: API, Dashboard and shared-library typechecks pass, generated clients are current, and dashboard production build passes with existing chunk warnings. Synthetic User Manager browser checks pass for cancellation, sending without saving the profile draft, visible confirmation, existing version-conflict behavior, invitations and mobile containment. The disposable dashboard integration suite exercises authorization, inactive/missing accounts, ignored recipient/redirect overrides, canonical callback, unchanged MFA policy, token-free response/audit and resend limiting. Remaining User Manager notification/linking and broader consolidation work remain active. No production configuration, data, GitHub push or deployment changed.

## Crew and client User Manager boundary checkpoint

Reconciled an authorization inconsistency: office route guards already excluded crew, but the shared capability helper and User Manager accepted/displayed office grants for crew. The shared helper now denies office capabilities for crew and client accounts, and both actor/session projections return empty office grant lists for those account types. Assigned-work and client/property authorization remain in their existing dedicated paths. Legacy crew review suggestions no longer propose ineffective office grants.

Invitation validation and account updates reject office grants or form subscriptions on crew accounts, matching the existing client restriction. The User Manager hides office checkboxes for both account types and explains their access boundaries. Previously stored incompatible grants are preserved in storage and displayed for explicit cleanup; clearing changes only the draft until Save. No migration silently deletes historical grants or subscriptions.

Validation: all 62 dashboard integration tests and disposable migration replay passed, including attempts to assign crew office grants and a crew session containing incompatible stored grants. All five capability tests pass, covering every known capability for crew/client denial and empty crew suggestions. Expanded User Manager browser checks pass for account-type switching, hidden office controls, existing incompatible grants and draft-only cleanup. API, Dashboard and shared-library typechecks and dashboard production build pass with existing chunk warnings. Validation found and corrected a test response typing issue and an ambiguous account-type selector label. No production data, push or deployment changed. Remaining User Manager form notification/catalog delivery and the full consolidation scope remain active.

## User Manager notification catalog contract checkpoint

Added a read-only Owner-only website form catalog through the existing bounded CMS service bridge and generated dashboard client. Core requires an active, attested canonical Owner after service authentication and fresh-grant resolution; local admin status or a Forms grant alone is insufficient. The response contains only ID, name, slug, active status and system status. Inactive forms remain identifiable for explaining saved subscriptions. Fields, submissions, integration settings and recipient addresses are excluded. This account-administration selector remains available independently of CMS editor feature visibility.

Delivery investigation found that the retained forms service currently snapshots admin recipients at submission time, uses private configured recipients for P1 public forms, and embeds `/admin` links in queued effects. Dashboard subscription storage therefore cannot be presented as effective delivery control yet. The next integration must reconcile the existing recipient sources, validate selected forms, recheck current access/subscription state at delivery, update links and preserve existing queued/history records through migration. No subscription checkbox UI or delivery behavior was changed in this checkpoint.

Validation: 27 Core CMS bridge tests pass, including member denial, unattested/inactive Owner denial and exact minimized catalog output. Seven dashboard transport tests pass, including the explicit GET-only Owner allowlist entry. Core/API/shared-library typechecks pass, generated clients are current and diff whitespace checks pass. A test-edit command initially used the wrong working directory; it made no change, was corrected, and the intended test was added and passed. No production data, configuration, push or deployment changed. The complete consolidation objective remains active.

## Canonical form recipient eligibility service checkpoint

Added confidential service endpoints under the existing federation ingress for paginated notification subject IDs and one-at-a-time current recipient resolution. They reuse the existing authenticated service boundary, strict purpose/body validation, browser-header rejection, rate limiting and bounded JSON transport. Lists contain at most 20 account IDs and a continuation cursor; email addresses are returned only when resolving one currently eligible account. The Core client rejects duplicate subject IDs, inconsistent cursors, oversized result sets and mismatched recipient identities.

Canonical eligibility requires an active verified office account, explicit form subscription, Forms access (or Owner), and enrollment when MFA is required. Crew/client accounts are excluded. Recipient resolution reads the current email and repeats all eligibility checks; a stored account ID is not a lasting permission grant. These services are the delivery integration contract only: existing effect queues and sender behavior are not switched yet. The worker, subscription UI/validation, private-recipient reconciliation and legacy queue migration remain to be connected before claiming effective consolidated notifications.

Validation: all 62 dashboard integration tests and disposable migration replay pass. Added cases cover canonical service lookup, disabled accounts, removed grants/subscriptions, unmet MFA enrollment, crew exclusion and two-page enumeration with no duplicates. Seven Core federation-client tests pass, including malformed and mismatched notification responses. API and Core typechecks pass. No production email, data migration, configuration, push or deployment occurred. The full consolidation goal remains active.

## Canonical form notification worker checkpoint

The form-effect queue now supports a canonical `dashboard_form_notification` payload with form/account IDs and the submitted notification content, without a frozen recipient email. Its worker resolves current eligibility/email through the authenticated dashboard service immediately before delivery and uses the canonical dashboard Forms URL. Removed/ineligible recipients complete as explicitly skipped; unavailable federation, lookup failures and provider failures enter the existing bounded retry/dead-letter flow. There is no legacy-address fallback. Provider delivery remains at least once, as with existing effects.

Queue deduplication keys distinguish canonical account IDs, preserving one job per recipient per submission and existing idempotent submission behavior. This extends the JSON payload contract without a destructive schema migration. Existing `admin_notification` jobs retain their current behavior pending the explicit legacy-recipient/queue migration; no live queue was rewritten. Submission fan-out, subscription UI/validation, native Forms navigation and rollout reconciliation are still required before enabling the consolidated delivery path.

Validation: 11 delivery/worker tests pass, covering current email selection, canonical link generation, revoked-recipient skips, lookup/provider retry and token-fenced completion. All 14 form-effects database tests pass against a new disposable local PostgreSQL instance, including canonical recipient separation and replay deduplication; the instance was removed afterward. Core typecheck passes after correcting a missing schema barrel export. A test-edit command initially targeted the wrong working directory and made no change; the corrected edits and intended tests passed. No production email, migration, configuration, GitHub push or deployment occurred. The full consolidation goal remains active.

## Canonical notification acceptance and fan-out checkpoint

Added opt-in `CORE_DASHBOARD_FORM_NOTIFICATIONS_ENABLED=true` acceptance mode. In that mode, a submission atomically stores a canonical dispatch effect alongside its other effects; public form acceptance performs no dashboard recipient lookup and therefore remains available during federation outages. The default retained recipient mode is unchanged. The opt-in replaces new legacy recipient effects and requires explicit recipient/subscription reconciliation before rollout; no environment was enabled here.

The worker resolves one bounded page of subject IDs per dispatch job. Under the existing claim-token transaction, storage verifies the submission/form relationship, inserts deduplicated account delivery jobs plus any continuation dispatch, and completes the parent. A stale claim produces no children. Completion replay does not create duplicates. Each child still rechecks eligibility and current email immediately before sending. Pagination failures use the existing retry/dead-letter mechanism rather than holding the public submission open or discarding its notification intent.

Validation: 23 intake/worker/delivery/CRM tests pass, including public acceptance with federation disabled, exclusive canonical dispatch selection and retry of failed recipient-page lookups. All 15 form-effect database tests pass against a fresh disposable local PostgreSQL instance, including recipient/continuation insertion, stale-claim fencing and replay deduplication. The database was removed afterward. Core typecheck and diff checks pass. Subscription UI and validation, legacy queued-message conversion, private-recipient mapping and the Forms destination still remain rollout gates. No production notification, configuration, migration, GitHub push or deployment occurred. The full consolidation goal remains active.

## Office-account form subscription controls checkpoint

Native User Manager now offers per-form notification checkboxes for office team accounts. The catalog loads on demand through the Owner-only bridge, supports retry, labels inactive/system forms, and identifies saved IDs absent from the catalog. Missing/inactive selections remain visible and removable; no catalog failure clears a draft. Adding selections requires Forms access, while existing selections can be preserved or removed after that grant is removed. Crew/client boundaries remain unchanged.

Account-update and invitation routes now validate only newly added subscription IDs against the current active website catalog using a fresh Owner service grant. Unknown/inactive additions and additions without Forms access are rejected. Retained selections, removals and unrelated profile edits do not require a website catalog request. Existing account version checks still reject concurrent stale updates after validation. Duplicate subscription IDs are rejected by the input contract. Catalog failure prevents new selections from saving without applying the accompanying account/invitation mutation.

Validation: all 63 dashboard tests and disposable migration replay pass, including addition-only validation, active/known form checks, permission checks, preservation/removal without lookup and lookup failures. Expanded User Manager browser checks pass for catalog failure/retry, disabled additions without Forms access, active selection, inactive-form denial and existing account workflows. API/Dashboard typechecks and dashboard production build pass with existing chunk warnings. Owner-account notification preference editing remains separate work because Owner identity/access editing is intentionally protected; legacy recipient/queue reconciliation and canonical delivery rollout remain incomplete. No production email, configuration, data migration, GitHub push or deployment occurred. The complete consolidation objective remains active.

## Owner notification preference editor checkpoint

Owner rows now have a separate notification-preferences editor. It reuses the form catalog controls while keeping profile, status, role, capability and MFA fields outside the edit surface. The dedicated strict API accepts only subscription IDs and an expected version, verifies both the acting Owner and target Owner, validates newly added forms, updates only subscription/version timestamps and records an account audit event. It does not revoke sessions or change Owner privileges. General Owner account edits remain prohibited.

The modal guards unsaved cancellation, retains selections on errors/conflicts, and offers explicit reload of saved preferences before retrying. Successful saves refresh the account list. This closes the Owner subscription editing gap without turning the protected Owner account into an ordinary editable team account.

Validation: all 63 dashboard integration tests and disposable migration replay pass, including non-Owner denial, non-Owner target denial, rejection of extra privilege fields, stale-version rejection, preserved role/name/status/MFA/capabilities/session and audit creation. Expanded browser checks pass for the restricted Owner surface, conflict draft preservation, reload and successful save with only the expected payload fields. API/Dashboard/shared typechecks and dashboard production build pass with existing chunk warnings. Generated contracts/clients are current. Legacy queued-recipient migration, private-recipient mapping, Forms workflow integration and the broader consolidation release gates remain active. No production email, configuration, migration, GitHub push or deployment occurred.


## Forms service bridge and contract checkpoint

Exposed the retained Forms, submissions and delivery-job operations through the existing Marketing CMS service bridge and generated dashboard client. Every Core handler now requires canonical `marketing.content.forms` access. Commercial inquiry backfill additionally requires an active attested Owner at both service boundaries. Delivery-job queries allow only bounded scalar status/limit/cursor filters. Existing form storage, submission history and delivery queues remain the source of truth; this checkpoint makes no data migration.

Closed a system-form identity bypass while connecting the editor APIs: ordinary creation cannot provision a system form, custom forms cannot promote themselves to system status, and existing system forms cannot be demoted, renamed by slug or changed to a different kind. Their editable content remains available, and the existing system-form delete prohibition remains enforced. These protections also apply to retained routes.

Validation: 29 Core bridge/backfill route tests pass, including denied Forms access, system-form creation/demotion/rename/deletion protection, allowed content updates, submission access and Owner-only backfill. All 64 dashboard integration tests and disposable migration replay pass, including the Forms transport allowlist and bounded query checks. Core, API, Dashboard and shared-library typechecks pass; generated contracts/clients are current and diff whitespace checks pass. A mutation test helper initially omitted request bodies; it was corrected and the intended protection assertions then passed. Native Forms editing, submissions/delivery UI, recipient migration and the full consolidation scope remain active. No production data, configuration, email, GitHub push or deployment changed.

## Native Forms workspace checkpoint

Added `/marketing/content/forms` to Marketing navigation with its independent Forms capability, lazy-loaded dashboard workspace and canonical API client. It lists/searches website forms, displays active/system identity, edits form metadata and submission settings, creates inactive custom form records, and reads saved submissions. System slug/kind controls are disabled in addition to the existing server protection. Settings edits carry the complete existing fields/settings objects in the request rather than reconstructing field definitions. The retained server schema remains authoritative; this checkpoint does not add storage-schema compatibility guarantees for unknown future fields.

Active-form saves require confirmation, unsuccessful saves preserve drafts, duplicate submission is guarded, and cancellation/navigation guard unsaved changes. Submission values are rendered as text, including nested answers and HTML-looking strings. Loading failures can be retried. The responsive workspace follows the consolidated dashboard theme.

Validation: synthetic browser checks pass for protected identity, failed-save retry, nested field/settings preservation in the payload, submission escaping, cancellation, permission denial and mobile containment. Five route tests pass, including explicit Forms-only access and crew denial. Dashboard typecheck and production build pass with existing chunk-size warnings. Browser checks identified ambiguous select/textarea accessible names; explicit labels corrected them before passing. Native field builder/advanced configuration, submission export/deletion, delivery-queue/backfill interfaces, concurrent edit protection and full retained Forms parity remain outstanding. No production records, emails, configuration, GitHub push or deployment changed; the complete consolidation objective remains active.

## Native form field builder checkpoint

Replaced the read-only field list with editable controls for all 21 retained field types. Users can add fields, edit labels/keys/help/placeholder/width/required behavior, reorder, duplicate and remove fields from future submissions. Duplicates receive fresh field IDs and keys, plus fresh list-column IDs. Existing nested configuration is copied and preserved when editing neighboring values. Removal explicitly confirms that historical answers remain saved.

Added type-specific controls for name format, section/divider styling, HTML text, page-step labels, choice layout/selection, consent, address layout, hidden values, time format and repeatable-list limits/columns. Choice labels/values and image URLs are editable without raw JSON. Existing unknown field types retain their saved configuration. Duplicate field IDs/keys and missing labels/keys are rejected locally before save; canonical Core request validation remains the server boundary. This checkpoint does not claim shared schema deduplication or a migrated public-form preview.

Validation: expanded synthetic browser checks pass for independent duplicate IDs/keys, ordering, preserved nested configuration, duplicate-key rejection before any mutation, image-choice controls, repeatable columns and mobile containment, alongside existing failed-save recovery, protected system identity, submission escaping, discard and permission checks. Dashboard typecheck/build and whitespace checks pass; existing build chunk warnings remain. Public-renderer preview, template parity, delivery queue/backfill, submission export/deletion, concurrency protection and remaining consolidated modules still require work. No production data, email, configuration, GitHub push or deployment changed.

## Native form delivery monitoring checkpoint

Forms now opens a native delivery monitor for the existing retained queue. The default view shows pending commercial handoffs and failed jobs; completed and all-monitored filters preserve the actual server scope rather than implying a history of every successful notification. Records show saved submission/delivery references, status, attempts, timestamps, error codes and linked lead references when available. Cursor pagination deduplicates appended records, filter changes reset the page, and canceled/stale reads cannot overwrite a newer view.

Only failed records offer Retry. The action confirms its possible delivery effect, guards duplicate clicks, preserves errors for retry, and reports acceptance as queued rather than delivered. It marks the successful local row queued before refreshing so a refresh failure does not immediately offer a duplicate retry. Closing or switching filters prevents obsolete results from updating the replacement view. Refresh is manual in this checkpoint; no automatic background refresh or commercial backfill interface has been added.

Validation: expanded synthetic Forms browser checks pass for cursor pagination, canceled retry without mutation, failed retry recovery, successful queue acceptance, completed-job retry exclusion, filter/cursor reset and all prior builder/settings/submission checks. Dashboard typecheck and production build pass with existing chunk warnings. Remaining Forms preview/templates, exports/deletion, backfill and concurrency work, plus the full consolidation scope, remain active. No production queue, email, configuration, GitHub push or deployment changed.

## Form submission export checkpoint

Added downloadable CSV and JSON to native Forms submission history. CSV uses the union of saved answer keys, so fields removed from the current definition remain represented. It quotes embedded delimiters/quotes/newlines, serializes nested answers as JSON and neutralizes formula-like spreadsheet cells, including user-controlled headers and metadata. JSON preserves the full loaded records and exact nested values. Downloads use sanitized filenames and release temporary object URLs. Buttons remain unavailable until the submissions request succeeds.

The retained admin CSV now shares the same pure export helper, preventing divergent escaping during consolidation. Export reads the already-loaded submission collection; the existing bounded bridge still rejects oversized responses rather than exporting a silent subset. Server pagination/streaming for very large histories remains outstanding.

Investigation confirmed that deleting a submission currently cascades its delivery queue/history records. No new deletion control was exposed in this checkpoint: a deliberate retention/cancellation workflow must accompany that operation during consolidation. This is remaining implementation work, not a request for additional authorization.

Validation: three shared export tests pass for historical keys, nested values, formula defenses, quoting, invalid legacy timestamps and source immutability; two retained Forms tests pass. Expanded synthetic native browser checks verify actual CSV/JSON downloads and nested/value preservation alongside prior Forms workflows. Core and Dashboard typechecks, Dashboard production build and whitespace checks pass, with existing build chunk warnings. No production records, queue changes, emails, GitHub push or deployment occurred. The full consolidation goal remains active.

## Form image-choice media integration checkpoint

Image-choice fields now open the shared dashboard media library when the account also has `marketing.content.media`. Forms access alone does not expose the picker. The library retains its canonical server authorization, upload and browsing behavior, accepts images for this field and disables non-image assets. Choosing an image changes only that option's draft URL; the form still requires Save. Existing image URLs remain editable without granting media-library access.

The picker uses a native modal dialog mounted outside the editor form, avoiding nested forms and accidental form submission. Escape/close dismisses it; field identity selects the destination while preserving the other options and configuration. Capability changes remount the workspace through its existing identity/grant key.

Source review also corrected a prior outstanding-work assumption: the retained Forms page has field presets and blank form creation, but no distinct saved-form-template library. Earlier checkpoint references to “template parity” were unverified placeholders, not evidence of a missing retained template module. Form preview, drag interaction parity, deletion/retention handling, concurrent editing and the rest of the approved consolidation scope still require work.

Validation: expanded synthetic browser checks pass for opening the image picker, rejecting document assets, selecting an image without saving the form, and absence of the picker without the Media grant, alongside existing Forms regression checks. Dashboard typecheck/build and whitespace checks pass with existing chunk warnings. No production uploads, records, emails, configuration, GitHub push or deployment changed. The full consolidation goal remains active.

## Isolated public form preview checkpoint

Forms now opens a draft preview using the actual website `PublicFormRenderer` in the existing isolated cross-origin preview runtime. A Forms-only `/form-builder` bridge operation returns just the configured preview URL, so opening a form preview requires neither Pages access nor unrelated content selectors. Connection failures can be retried without losing the editor draft; missing rollout configuration remains visible rather than substituting an unrelated preview.

Extended the bounded preview envelope with an optional form draft, mutually exclusive with page blocks, and advanced the protocol to version 2 so a version-1 renderer cannot silently accept a new form payload. The receiver validates form content with the canonical schema, sanitizes a rendering copy, supplies an unsaved form override and recovers from malformed drafts on a later valid revision. Existing exact-origin/source/channel/revision checks and size/depth limits apply to forms. The override avoids reading the saved public form. The sender keeps the original editor data unchanged, supplying preview-only placeholder name/slug when those inputs are still blank.

The existing inert renderer and read-only network policy prevent submission and notification effects. Preview remains read-only: multi-step navigation and dynamic user input are disabled, so interactive step coverage remains a follow-up. Version 2 requires coordinated dashboard/Core release; no production preview configuration was enabled.

Validation: 34 Core protocol/receiver/bridge tests pass, including Forms-specific access, mixed-document rejection, older protocol rejection, invalid-draft recovery, HTML sanitization and no saved-form fetch. Eight dashboard transport tests pass; Core/API/Dashboard/shared-library typechecks pass. Core and Dashboard production builds pass with existing chunk warnings. Synthetic native Forms browser checks verify draft transfer with no save, and the Sections browser regression passes. The built cross-origin runtime test additionally renders unsaved form fields and confirms a programmatic submit attempt creates no form API request; anonymous-read and injection defenses still pass. Generated contracts/clients are current and diff checks pass. No production records, email, configuration, GitHub push or deployment changed. The full consolidation objective remains active.

## Multi-step form preview checkpoint

Added a preview-only step selector outside the inert public form. Editors can inspect every step, including named and empty page breaks, without enabling field submission or notification effects. Page splitting now lives in a shared pure helper used by the public renderer and the preview selector. The optional controlled page index applies only with a draft override; ordinary public forms retain their local Next/Previous navigation. Out-of-range preview indices clamp after field/page edits, and a new preview session resets to the first step.

Validation: four page-layout/receiver tests pass, covering retained leading/consecutive/trailing page-break semantics, source preservation, preview navigation and ordinary public navigation. The built cross-origin browser test switches steps, verifies the expected fields and retains inert/no-form-request checks. Core typecheck/build and whitespace checks pass with existing build warnings. An initial test command ran from the workspace root and used the wrong test environment; it failed before the intended receiver tests. The corrected Core-local command and final rerun passed. No repository dependencies were changed. Production data, configuration, GitHub push and deployment remain untouched; the complete consolidation goal remains active.

## Form optimistic concurrency checkpoint

Existing form updates now require the timestamp observed when the editor loaded its saved draft. Core compares that timestamp atomically in the SQL update and returns a conflict instead of overwriting newer data. Comparison uses the millisecond precision exposed by the JSON client, while updates advance the database timestamp monotonically by at least one millisecond. This handles database microseconds, near-simultaneous writes and legacy null timestamps without adding a schema migration. Internal system-form updates also advance the timestamp.

Both the native dashboard and retained admin editor now send the precondition. Missing/invalid preconditions fail validation, so the updated editor/handler must release together. The generated update contract requires the precondition; creation remains unchanged. The native conflict workflow keeps the draft and provides an explicitly confirmed reload of the saved form, updating the cached list as well. It never silently rebases and retries an outdated draft.

Validation: all 16 form-effects database tests pass against a fresh disposable local PostgreSQL instance, including competing writers, microsecond timestamps under a non-UTC database session, stale/null rejection and legacy-null updates. The instance was removed afterward. All 31 Core bridge/retained Forms tests pass, including missing preconditions and conflict responses. Expanded native browser checks pass for the sent timestamp, conflict draft retention, canceled reload and accepted reload. Core/API/Dashboard/shared typechecks and Dashboard build pass with existing chunk warnings. API documentation and generated clients are current. No production data, schema migration, configuration, GitHub push or deployment changed; the full consolidation goal remains active.

## Events service bridge and typed contract checkpoint

Connected retained event management to the Business Center bridge behind the canonical Events capability. Events uses its own Events feature gate before the generic CMS gate, preserving independent availability. Explicit method/path allowlisting covers event listing/detail/create/update/delete/duplicate, venues, organizers, analytics and existing reminder/recording notification actions. Added a retained single-event read for native editor loading. Router-level authorization now protects both the service mount and retained event routes, including mutations and notification actions.

Added typed event, venue and organizer contracts covering the current schedule, recurrence, taxonomy/audience, registration, venue/location, speaker, recording and pricing fields, plus generated clients. Existing OpenAPI paths/schemas were compared structurally against HEAD and remain unchanged; this checkpoint adds nine paths and nine named schemas. No event data was copied and no public event route changed.

Source assessment identified remaining event-specific release work: registration management and settings still need scoped bridge/native surfaces; legacy member/counselor visibility and registration/payment identities need explicit reconciliation; cancellation currently cancels registrations before querying confirmed recipients, and the notification response labels an asynchronous attempt count as sent. These retained behavior gaps must be resolved with the native workflow rather than treated as completed parity. No cancellation or notification was executed during implementation.

Validation: all 30 shared Core bridge tests pass, including Events-only access, independence from CMS feature availability, denied neighboring capabilities on read/mutation/notification routes and Events feature shutdown. Nine dashboard transport tests pass. Core/API/Dashboard/shared-library typechecks and Dashboard production build pass with existing chunk warnings. A duplicated test mock initially replaced the SEO audit's event fixture and caused two failures; the fixture was corrected and the full bridge suite passed. Generated clients are current and whitespace checks pass. No production data, email, configuration, GitHub push or deployment changed. The full consolidation goal remains active; the native Events workspace is next.

## Native Events workspace checkpoint

Added `/marketing/content/events` to Marketing navigation with its own Events capability and a lazy-loaded native workspace. It lists/searches/filters events, loads a fresh record for editing, creates inactive-registration draft events, edits details/classification/location/schedule/publication, and creates retained-service draft duplicates for review. Description editing reuses the shared rich-text editor with independent Media access. Existing registration, fee, recurrence, recording and linkage values travel unchanged when editing other details; IDs/creation metadata are excluded from save payloads.

Dates are entered explicitly in the user's device time zone, sent as UTC, and retain their original timestamp precision when untouched. Listing cards format dates in the time zone they label, with a device-zone fallback for invalid legacy zone values. Visibility and the retained website membership restriction are presented separately from dashboard staff permissions. Published saves and removal from publication require confirmation; canceled events can have details edited but their status is not reactivated through this initial surface. Native cancellation/notification/registration, venue/organizer management, advanced scheduling, concurrency and the remaining Events workflows are still pending.

Failed saves preserve drafts, cancellation guards unsaved edits, list/detail failures can retry, and duplicate requests are guarded. A duplicate remains a draft and is opened for date/settings review. No event deletion or outbound-notification control was added in this checkpoint.

Validation: synthetic browser checks pass for preserved timestamps/settings, device-time conversion, draft defaults, publication cancellation/confirmation, failed-save recovery, duplication, status filtering, discard protection, mobile containment and denied access. Six dashboard route tests pass, including Events versus Forms separation and crew denial. Dashboard typecheck/build and whitespace checks pass with existing chunk warnings. Browser fixtures were corrected to match Chrome's normalized datetime-local value format; final checks pass. No production events, email, configuration, GitHub push or deployment changed. The full consolidation goal remains active.

## Native event venue and organizer checkpoint

Added shared venue and organizer directories under Events, with search, create/edit/delete, failure retry, draft preservation and unsaved-change guards. Venue controls cover address, coordinates, virtual status, parking, accessibility, transit and arrival notes. Organizer controls cover contact information, description and image URL; image selection uses the shared Media library only with the separate Media grant. Editors preserve other loaded fields and exclude record metadata from writes.

The event editor now loads shared-record choices under Events access. A failed lookup preserves saved links and offers retry; missing saved references remain identifiable rather than being silently cleared. Choosing a record changes only its link. Explicit confirmed actions copy venue details into the event's location/coordinates/delivery mode or organizer details into its speaker fields, without modifying the shared record. This retains the original copy-detail capability while making the replacement of event-specific values deliberate.

Deletion confirms the existing data behavior: the shared record is removed, event records remain, foreign-key links are cleared and copied event details remain. No automatic deletion was performed during implementation. Shared-record concurrent-edit protection remains part of the outstanding Events work, along with registrations, notifications/cancellation, settings and advanced event controls.

Validation: expanded synthetic Events browser checks pass for venue save failure/retry, retained arrival-related data, venue creation, canceled/confirmed deletion, organizer editing with retained image, Media-grant exclusion, catalog failure/retry, retained links, explicit copy/cancel behavior and no shared-record mutation from event selection, alongside prior Events workflows. Dashboard typecheck/build and whitespace checks pass with existing chunk warnings. No production records, uploads, email, configuration, GitHub push or deployment changed. The complete consolidation goal remains active.

### Event attendee workspace — 2026-09-17

Implemented native attendee search, registration/attendance filters, check-in and attendance correction under Marketing Events. The confidential CMS bridge exposes a minimized attendee DTO: contact details, registration/payment status, notes and attendance dates; local account links, payment intent/checkout identifiers and payment amounts are omitted. Both operations require `marketing.content.events` and the Events feature independently of the general CMS feature. Check-in accepts only a boolean, constrains the atomic write by both event and registration ID, preserves the original check-in timestamp on repeated requests, and records the existing activity-log event. Attendance does not modify admission, payment or waitlist state and sends no email.

The UI confirms attendance changes, renders notes as text, uses the returned saved record, and blocks further attendance writes after an ambiguous failure until a successful refresh. Core retains ownership of registration data. Generated API contracts and the explicit Dashboard transport allowlist cover only attendee listing and attendance updates; status changes, deletion and refunds are not newly exposed through these paths.

Validation: 31 Core bridge tests; 9 Dashboard transport tests; one real PostgreSQL attendance test against a newly created local disposable database with full Core migration replay (event mismatch, missing record, repeated check-in timestamp, clearing attendance, registration/payment preservation); extended Chrome Events workflow test (load retry, search, confirmation cancellation, mutation failure/recovery, check-in/clear, literal HTML notes, mobile layout); Core/API/Dashboard type checks and Dashboard build. Existing bundle-size advisories remain. All listed checks passed. No production data, emails, migrations or deployment touched.

Remaining Events work includes advanced registration settings and form references, cancellation/waitlist transaction and notification correctness, recurrence/recording controls, export retention and concurrency workflows. This checkpoint does not complete the consolidation goal. Final GitHub push remains deferred until the full goal is validated; production cutover requires separate release authorization.

### Event registration controls and form references — 2026-09-17

Added native registration enablement, automatic/manual approval, capacity, waitlist, opening/closing dates and reusable form selection. Date controls explicitly use the editor’s device time zone and preserve unchanged stored timestamps. Pending registrations are now included in the attendee filter. The selector is authorized by Events and returns only IDs, names and slugs of active non-application forms, without granting Forms administration or disclosing fields/settings/notification recipients. Its static transport route precedes the event-ID wildcard.

Core validates new form assignments against eligible existing forms. An unchanged saved reference may be retained when a form becomes inactive; the UI preserves and labels unavailable references instead of erasing them, offers retry for catalog failures, and permits explicit replacement/removal. Registration booleans, capacity and each date are validated server-side. Updates validate the combined saved/requested date range so a partial close-date update cannot precede the saved opening date. Registration settings change future admission behavior through the retained public handlers; they do not rewrite existing attendees.

Validation passed: 32 Core bridge tests (including selector minimization, capability isolation, unchanged/inactive/application form handling and invalid partial date/capacity requests), 10 transport tests, extended Chrome Events regression (catalog error/retry, saved reference preservation, form replacement, manual approval, waitlist, capacity and local-time-to-UTC persistence), Core/API/Dashboard type checks, shared-library type checks, Dashboard build. Existing bundle-size warnings remain. All browser fixtures are synthetic and no live submissions were sent.

Remaining Events parity includes admission status/cancellation/waitlist correctness, fee/payment configuration, recurrence, recording controls and export/concurrency workflows. Full consolidation and final push remain outstanding; no production release was performed.

### Event cancellation transaction and Dashboard action — 2026-09-17

Replaced the retained cancellation cascade that canceled registrations before querying confirmed recipients (which produced an empty notification list). Event cancellation now commits the event change and cancellation of confirmed, waitlisted **and pending** registrations in one transaction. Its returned changed rows supply notification recipients. Concurrent/repeated cancellation requests cannot reselect already-canceled registrations, and previously canceled timestamps, payment state/amount/provider references and attendance history remain unchanged. Removed the superseded count-only cancellation helper after confirming its sole caller was the replaced route.

Added an explicit Dashboard cancellation action, separate from ordinary event editing. The confirmation explains the affected registration states, attempted emails and absence of refunds. It sends only the status change, uses the saved response, removes the action after cancellation and tells users to verify saved status after an uncertain failure. Core logs both false email-provider results and thrown failures without adding recipient emails to these new log messages.

Validation passed: 33 Core bridge tests, extended Events Chrome workflow including confirmation dismissal and cancellation payload/result, Core and Dashboard type checks, Dashboard build, and two disposable PostgreSQL tests. The database cancellation test concurrently cancels one event, verifies exactly one three-recipient snapshot, preserves prior cancellation/payment history and another event, and uses an injected database trigger failure to prove both event and registration writes roll back. The test initially asserted the outer error text; corrected it to inspect the ORM-wrapped database cause, then passed. The local disposable database was removed after testing. No live emails or production mutation occurred.

Release work still includes durable notification retries/delivery visibility and serialization with public registration creation and individual status/waitlist transitions. This transaction alone does not establish those broader concurrency guarantees. Event fee/recording/recurrence parity, the other consolidation tracks and full release validation remain outstanding; GitHub push is deferred until the full goal is complete.

### Agreement template library backend and migration — 2026-09-17

Extended the existing `agreement_template` foundation rather than introducing another terms store. Additive migration `0029_agreement_template_library.sql` adds MSA/scope/cost/package kinds, draft/publication/archive state, structured payloads, family/source references and edit versions. Existing IDs, body text, version, active state and timestamps are preserved; each legacy row gets its own family because names alone cannot prove historical lineage. Replaced name/version uniqueness with family/version uniqueness so independently named copies can coexist. The legacy body-only creation/revision contracts retain immediate MSA publication; typed creation/revision use drafts.

The library supports create, read/filter, optimistic draft edit, publish, revise, duplicate and archive. All mutations require the independent template-maintenance grant, serialize family changes and write audits. Published versions are immutable through the API. Package publication checks exact published component IDs/types and stores those references; subsequent archival does not rewrite a package. Cost defaults use integer cents and at most two decimal quantity precision, with distinct one-time, fixed-monthly and per-visit bases. The existing estimate selector remains published-MSA-only and continues snapshotting text; new kinds cannot be accidentally substituted into the old recurring estimate path. Generated OpenAPI/client contracts are updated.

Validation: the full isolated Dashboard suite passed 70 tests including migration replay, legacy lifecycle regressions, two simultaneous draft updates (one succeeds/one conflicts), type/permission boundaries, publication/revision/archive, fixed package references, independent duplicates, stale revision rejection, and preserved estimate snapshots. A separate populated pre-0029 table test applies the actual migration and compares every original field before/after. API/Dashboard/shared-library type checks passed. No P1 legal content was invented or imported, and Blythe remained untouched.

Deployment ordering: quiesce template writers while applying 0029 and deploying the new API together. Old API binaries may violate the new status/active check when revising templates and are not safe mixed-version writers. For rollback, retain the migration/data and deploy a schema-aware prior-compatible handler; do not drop new columns or collapse families once typed templates exist. A pre-cutover database backup and staged migration rehearsal remain release gates. No production migration has been applied.

The native Templates workspace, clause-library links, placeholder/rich-text handling, client composition drafts, comparison/apply controls, Save as template review, proposal/PDF snapshot parity and full approval/activation rehearsal remain required. This is the library foundation, not completion of the agreement workflow or consolidation goal.

### Native agreement Templates workspace — 2026-09-17

Added `/agreements/templates` under Revenue with an Agreements/Templates navigation link when both permissions are present. The route requires `revenue.agreement-templates.manage` independently of Sales, Agreements or Billing; it loads the library through generated clients and stays separate from service-agreement record routes. The workspace uses the consolidated dashboard theme and lazy loading, with no earthtone illustration. The term-library link appears only with its own grant.

Editors now cover MSA terms, scope/deliverables/exclusions, reorderable scope and cost rows, USD amounts converted to exact integer cents, two-decimal quantities, distinct billing bases and fixed published component selections for packages. Draft saves, explicit publication, revision creation, named duplication and archival use the versioned API. Published/archive editors are read-only. Unsaved edits block publication/archive/copy; conflicts preserve local content, and explicit reload with discard confirmation works even when a failed save did not change the server edit version. Missing component options preserve saved IDs and catalog failures have a retry. The implementation separates library navigation, editor lifecycle, row controls and draft conversion.

Validation passed: seven route tests including independent template grants and protection against the generic agreement-ID route; Dashboard type check and production build; a Chrome test covering all four editors, save/publication/revision/duplicate/archive actions, both changed-version and unchanged-version reload recovery, exact 100.01 USD → 10001 cents and 1.25 quantity, scope reorder/removal, component failure/retry and exact package references, absent clause permission, removed template permission, and mobile width. Inspected the stable mobile screenshot after the responsive navigation transition. Existing bundle-size warnings remain. All records and browser responses were synthetic; no deployment or live legal content was changed.

Client composition drafts and estimate integration, supported placeholder resolution, rich-text/combined previews, Save as template review, customer review/PDF parity and complete approval/activation rehearsal remain unfinished. The library UI is not a completed agreement sales workflow, and the full consolidation goal/final GitHub push remain outstanding.

### Client agreement composition draft backend — 2026-09-17

Added `agreement_composition_draft` through additive migration 0030, with client/property/inquiry/source-estimate references, future prepared-estimate linkage, content/context/source snapshots, optimistic versions, and actor-scoped creation idempotency. Creation and edits are Sales-only; Agreements may read, while template-maintenance access alone does not grant client draft access. Scoped audit events accompany writes. Explicit context changes validate known client/property/inquiry relationships without onboarding or granting access. Inquiry-only drafts remain private with unresolved client fields.

Selections copy exact published template versions and give client scope/cost rows fresh IDs. Source archival/revisions and another client's edits cannot rewrite a saved draft. The plan's archived-version exclusion is enforced for new package selections as well as individual selections; a package whose components have been archived must be revised before creating new client drafts. Idempotent retries of already-created drafts still return the saved result after source archival.

Preview resolves supported client/property/date placeholders from the saved verified context, reports unresolved tokens, and performs single-pass text substitution. Exact integer arithmetic produces per-line half-up cents and separate billing-basis totals without implying a mixed-rate contract total. APIs/generated contracts cover bounded cursor listing, create/read/versioned edit and explicit context changes. Cursor ordering includes exact microsecond timestamps plus UUID tie-breakers. Prepared status requires a linked estimate; the preparation workflow itself remains forthcoming.

Validation passed: all 73 isolated Dashboard tests and migration replay. New coverage includes exact rounding/placeholder behavior, independent drafts for two clients, simultaneous creation retries, duplicate-operation conflicts, simultaneous edits, immutable source snapshots, archival exclusion through packages, saved retry recovery after archival, inquiry-only creation with no new organization/contact/property links, explicit later context attachment, mismatched inquiry/client/property denial, role boundaries, no estimate side effects, and pagination of records sharing microsecond timestamps. API, shared-library and Dashboard type checks passed. OpenAPI structural comparison confirmed no prior endpoint/schema was modified. Synthetic test containers were cleaned up; no production migration or real client data was used.

Remaining agreement work: native client draft editor and navigation from Sales/Agreements/inquiries, comparison and explicit template switching, Save as template review, rich-text/combined preview and placeholder send gates, idempotent estimate preparation, shared customer review/PDF snapshots, billing allocation and complete approval/activation rehearsal. Migration 0030 must precede deployment of these routes; rollback should retain private draft data and disable the new routes rather than drop the table. Full consolidation and final GitHub push remain outstanding.

### Native client agreement draft workspace — 2026-09-17

Added `/agreements/drafts` and stable `/agreements/drafts/:id` links under Revenue, with entry links from Sales, Agreements and each selected commercial inquiry. Sales can create and edit; Agreements grants read-only draft access; template-maintenance, Billing, crew and client access alone do not authorize this workspace. Route and server permissions remain separate checks. Account/capability changes remount the workspace and remove its local state.

Draft setup selects a published package, individual published MSA/scope/cost versions, or a blank draft. Active client/property references and recent inquiries use existing authorized endpoints; older inquiries can enter through the paginated Sales inbox link. Creation keeps the exact request and operation ID after an attempted save, freezes the setup and offers an identical retry if the response was lost. An explicit restart warns to check the saved list before creating another request. This in-memory retry protection does not claim persistence across a browser reload.

The editor reuses the template scope/cost controls and exact-cent conversion. It supports client-specific terms, dates, scope and exclusions, reordered/deleted cost rows, separate billing bases and notes. Source versions remain visible. Explicit context changes are version-checked, refresh verified placeholders and cannot silently overwrite unsaved content. Stale/error saves retain local edits; confirmed reload replaces them even if the server version did not change. The authoritative saved preview labels its version, excludes unsaved edits, shows unresolved placeholders, renders content as text and separates billing bases. No template content is mutated, proposal sent or operational agreement activated by these actions.

Validation passed: eight dashboard route tests; Chrome draft workflow covering catalog failure/retry, lost-response creation retry with identical key/body, inquiry context, quantity precision rejection before transport, CAS conflict, canceled/confirmed reload, exact cents, saved-preview separation, context refresh, read-only and revoked grants, and 390px mobile overflow/screenshot review. Existing template-library browser regression passed after extracting shared cost helpers. Dashboard typecheck and build passed; existing large-chunk advisories remain. Browser data was synthetic, with all API traffic intercepted. No production mutations or GitHub push occurred.

Still outstanding in the agreement workstream: explicit source switching/comparison, save-as-template with personal-data stripping and review, richer sanitized document formatting, complete context/estimate selection, estimate preparation and send gates, immutable customer/PDF parity, multi-basis allocation, and approval-to-activation rehearsal. The overall consolidation goal remains active; this is a local checkpoint, not a completed release candidate.

### Reviewed agreement template replacement — 2026-09-17

Implemented Sales-only review/apply endpoints and the native section comparison workflow. Staff choose a published package or individual sources, check the exact sections to replace, review current versus proposed content and explicitly confirm application. Scope/cost notes travel with their respective rows; unchecked content, dates and client/property context remain unchanged. Editing selection invalidates the comparison. Read-only users do not receive replacement actions, and the editor prevents concurrent context/content reloads while replacement is open.

The server resolves sources under transaction locks for both review and apply. Review performs no mutation. A consistency token binds the saved draft version, normalized selection, sections and exact source snapshots. Applying rechecks publication and rejects stale versions, changed sources or altered tokens; competing requests have one winner. New copied rows receive independent IDs. Applied sources append to source history without modifying earlier snapshots. The audit event records section/source/version provenance. No schema migration, template mutation, estimate creation, client message or operational activation occurs.

Validation passed: complete disposable Dashboard suite (74 tests, no skips) and migration replay, including unchanged review reads, partial replacement preservation, explicit capability denial, invalid sections/token rejection, source changes/archive between review and apply, concurrent/repeated apply behavior, independent drafts and audit count. Extended Chrome workflow passed for comparison, cancel confirmation, checkbox invalidation, failed apply/recompare, successful partial replacement and existing editing/access/mobile checks. API/Dashboard/shared-library typechecks and Dashboard build passed; existing large-chunk advisories remain. No production writes or GitHub push performed.

This completes explicit template switching for plain-text agreement drafts. Save-as-template with personal-data stripping/review, richer document formatting, complete estimate context selection, preparation/send gates, customer/PDF parity, pricing allocation and end-to-end sales/activation rehearsal remain outstanding within the unchanged full consolidation goal.

### Save client agreement components as reusable templates — 2026-09-17

Added the native Save as new template workflow for MSA, scope and cost components, reusing the existing typed template editor and unpublished creation endpoint. The preparation endpoint requires template-maintenance plus Sales-or-Agreements access and the exact source draft version. It performs no persistence, publication or source mutation. Prepared candidates omit related record IDs and source history, use generic names, and generate new scope/cost row IDs. Known saved/current client/property/date values become supported placeholders; inquiry details, linked organization/contact values, UUIDs and email addresses become review markers. Supported placeholders remain intact; unsupported placeholders are replaced so they cannot carry private values through the redaction step. Matching avoids ordinary word fragments and recognizes common separators in known phone numbers. Cost quantities reset to 1 and unit prices to zero; billing bases remain intact.

The editor requires acknowledgement that reusable text and defaults were reviewed, and clears that acknowledgement whenever content changes. It explains the limitation: matching known values cannot detect every client-specific instruction or negotiated term in arbitrary prose. Publication remains an explicit separate library action. Components can be reviewed/published individually and assembled into named agreement packages using the existing package editor. Client-specific titles, descriptions and package notes are not silently copied into new components.

Validation: final complete disposable Dashboard suite passed 75 tests with no skips, plus migration replay. Coverage includes pure redaction and source immutability, boundary-safe name matching, preserved supported placeholders, rejected private/unsupported placeholders, contact phone variants, exact default resets/new row IDs, linked organization/contact lookups, stale source versions, permission intersections, and no template/source mutation during preparation. A newly added contact fixture initially omitted its required source field; that fixture was corrected, and the complete suite reran successfully. Chrome agreement and template-library workflows passed, including hidden export for Sales-only users, quantity/price reset, mandatory review and invalidation after edits, exact edited cents, unpublished template save and unchanged agreement content. API/Dashboard/shared-library typechecks and Dashboard build passed, with the existing bundle-size advisories.

No production migrations, client messages, deployment or GitHub push occurred. Proposal preparation/send gates, richer document formatting, customer/PDF parity, pricing allocation and the sales-to-activation rehearsal remain required before the full consolidation goal is complete.

### Estimate PDF preservation and customer MSA parity — 2026-09-17

Tracing the retained proposal handoff exposed a concrete loss risk: the old PDF function emitted one page and placed long text beyond its boundary. It also omitted the saved MSA snapshot from both PDF and public review. Replaced that writer with a dedicated renderer that wraps/paginates text, embeds fonts, distinguishes scope/cost/MSA/additional terms, and numbers pages. Customer JSON/page now include the saved MSA snapshot and preserve plain-text line breaks. Approval completion no longer leaves a misleading loading message.

Visual QA exposed an additional deadline discrepancy (browser-local date versus PDF UTC date). Both views now use one shared formatter with an explicit UTC date and time. Send and legacy-send transitions preflight supported characters before status/outbox writes; unsupported glyphs produce a 422 code-point list rather than silently losing or altering contract text. This check also protects PDF downloads of existing records. The font repertoire is deliberately explicit, not a claim of universal script support.

Dependency decision: no PDF library existed in the API package. Added exact PDFKit 0.20.2 (MIT) and development types 0.17.6 to replace the hand-built PDF implementation. PDFKit's documented wrapping/pagination and embedded-font support address the observed defect without adding browser automation or a Python production runtime. Vendored unmodified Noto Sans regular/bold at a fixed upstream commit with SIL Open Font License and SHA-256 provenance. The build copies fonts into the Dashboard output, and a regeneration script derives the supported-code-point intersection from those fonts. No runtime font/network dependency or host-font assumption is introduced. References: https://pdfkit.org/docs/text.html and the source/license links recorded with the font assets.

Validation evidence: a synthetic 120-section scope plus 90-line MSA produced 12 pages; text extraction found the final scope/MSA/additional-term markers, exact amount and accented names. All page text bounds were checked and the rendered contact sheet and cost/terms transition page visually inspected. No clipping, overlap or missing page numbers was observed. The complete disposable Dashboard suite passed 77 tests with no skips and migration replay. New tests cover pagination, embedded font/Unicode maps, supported accents and unsupported-glyph rejection. Integration checks verify the public MSA snapshot, PDF download, unchanged approval handoff, and rejection before creating recipients or outbox messages. Chrome checks cover literal MSA text/newlines, additional terms, consistent expiry, mobile layout and completed approval state. API/Dashboard/shared-library typechecks and both builds passed; existing Dashboard bundle-size advisories remain.

This is a prerequisite correction, not completion of composed-agreement delivery. Draft preparation, immutable customer/property/document snapshots, pricing allocation, revision compatibility and sales-to-activation rehearsal remain outstanding in the full goal. All data was synthetic/local; no production release or GitHub push occurred.

### Issued estimate document snapshots — 2026-09-17

Added the additive estimate snapshot migration and shared document reader. The database captures document content, party labels/address, ordered price lines, terms/MSA, template references, recurring configuration and expiry on the first draft-to-sent transition. Capture is in the same transaction as send; PDF character validation then checks that exact captured content, and failure rolls back status/snapshot before any recipient/outbox creation. Public JSON/PDF and office document reads use the frozen content while retaining live approval/status/current-revision state. Customer review now includes the captured address and revision label.

Protection triggers reject subsequent content/snapshot changes, deletion, return to draft, and line insertion/update/deletion. Line mutations lock their parent estimate(s), serializing them with the send row lock. A revised estimate is a new draft row without a snapshot, retaining prior content/history and capturing independently on its own send. Older issued records remain explicitly unsnapshotted: current names cannot reconstruct what was historically issued. The office reader labels that state `legacy_live`, and a read-only SQL inventory is included for the authorized reconciliation rehearsal. No historical metadata was fabricated or backfilled.

Validation passed: complete disposable Dashboard suite (77 tests, no skips) plus migration replay; client/property rename preservation; direct immutable-field/line/delete rejection; unchanged approval conversion; old-link invalidation/new-revision capture; legacy fallback labeling; unsupported-character send rollback. Two deterministic PostgreSQL race checks observed the competing backend blocked on the parent estimate: a line edit committed before send appears in the snapshot, and an insertion queued after send is rejected. Browser checks passed for the customer revision/address display and existing MSA/expiry/approval behavior. API/Dashboard/shared-library typechecks and both builds passed, with existing Dashboard chunk advisories.

Deployment/rollback ordering and the legacy evidence limitation are documented in the Dashboard API guide. No production migration, customer message, deployment or GitHub push occurred. The goal remains active: composed-draft preparation, multi-basis pricing allocation, richer formatting, full sales/activation integration, and the broader CMS/CRM retirement work are still outstanding.


### Mixed-basis agreement pricing review — 2026-09-17

Traced the existing estimate-to-approval path and confirmed that it creates one work order or recurring service and uses one global estimate cap. The consolidation requires explicit component allocations beneath that cap before multiple operational records can be generated safely. Added a shared financial-review contract/calculator, version-checked Sales-only API, generated client and native draft-editor review. No schema change or operational writer was introduced at this stage.

The reviewer binds every saved cost row to its billing basis and requires explicit scope coverage. One-time charges use exact rounded cents; monthly charges require a complete calendar-period plan using existing service-agreement validation; partial periods and deviations from the listed monthly rate need a written review reason. Per-visit charges use an explicit bounded visit allowance and finite term. The overall proposed authorization sums these finite amounts, preserving the existing estimate ceiling. It does not imply that schedules have been created or that recipients/context are eligible. Inputs are transient, clearly labeled, discarded on close, and never presented as a prepared or approved agreement. Any edit invalidates the visible result; stale-version failures retain review inputs.

Validation: 83 disposable Dashboard tests passed without skips, plus migration replay. Six new calculation/contract tests cover exact mixed pricing, partial/leap months, explicit exceptions, term coverage, missing/unknown/shared scope, unresolved placeholders, bounded visit counts and individual/combined ceilings. API integration verifies Sales-only access, stale rejection and no draft/estimate mutation. Browser pricing checks passed for exact request cents, partial/leap periods, blockers, invalidation, stale-input retention, mobile and read-only access; the existing agreement-draft browser suite also passed. API/Dashboard/shared-library typechecks and both builds passed, with existing Dashboard chunk advisories.

Next required integration remains allocation persistence, transactional preparation, immutable composed document rendering, approval fan-out, allocation-specific billing caps alongside the global cap, and revision/activation rehearsal. Broader CMS/CRM migration and retirement scope remains active. No production data changes, customer messages, release or GitHub push occurred.

### Persisted private pricing plans — 2026-09-17

Extended the reviewed allocation calculation with an additive nullable pricing-plan column, version-bound shape constraint, invalidation trigger and Sales-only save action. Saving recalculates against the locked current draft, rejects blockers or stale versions, increments the draft version and audits the proposed limits. It preserves the existing private draft state without creating estimates, operational records or notifications. Subsequent title/content/dates/context/template-history changes clear the plan at the database boundary, including writes through older handlers; a version change cannot retain an unmodified old plan.

The editor reopens saved scope selections, monthly charges/explanations and visit allowance, shows saved proposed limits to Agreements readers, and requires a fresh calculation after edits. Saving recomputes server-side rather than trusting the browser result. Failed saves retain inputs and invalidate the result so an outdated review cannot be resubmitted blindly. API/migration deployment and rollback behavior are documented; legacy data remains untouched with a null plan.

Final validation passed: 83 disposable Dashboard tests with no skips and migration replay; capability intersection, blocked-save rejection, concurrent-save and save-versus-edit races, exact persisted version/amount, audit, direct stale-version rejection, context/title/cost invalidation and recalculation after cost changes. Two test-fixture issues were corrected during validation (a duplicate local variable and a pagination expectation after adding another draft); the full suite subsequently passed, including the added direct-cost-change regression. Chrome pricing tests passed save/reopen with exact amounts and reasons, along with conflict retention/mobile/read-only checks; the existing agreement-draft workflow passed. API/Dashboard/shared-library typechecks and builds passed, retaining existing chunk-size advisories.

This completes private pricing-plan persistence only. Transactional preparation, immutable composed proposal/recipient rendering, operational conversion and billing allocation enforcement remain outstanding. The complete consolidation goal stays active; no production migration, deployment, customer message or GitHub push occurred.

### Operational allocation and billing boundary — 2026-09-17

Implemented the shared estimate-allocation boundary required by mixed agreements. The customer-facing estimate remains the single approval and overall financial authorization. Its one-time/monthly/per-visit allocations carry independent identities, scoped work, ceilings, source-row references and recurring configuration. Issuance reconciles the sum and freezes the allocation rows in the same document snapshot. Historical estimates remain unallocated. The recurrence unique indexes now preserve the existing legacy rule while allowing one recurring service per explicit component.

Composite references and database guards bind jobs, recurrences, service agreements and charges to the approved property/estimate/basis, preserve issued allocation content and prevent reassignment of allocated authorizations. Every allocated billing writer locks the parent estimate and checks both component and overall totals; failed posting drafts still consume capacity. A shared API helper applies the same calculation to manual billing, project-phase intents and recurring agreement charges. Manual/phase inputs now accept an explicit allocation ID and reject omission for allocated estimates. Existing unallocated paths retain their prior behavior.

Recurring agreement creation selects the approved allocation for its billing basis and records its scope/identity. Creation, draft editing and activation reject dates, rates or monthly charges that differ from the issued configuration, even when an altered amount would fit the cap. Activation also checks reviewed recurrence cadence/interval/time. Charge previews use component totals with remaining capacity bounded by the overall estimate, and prepared monthly/per-visit charges retain allocation identity and their existing idempotent receipts. A new estimate can authorize a retained recurrence for renewal without overwriting its original provenance; complete composed renewal/change-order and scheduling rehearsal remains outstanding.

Validation: full disposable Dashboard suite passed 88 tests with no skips and migration replay. Five new integration cases cover issued sum reconciliation/snapshot protection, missing/foreign/wrong-basis references, separate monthly and per-visit recurrence identities, direct insert/update/reparent billing rejection, failed-posting consumption, and a deterministic two-connection overrun race observed via PostgreSQL blocking state. Service tests verify allocation-specific scope/capacity, rejection of changed monthly/visit rates, activation, exact charge linkage and idempotent replay. HTTP tests verify the manual and phase writers require allocation selection, share the cap and retain retry behavior. Shared-library/API/Dashboard typechecks and the API build passed. No frontend implementation changed in this checkpoint; native billing selectors are still required.

Deployment and rollback ordering are documented in the API guide. This migration/schema foundation does not itself expose composition preparation or send: transactional preparation, complete composed customer/PDF rendering, approval conversion, manual billing selectors, revision/change-order handling and the complete operational rehearsal remain pending. Broader Marketing/CRM migration and retirement work also remains active. No production migration, deployment, customer message or GitHub push occurred.

### Native billing allocation selection and retry recovery — 2026-09-17

Added a Billing-only capacity endpoint and shared native allocation picker in the general billing dialog and project-phase form. The endpoint returns only current approved/billed/remaining amounts and component identifiers/labels; Sales access alone does not expose billed totals. It holds the parent authorization lock while calculating a consistent snapshot, and every writer still rechecks capacity. The UI blocks preparation during loading/error states, offers retry, requires an explicit component for allocated estimates, disables exhausted choices and shows the selected component balance separately. Unallocated historical estimates retain their ordinary workflow. Late responses cannot replace choices for a newly selected estimate.

Project switching now clears prior billing context and ignores superseded phase-list responses. Work on this path exposed an existing retry defect: the phase form generated a new operation ID after every failed attempt. It now retains the exact submitted request, fixes its details while unresolved, blocks project switching and guards navigation until the request is retried or explicitly replaced after checking existing drafts. A lost-response retry reuses the original ID/body, and success clears the old error and closes the form. State remains in memory; hard-reload recovery still requires checking existing drafts. The backend idempotency record remains authoritative.

Validation passed: full disposable Dashboard suite (88 tests, no skips) and migration replay; endpoint field minimization, Billing-only access, rejection of unapproved estimates, legacy empty-allocation responses and updated remaining capacity after charges. Chrome covered unavailable/loading gates, explicit selection, disabled exhausted options, exact request cents/allocation identity, conflict retention, general-billing operation-ID reuse, stale response exclusion, historical estimates, project switching and an aborted-response phase retry with byte-equivalent request data. Desktop and settled mobile layouts were visually inspected; form controls were readable and no horizontal overflow was found. Shared/API/Dashboard typechecks and both builds passed, with existing Dashboard chunk advisories. Browser checks synchronized the mobile drawer transition and used actual native option disabled state rather than assuming a screenshot taken during animation represented the settled layout.

Transactional composed-proposal preparation, public/PDF rendering, approval conversion, revision/change-order compatibility and the full sales-to-activation rehearsal remain required, along with the broader consolidation scope. No production migration, deployment, customer communication or GitHub push occurred.

### Internal composed proposal preparation — 2026-09-17

Added an internal preparation service and additive migration `0034_composed_estimate_preparation.sql`. A current saved pricing plan is recomputed from its source rows, then the service validates active client/property context, optional project membership, exact recurring schedule coverage and first visits within the reviewed term. Current party details must match the reviewed snapshot; changed context requires explicit refresh and repricing. Every customer-visible source string receives PDF font validation before persistence. Original per-basis rates stay separate from finite authorized component amounts; monthly review reasons remain outside operational configuration.

Preparation locks the composition and creates one composed estimate, its allocations and every original cost line in one transaction. The final composition snapshot freezes party details, rendered content, dates, template provenance, original pricing review and component scope/configuration. The source draft links that estimate and retains its pricing review at the new version. Normalized request fingerprints recover concurrent and lost-response retries without duplicate estimates or audit events; fingerprints are omitted from draft responses. Preparation itself creates no jobs, recurrences, service agreements, recipients or billing drafts.

Database guards freeze prepared content, cost lines and allocations. Deferred validation prevents incomplete preparation from committing, requires component caps to equal the proposal maximum, and requires exactly the reviewed cost rows for each component. Historical one-time and recurring estimates retain their existing paths. No historical composition snapshots are invented.

Validation: 93 Dashboard tests passed with no skips and migration replay in disposable PostgreSQL. New pure and integration tests cover exact mixed-basis amounts, internal review-note separation, schedule boundaries, stale pricing/context, authorization, simultaneous identical retries, changed-request conflicts, unchanged operational tables, unsupported PDF text rollback, immutable content and rejection of incomplete seals/allocations. API and shared-library typechecks passed.

This service is deliberately not mounted as an HTTP endpoint or exposed by a preparation button yet. Composed customer/PDF rendering, multi-component approval conversion, revisions/change orders and end-to-end activation must be integrated before users can create composed estimates. The broader consolidation goal remains active. No production migration, deployment, customer communication or GitHub push occurred.

Additional regression: the isolated service-agreement suite passed all eight lifecycle/charge/schema cases, the durable charge-preparation worker case and the real HTTP authorization case, including migration replay. Its initial schema-parity failure identified an accidentally added ORM field on `service_agreement`; that declaration was corrected before the passing rerun. The API build passed.

### Composed customer documents and PDF parity — 2026-09-17

Added an explicit customer projection from the immutable preparation snapshot and a shared ordered formatter consumed by both the public review screen and PDF. Each component retains its allocated scope, original quantity/unit/rate and row total, billing basis, aggregate rate, authorization ceiling, monthly charge periods or maximum visit allowance, and reviewed service schedule with America/New_York time. Shared notes, exclusions, package text, full MSA and total authorized maximum remain visible. Prepared dates and the proposal validity instant are distinct. Literal HTML remains text and long paragraphs preserve line breaks.

The projection omits source/template/actor identifiers, internal pricing review reasons and retry metadata. It validates row coverage, component uniqueness and monetary reconciliation instead of silently dropping missing rows. Existing issued document consumers now read composed party labels from the frozen preparation snapshot. Migration `0035_composed_estimate_issuance.sql` runs after normal document/allocation capture, rechecks active operational property/client ownership and stores the same frozen party labels in the issued document. A failed eligibility check rolls back issuance. Client-portal reads also require access to the original agreement client, preventing a new property client from gaining access to the former client's proposal.

Validation includes real database and HTTP tests for customer projection/privacy, original reviewed notes being absent from customer data, malformed row/rate rejection, screen/PDF block parity, archived client/property or changed ownership blocking issuance, frozen party names after display-name edits, public JSON/PDF and portal access after property reassignment. Browser tests exercise all component prices/periods/limits, escaped text, terms, PDF link, desktop/mobile rendering and the existing noncomposed review path. A synthetic three-page PDF and an eleven-page long agreement were text/bounds checked; all 240 long-form paragraphs and the final terms marker were retained. Visual inspection found a trailing page containing only the validity date, corrected by placing validity in the shared header area; the final long document has no such extra page.

The composed preparation endpoint remains unmounted until multi-component approval conversion and revision/change-order integration are complete. This is continuing implementation, not a completed consolidation or release candidate. No production migration, customer communication, deployment or GitHub push occurred.

Final validation for this checkpoint passed: all 93 Dashboard tests with no skips and migration replay, shared-library/API/Dashboard typechecks, API and Dashboard builds, and both composed/legacy customer browser scripts. The Dashboard build retains its existing large-chunk advisory. The shared-library declarations were rebuilt before the API typecheck; a test response was explicitly typed after an initial unknown-response typing error. No failed check is being treated as a pass.

### Composed approval conversion — 2026-09-17

Composed estimate decisions now branch into an explicit multi-component converter instead of the legacy one-time fallback. The decision transaction locks the estimate, validates current original-client/property eligibility and the deciding contact or portal client, reconciles persisted allocations with the frozen customer document, records approval and creates all authorized operational components. One-time work is an unscheduled job; each monthly/per-visit component creates its own paused recurrence and draft service agreement, preserving scoped work, allocation identity, exact first-visit date/cadence/time, reviewed rates/periods, accepted MSA and contact attribution. The overall and per-component billing boundaries remain unchanged. Approval does not activate recurrences, generate visits or create billing drafts.

Component creation and approval audits share the transaction. Existing decision-state locking admits only one winning approval; repeats do not create further records. The response adds component identifiers alongside the compatible primary `jobId`/`recurring` fields. Notifications distinguish recurring activation work from readiness for dispatch. Composed decline and approval both revalidate original-client decision authority, expiry and current revision; a property transfer or removed contact cannot approve the old client's agreement through a remaining link.

Disposable integration tests cover full mixed, one-time-only and recurring-only proposals; simultaneous public approvals; revoked repeat links; original-client access and archived contact rejection; a forced final-audit failure rolling back approval, all jobs/recurrences/agreements and recipient decision; exact scope/allocation identity, first-visit anchors and rates; accepted terms/contact attribution; and absence of billing/automatic activation effects. Full Dashboard validation passed 93 tests with no skips and migration replay. No frontend, production data or deployment changes were required for this converter.

Preparation remains internal while native preparation controls, composed revision/change-order workflows and full activation/scheduling/billing rehearsal are completed. In particular, recurring visit generation must honor the reviewed finite visit allowance, not only the financial cap. Broader consolidation and retirement work remains active. No production migration, customer communication or GitHub push occurred.

Additional validation passed for this conversion checkpoint: API typecheck and build; isolated service-agreement schema/lifecycle/charge regressions, durable charge-preparation worker and real HTTP authorization test, with migration replay and no live providers. Existing legacy one-time and recurring approval tests remain green in the Dashboard suite.

### Recurring visit authorization and finite allowances — 2026-09-17

Migration `0036_recurring_visit_authorization.sql` adds nullable `work_order.service_agreement_id` and an allowance index. Allocated visit generation now resolves the governing agreement by occurrence date, including retained legacy recurrences with newer allocated agreements. Effective legacy agreements retain their original behavior within their covered period. Allocated work must fall within the reviewed term, first-visit boundary and cancellation cutoff, with the approved cadence/interval/time and current operational client/property context. Ambiguous overlapping allocated agreements fail closed. Generated scope comes from the governing agreement snapshot.

The worker filters ineligible/exhausted services before the candidate limit, locks selected recurrences and rechecks capacity before inserting. Database triggers also enforce manual inserts, attachment of previously unlinked jobs and reactivation of cancelled/skipped visits, serializing on the recurrence. New records receive exact agreement provenance; existing linked provenance cannot be changed. Scheduled-date changes must remain within the agreement's effective term. Completion of already-authorized work remains possible after cancellation or renewal without reopening authorization.

Per-visit capacity counts reserved/active/completed work and any visit with a charge. Cancelled/skipped uncharged work can release its slot; charged work continues to consume it. Historical unlinked visits in the agreement term, or without an occurrence date, conservatively consume capacity until reviewed. This does not invent a historical agreement assignment. No legacy records are backfilled.

New isolated PostgreSQL tests exercise concurrent workers, exact finite generation, scoped work/provenance, released versus charged slots, blocked reactivation and unlinked-job attachment, forbidden date/provenance changes, out-of-term and cancelled work, changed recurrence configuration, retained legacy identities, conservative historical handling and a deterministic two-connection race for the final slot. The latter verifies the second writer blocks on the first and then rejects after the first commits. The service-agreement suite includes these tests after its existing lifecycle and charge-worker cases.

During validation, an unsupported SQL time-format call was corrected to compare the stored local time directly. The Dashboard harness now reports both stdout and stderr so a migration failure cannot hide behind successful earlier migration output. A test initially tried to bill a future visit; the existing billing guard correctly rejected it, and the fixture was corrected to use today's reviewed visit. A temporary syntax error while replacing the worker's scope lookup was fixed before rerunning the suite. Native allowance visibility, complete activation/renewal/scheduling rehearsal and the other consolidation requirements remain outstanding. No production migration, deployment, external communication or GitHub push occurred.

Final validation for this allowance checkpoint passed: 93 Dashboard tests with no skips and migration replay; eight existing agreement lifecycle/charge/schema tests, the durable charge worker, two new visit-authorization tests and the HTTP authorization test; shared-library/API typechecks and API build. All databases and records were disposable/synthetic. No UI was changed in this checkpoint.

### Operational allowance visibility — 2026-09-17

The existing Operations recurring-services endpoint and table now expose generation status, visit allowance, reserved visits and remaining visits for the next occurrence. Status distinguishes paused/awaiting activation, legacy schedules, missing effective agreements, exhausted allowances, authorization review and an authorized next visit. Counts use the same reservation/charge and historical-visit rules as generation; this is a read snapshot, not a reservation. Monthly and legacy services without a per-visit allowance return null counts rather than a misleading zero. No financial amounts or private allocation configuration are added to the response.

Status and remaining count appear beside the service title, ahead of secondary table columns, so mobile users can find the actionable information first. The table retains horizontal scrolling for other columns and explains when cancelled/skipped work releases capacity. OpenAPI and the generated client model include the additive fields; the existing Operations Recurring capability gate is unchanged.

Validation passed: real database assertions for authorized/exhausted states, exact counts, immediate release of an uncharged cancelled slot and omission of financial/configuration fields; the full isolated agreement suite; shared/API/Dashboard typechecks; API and Dashboard builds; and desktop/mobile browser tests with exact exhausted/available/legacy states. Visual inspection prompted the column reorder. Existing Dashboard bundle-size advisories remain. Full preparation/activation, revision/change-order work and broader consolidation requirements remain active; no production migration, deployment or GitHub push occurred.

The full Dashboard suite also passed all 93 tests with no skips and migration replay after the endpoint change. Browser checks passed again after the column reorder, and the Dashboard build was repeated successfully for that final layout.

### Internal composed proposal revisions — 2026-09-17

Added additive revision lineage fields in migration `0037_composed_revisions.sql` and an internal Sales-only revision-draft service. Creation is idempotent under the same actor/operation lock namespace as ordinary composition creation. It copies the frozen client-specific proposal, dates and exact template history into independent editable scope/cost rows, with no pricing plan. When the original prepared draft still renders exactly to the frozen document, its editable placeholder source and inquiry linkage are preserved; otherwise the frozen rendered content is the recovery source. Creating a revision draft does not supersede the live proposal.

Preparation rechecks and locks the source estimate before context reads can acquire shared locks on it. It requires the expected current revision, original client/property and an unapproved source. Once a fresh pricing review, context and PDF checks pass, the replacement inherits the series, advances the revision, retains project/request/change-order lineage and atomically makes the previous version noncurrent. Competing replacements cannot both become current. If approval wins first, revision preparation rejects and requires a change order. The old immutable document remains intact. Existing flat revision/change-order endpoints now reject composed agreements explicitly instead of dropping their structured content.

The revision creator and composed preparation remain internal while the native workflow and structured change-order behavior are completed. Validation covers identical concurrent draft creation, fresh row identities and pricing reset, preserved placeholder content, original availability while editing, preparation blocked before repricing, competing replacements with one winner, original snapshot preservation, series/revision continuity and rejection of approved sources. A template-history serialization issue found by the test was corrected using explicit JSON serialization. This is not completion of the full revision/change-order or consolidation scope, and no production migration, deployment or GitHub push occurred.

Final validation passed for this internal revision checkpoint: 93 Dashboard tests with no skips and migration replay, shared-library/API typechecks and API build. The final test run explicitly verifies independent scope/cost row identities as well as concurrent supersession behavior. No frontend changes were made.

### Internal structured additional-work change orders — 2026-09-17

Added migration `0038_composed_change_orders.sql` and an internal Sales-only creator for additional-work drafts against current approved composed agreements. This follows the existing additive change-order model rather than replacing accepted recurring commitments. The draft inherits reviewed MSA/template provenance and verified inquiry linkage, but scope, costs, notes and dates start empty for explicit new review. Retry-safe creation shares the ordinary composition operation lock and does not alter the base agreement. Editable placeholder terms are retained when the original prepared source still matches its immutable document.

Preparation validates the approved parent and original client/property, preserves default project context and creates a separate estimate series linked through `change_order_for`. It neither supersedes the base nor copies its charges. The immutable snapshot and minimized customer projection identify the original agreement title/revision; the common HTML/PDF formatter labels this as additional work. Revisions of an unapproved change order preserve its parent relationship. Existing cancellation and successor-agreement workflows remain necessary for replacing or reducing already-active recurring commitments.

Validation passed: 93 Dashboard tests with no skips and migration replay, shared/API/Dashboard typechecks, both builds, and customer browser checks including the additional-work heading. New integration assertions cover concurrent/idempotent draft creation, stale-parent rejection, empty work/prices/dates, pricing-review requirement, distinct series/revision and parent linkage, an unchanged base amount/current status, private parent-ID omission from the customer projection, and separate customer approval creating only the additional job. Native composition preparation/revision/change-order controls and the full operational rehearsal remain pending. No production migration, deployment, customer communication or GitHub push occurred.

### Native composed proposal preparation and related drafts

The dashboard now exposes saved, reviewed agreement drafts through **Prepare proposal**. Staff select proposal validity and an explicit first visit, cadence, interval and Eastern local time for each recurring allocation. Preparation seals the proposal and links to its Sales card; sending remains a separate existing Sales action. An uncertain preparation response retains the exact request and freezes its inputs for retry or a draft-status reload. Sales offers composed revision drafts for unapproved proposals and separate additional-work drafts for approved proposals. Legacy flat revision/change-order forms remain limited to legacy proposals.

Three Sales-authorized HTTP endpoints now mount the previously internal preparation/revision/change-order services. OpenAPI and generated clients include their request/result contracts. Earlier entries describing these services as unmounted are historical. Full workflow rehearsal, remaining consolidation parity and release readiness are still pending; this checkpoint is not a production cutover or a completed consolidation.

Validation: API and dashboard type checks passed. The preparation browser fixture verified required first dates, independent recurring schedules, lost-response exact retry, frozen choices and the prepared Sales link. The dashboard integration suite passed all 93 tests and migration replay, including explicit HTTP denial checks for agreement readers, template managers and client accounts on the three new routes.

### Approved recurring handoff and mixed billing rehearsal

The Operations activation handoff now uses the shared agreement/property/estimate/recurrence lock order and validates the submitted first visit and time against the approved allocation before changing either record. It rejects dates outside the accepted term. The dashboard prefills first visit/time and makes them read-only for allocated proposals. The transaction lives in `recurring-job-activation.ts`; the route delegates validation and mutation to that service.

The composed integration fixture now exercises both allocated recurring components through the actual activation HTTP endpoint, checks rejected schedule changes leave the agreement draft/recurrence paused, and checks repeat activation is rejected. A second, current-date mixed proposal runs through HTTP preparation, outbox-only sending, public client approval, Operations activation, the recurrence generator and retry-safe billing services. It verifies immutable agreement provenance on generated visits, separate monthly/per-visit billing allocations, a review prerequisite for visit billing, exact repeat-charge receipts and no generation beyond the finite term. Crew execution/review is explicitly simulated in this fixture; this is not evidence of a completed full-browser crew workflow or external invoice posting.

Validation: the 93-test dashboard suite and migration replay passed with the mixed handoff rehearsal. Browser checks verified approved schedule values are prefilled/read-only, alongside recurring allowance desktop/mobile checks. API and dashboard type checks passed. The broader consolidation and full release validation remain incomplete.

### Complete account access-history traversal

User Manager access history now has an Owner-only paginated API and a **Load older access history** control, replacing the silent 100-event cutoff. The cursor includes the account identity and exact microsecond timestamp plus event ID; another account's cursor is rejected. Older pages append without discarding the current list. Request failures preserve loaded history and the retry cursor. A fresh history request resets the view, and an empty history is explicit.

Validation: the disposable dashboard suite passed all 93 tests and migration replay. Its Owner HTTP fixture traversed 107 events with equal and microsecond-separated timestamps, inserted a newer event between pages, verified no omissions/duplicates, refreshed to see the new event, and rejected malformed/cross-account cursors, excessive page limits and non-Owner reads. The User Manager browser regression passed pagination, failed-page retry, retained rows, existing edit-conflict/invitation/notification behavior and mobile containment. API/dashboard type checks and the dashboard build passed. Invitation pagination and other User Manager migration/reconciliation requirements remain open.

### Complete invitation-history traversal

User Manager now pages through all invitations instead of silently stopping at 200. The Owner can append older pending/expired/accepted/revoked records, retain the existing rows and cursor after a failed request, and retry. Existing resend/revoke actions still refresh the list from its newest page. Responses explicitly project invitation administration fields and never return token hashes. OpenAPI/generated clients include the bounded page size and opaque timestamp/ID cursor.

Validation: all 93 dashboard tests and migration replay passed, including traversal beyond 200 invitations, equal/microsecond-separated timestamps, all historical lifecycle states, token omission, Owner-only access and malformed/excessive query rejection. User Manager browser checks passed loaded-row preservation and retry after a failed invitation page along with access-history, edit-conflict, notification, invitation creation and mobile coverage. API/dashboard type checks and the dashboard build passed (existing chunk-size warning). Account linkage and migration reconciliation remain separate open requirements; no production changes occurred.

### Read-only identity reconciliation tooling

Added `scripts/consolidation/reconcile-identities.mjs` and the operator guide `docs/implementation/identity-reconciliation.md`. The tool accepts minimal Core admin/editor, canonical dashboard and identity-link snapshots, retains revoked-link history, reports duplicate/dangling/multiple/missing links and profile/access-review gaps, and offers unique email matches only as candidates requiring dual-account proof. It writes a private, exclusive-create JSON report with input digest and no email addresses; it never mutates identity or access state. Existing recorded ID links remain authoritative even when emails differ.

Validation: four Node tests passed covering matching versus authoritative links, duplicate and revoked/dangling links, missing/ineligible profiles, unreviewed grants, unknown/credential-field rejection, deterministic findings and private no-overwrite output. This is tested reconciliation tooling, not an executed production inventory. Core suspension, MFA, detailed grant parity, client scope and actual approved account linking remain separate validation/migration requirements.

### Retained Careers service bridge

The dashboard CMS allowlist now includes Careers jobs, applications/status notes, bounded resume downloads and Owner-only settings. Core mounts the retained Careers handlers behind the existing Careers feature flag and the dedicated `marketing.content.careers` grant, independently of general CMS enablement. Settings require an active attested Owner, including case-insensitive route variants. Public application submission is not exposed through this administrative bridge. Careers settings retain redacted reads. Saving now omits blank/whitespace credential fields and uses one atomic settings batch, preserving stored credentials during ordinary edits rather than writing redacted blanks back.

The retained job handler now derives `createdBy`/`updatedBy` from the linked actor, ignoring forged form values and preserving original authorship on edits. OpenAPI and generated clients describe Careers records, writes, application notes, resumes and settings. No career records or feature flags were changed in production.

Validation: 37 focused Core tests passed, including Careers capability isolation, disabled-feature behavior, Owner settings access, trusted authorship, redacted credential round trips, explicit secret replacement and write-failure behavior. All 93 dashboard tests and migration replay passed with allowlist coverage. Core/API/dashboard type checks passed. Native Careers screens, save-conflict handling, fuller recruiting workflow parity and final module verification remain outstanding.

### Native Careers job workspace

Marketing → Content → Careers now opens a native job list/editor under its dedicated leaf grant. It supports loaded-list search, draft creation, job metadata, rich description/requirements/benefits/application instructions, status/visibility/employment/work-mode choices, salary fields, publication/closing dates and SEO fields. Existing directory references and provider metadata survive edits. The Careers feature-disabled response clears stale records and prevents creation.

Job updates accept `expectedUpdatedAt` and use a conditional database update; each write advances the timestamp by at least one millisecond to keep successive versions distinguishable through JSON timestamps. A stale result returns 409 and does not dispatch a job-update webhook. Native saves send the loaded version, preserve local edits on failure and offer explicit discard/reload. Uncertain creation results disable repeated creation and direct the user to refresh the list before creating another job. The native form does not yet include application review, directory reference selection, deletion, integration settings or an idempotent creation protocol; those are still required parity work.

Validation: all 93 dashboard tests and migration replay passed; 36 Core bridge tests passed, including expected-version propagation, stale-result conflict and invalid-version rejection. Core and dashboard type checks passed. The Careers browser fixture passed grant gating, failed-save retention/reload, source metadata preservation, mobile editor containment, uncertain creation and disabled-feature behavior. Mobile output was inspected. Dashboard build passed with existing chunk-size warnings. The opt-in `P1_CAREER_CONCURRENCY_TEST=true npx vitest run server/storage/career-concurrency.integration.test.ts` fixture passed against disposable PostgreSQL 17 using the real CareerStorage and replayed Careers migration. It proves one winner for concurrent writes, rejection of stale JSON timestamps, monotonic serial versions and conflict after legacy writes. The first rehearsal found a local-time parameter mismatch; explicit UTC timestamp comparison corrected it before the passing rerun.

### Native Careers application review

Careers now includes a native Applications view with loaded-record search/status filtering, applicant/job context, literal cover-letter text, safe external links, the protected resume download endpoint, review status and note editing, and chronological status/note history. Versioned review saves retain local choices after errors; explicit reload clears the local draft only after fetching saved status/history, including recovery when the original save response was lost.

The retained application handler now delegates status-plus-note persistence to one locked transaction. Stale versions conflict before mutation; note insertion failures roll back status/version changes. Note-only writes also advance the version, preventing duplicate notes from a repeated versioned request. Generic application updates advance the same timestamp monotonically, so changes from existing consumers invalidate old review forms. The new job-update Drizzle field types were narrowed to the existing database insert type after Core typechecking exposed a string-enum mismatch.

Validation: the opt-in disposable PostgreSQL fixture passed competing review saves, trusted note authorship, status/history correspondence, injected note-write rollback, note-only duplicate prevention and invalidation after a legacy application update, along with the prior job-save cases and migration replay. All 93 dashboard tests and 37 Core bridge tests passed. Application browser checks passed unsafe-link rejection, literal applicant text, resume endpoint linkage, stale-review retention, lost-response reload recovery and mobile containment; screenshots were inspected. Core/dashboard type checks and dashboard build passed. Actual resume file delivery, applicant intake/public lifecycle parity, directory selectors, Owner settings and full Careers release validation remain open.

### Careers local resume storage boundary

The retained resume loader now reads through a dedicated asynchronous file helper. It checks path boundaries and resolved symlink targets, accepts only regular files, closes file handles reliably and preserves the current-private then legacy-upload lookup order. Nested legacy names, Unicode filenames and configured symlink storage roots remain supported. Missing files return the existing unavailable result; unexpected storage errors still propagate. The R2 path and public upload limits are unchanged.

Validation: 42 focused Core tests passed, including three real-filesystem cases for binary fidelity, nested/Unicode filenames, configured symlink roots, neighboring-prefix traversal, absolute paths, escaping file/directory symlinks, non-file targets and missing/broken storage. Core typechecking and build passed (existing PostCSS/chunk-size warnings). Full HTTP/browser resume delivery and download failure feedback remain open; this checkpoint does not constitute complete Careers parity or production validation.

### Careers resume download delivery and recovery

The native application review now downloads resumes through the generated authenticated client, creates a temporary browser download with the original display filename (path/control characters removed), and reports missing-file, access and transport failures in place. Review status and note state remain independent. Concurrent clicks are gated, requests time out after 30 seconds, unmount cancels pending requests, and temporary object URLs are released. Invalid/empty responses do not become downloads.

Core now uses the existing Express attachment encoder for legacy filenames and returns private, no-store octet-stream attachments with nosniff. The dashboard gateway retains its bounded binary handling and fresh capability check; provider response headers are not forwarded. No storage records, permissions, provider credentials or database schemas changed.

Validation: all 94 dashboard tests and migration replay passed, including resume transport byte fidelity, response-size rejection and missing-file behavior. The 38 Core bridge tests plus three real-filesystem resume tests passed; the new actual-handler HTTP case verifies permission denial, binary bytes, encoded Unicode/control-character filenames, attachment/cache/nosniff headers and missing records/files. The browser fixture downloaded and read the exact synthetic file bytes, verified its filename, and recovered from missing-file, network and access failures while retaining unsaved notes. Existing review-conflict and lost-response checks passed; mobile output was inspected. Core/dashboard type checks and both builds passed (existing PostCSS/chunk-size warnings). These are separate local handler/transport/browser tests with synthetic upstream data, not a live R2/production end-to-end download rehearsal. Careers settings, directory selection, remaining intake/lifecycle parity and the broader consolidation release gates remain outstanding.

### Native Owner Careers settings

Marketing → Content → Careers now exposes an Owner-only settings screen for all retained sharing toggles, integration switches (including the previously unexposed Indeed disposition setting), partner ID, webhook URL and credential replacement fields. It identifies these as website settings and exposes the public Indeed feed path for provider setup. The manager remounts on role changes; members do not render or fetch the settings screen. The retained server independently requires an attested Owner and the Careers feature flag for writes.

Credentials use masked replacement inputs, remain in local component state only, and are cleared on successful saves or explicit reload. Empty fields retain stored secrets through the existing service contract. Failed/uncertain saves retain local edits and provide discard/reload recovery; navigation prompts protect unsaved changes. No new storage schema or API contract was introduced. Explicit credential removal and versioned settings conflict detection still require follow-up work.

Source review found Google Indexing, Indeed disposition sync, ZipRecruiter sync and LinkedIn API flags stored by the legacy service without corresponding provider execution in the inspected Core server. The new screen preserves configuration and does not claim connection verification. Indeed feed, Indeed Apply and generic webhook handlers exist; live partner credentials and provider delivery were not exercised. This source limitation must remain in the consolidation inventory rather than being represented as a completed integration.

Validation: 39 Core bridge tests and two settings-service tests passed, including Owner-only writes, feature gating, credential preservation and failed settings writes. Native job-editor browser regression checks passed. The new settings browser fixture passed loaded-setting preservation, credential replacement, uncertain-save retention/reload, clearing credential inputs after success, member exclusion and mobile containment. Mobile screenshots were inspected and checkbox alignment corrected. Dashboard typechecking and build passed with the existing chunk-size warning. The broader consolidation goal and production release gates remain open.

### Versioned Careers settings saves

Administrative Careers settings now read values and their opaque revision in one uncached database snapshot. The revision is a keyed digest of stored rows (including ciphertext, identity and timestamps), never a hash of exposed plaintext credentials. The native form sends the loaded revision; the server requires it for Business Center requests and returns 409 before writing stale settings. Missing/malformed revisions return 400. Older non-federated writers retain compatibility; their value, credential, insert and delete changes affect later revision comparisons.

Versioned settings batches acquire a short `SHARE ROW EXCLUSIVE` lock on `system_settings`, compare the current category, and perform the existing atomic upsert. This coordinates legacy/direct SQL writers and absent rows without adding a new revision table or depending on every writer adopting an advisory lock. Ordinary reads remain available. The tradeoff is that other settings writes wait during this brief metadata transaction; a five-second lock timeout bounds acquisition, and no provider call or encryption occurs while holding the lock. Settings categories outside the target do not change its revision. Database and encryption configuration are unchanged.

The form retains edits on conflicts and disables saves against an older service that does not return a version. Core should be updated before this dashboard editor is released. Explicit credential removal, broader Careers parity, provider execution gaps and the remaining consolidation requirements are still open.

Validation: `node scripts/test-core-settings.mjs` passed all eight tests against its own disposable PostgreSQL 17 database, including concurrent first saves, stale-cache bypass, credential replacement/deletion, unrelated-category isolation, and an observed database lock wait behind a legacy transaction followed by rejection of the stale save. The existing atomic-write/cache behavior remained covered. All 54 focused Core handler/service/cache tests passed, including missing/malformed version rejection and conflict status propagation. The settings browser fixture passed explicit stale-version retention/reload plus earlier Owner, credential and recovery checks. Core/dashboard type checks and both builds passed (existing PostCSS/chunk-size warnings). Generated API types were refreshed; semantic comparison confirmed only the settings version field and 409 response changed in OpenAPI. No production data was used or modified.

### Explicit Careers credential removal

Owner Careers settings now distinguish keeping a blank credential, supplying a replacement and explicitly removing a saved credential. Selecting removal clears the local replacement and turns off its integration in the submitted settings. The server validates known credential names, requires the loaded revision even for legacy removal calls, rejects simultaneous replacements and enabled integrations, and writes selected empty encrypted values in the existing versioned atomic batch. Unselected credentials remain unchanged. Read responses omit removal instructions. The native editor retains removal selections after uncertain responses and resets them only after a successful save or explicit reload.

Validation: 57 focused Core handler/service/cache tests and nine real PostgreSQL settings tests passed. The new cases cover selected-only removal, whitespace clearing, invalid combinations, Owner access, encrypted empty storage, atomic integration disablement and rejection after a newer secret replacement. The browser fixture passed removal controls, disabled integration/replacement inputs, request payload and lost-response reload recovery without duplicate writes, alongside the existing settings checks. Mobile output was inspected. Core/dashboard typechecking and builds passed with existing PostCSS/chunk-size warnings. API generation completed; semantic comparison limited changes to the Careers update request and its new schema. No production credentials were accessed or changed.

Remaining Careers work includes directory associations, idempotent job creation, safe deletion/retention behavior, applicant intake/public lifecycle, notification/webhook parity and the documented provider execution gaps. The full consolidation goal remains active.

### Careers deletion preserves applications and notes

Source inspection found that the legacy job deletion cascaded through applications and review notes. The retained storage method now locks the job, checks its loaded timestamp, and refuses deletion when any application exists. The parent lock conflicts with incoming application foreign-key locks, so an application committing during deletion is observed before the delete can proceed. Existing database foreign keys are unchanged; protection is enforced by the retained deletion service, not a new SQL retention constraint. Jobs without applications can still be deleted. Successful deletion alone dispatches the existing deletion webhook.

The native job editor offers explicit confirmation, requires a saved clean draft and loaded version, directs users toward Archived status to retain history, and handles stale or uncertain results. After an uncertain delete it blocks further saves/deletes until reload or return to the list. The gateway now forwards version bodies only for this DELETE operation. During browser verification, switching the shared rich-text editor’s editable state was found to emit a content update, making pristine nullable fields dirty; editable-state changes now suppress that content event.

Validation: the disposable PostgreSQL Careers fixture passed application/note retention, stale deletion, successful unused-job deletion and an observed lock wait behind a concurrent application insert, alongside the existing job/review tests and migration replay. All 42 Core bridge tests passed, including permission/version errors, protected-job conflicts and webhook suppression. All 95 dashboard tests and migration replay passed after correcting the new transport test’s legacy Blog path. The deletion browser fixture passed clean-draft gating, cancel confirmation, stale reload, uncertain-response recovery and successful deletion. Existing Careers and Blog browser checks passed after the rich-text correction. Core/dashboard type checks and builds passed with existing PostCSS/chunk-size warnings. No production deletion or migration ran. Remaining parity work and the full consolidation goal remain open.

### Offline CRM relationship reconciliation

The read-only CRM reconciliation command inventories Core leads, clients, notes/tasks and canonical identities against existing dashboard records. It recognizes commercial handoffs by source instance and submission ID, derives existing client relationships only through verified lead mappings, and reports conflicting mappings, duplicate submissions, missing parents, unresolved historical identities and stage differences. It never matches contact details, creates records, changes stages or triggers onboarding. Unmapped records remain review items.

The strict projection excludes contact details, note bodies and credentials. Reports are exclusively created with private permissions and an input digest. Large duplicate groups retain a finding for each source while bounding related-ID samples. See [CRM reconciliation](crm-reconciliation.md) for the projection, operation and remaining migration contract.

Validation: all ten CRM reconciliation tests passed, including receipt reuse, cross-instance separation, conflicting parents/children, orphaned and revoked identity references, deterministic ordering, invalid projection rejection, private/no-overwrite CLI behavior and 2,000 duplicate submissions. The four existing identity reconciliation tests also passed. No production inventory or import ran. Native CRM notes/tasks, payload-preserving import, client onboarding and the full consolidation release gates remain open.

### Native inquiry note history in Sales

Both the commercial inbox and general inquiry list now expose native notes using the same canonical Sales history. Notes render as literal text, retain creation times and authorship, and load older pages without a fixed history cap. A missing historical author remains visible rather than hiding the note. Customer notes remain in the existing customer workspace.

The new additive `0039_lead_notes.sql` stores immutable notes, enforces source-note uniqueness for eventual imports and requires native authors or historical provenance. The API independently checks Sales access and ignores no browser-supplied privilege fields: unsupported fields are rejected. Note creation and its audit entry are atomic. The client generates one UUID per intended note and retries the same payload after an unconfirmed response; concurrent identical retries produce one record and audit entry. Different authors, inquiries or text cannot reuse that ID. Note creation does not alter status or follow-up fields. Native notes use no Core admin route.

The browser retains drafts after failed reads and uncertain writes, blocks editing the uncertain payload, and warns before dashboard navigation or switching commercial inquiries. Pending requests abort on unmount and time out after 30 seconds. Notes are fetched only when opened. Mobile inspection found the initial panel too narrow inside an inquiry row; it now occupies its own full-width row.

Validation: all 96 dashboard tests and migration replay passed against disposable PostgreSQL, including concurrent replay, audit count, changed payload/author/parent conflicts, append-only enforcement, source uniqueness, null historical authors, grant revocation and 107 notes spanning sub-millisecond timestamps. Browser checks passed paging, lost-response retry, literal rendering, retained drafts, navigation cancellation and mobile containment. API/dashboard typechecking and builds passed after correcting a frontend type import and an explicitly typed test response; the existing bundle-size warning remains. API generation changed only the two note operations and associated schemas. No production migration or Core import ran. CRM tasks, historical payload migration, client reconciliation/onboarding and the full consolidation release gates remain open.

### Native inquiry and customer follow-up tasks

The commercial inbox and general inquiries now expose follow-up tasks, and the existing customer Internal notes tab is labeled Notes & tasks. One canonical task model serves both contexts, with independent server authorization for Sales and Customers. Minimal assignee lists include only active accounts with the relevant area grant; new assignments are revalidated under transaction locks. Existing historical assignments can remain unchanged without granting those assignees new access.

Tasks support titles, optional due times, assignments, completion/reopening, open/completed/all filters, older pages and revision history. Native creation uses a stable requested UUID and immutable normalized creation payload so replay after an uncertain response—or after subsequent edits—does not duplicate or revert the task. Versioned edits reject stale changes. Database triggers preserve origin and all revisions, prevent deletion and require advancing versions. Audit failures roll back task and revision changes together. Historical null authors remain visible, and source IDs are unique within source instance and lead/client source table. No Core import or user linking occurs automatically.

The browser retains local changes on failure, keeps uncertain creation payloads fixed for retry, and requires saved-state reload after an uncertain edit. Navigation warns about unsaved changes. Editor focus follows the opened task. Due dates use the displayed browser timezone, preserve an unchanged saved timestamp, and reject local times skipped by daylight saving. Completed work remains readable; no reminder dispatch or operational work order is implied.

Validation: all 97 dashboard tests plus migration replay passed on disposable PostgreSQL. New coverage includes area separation, assignee minimization/eligibility, concurrent creation and edit conflicts, creation replay after edits, explicit completion/reopening revisions, revoked access, parent isolation, source uniqueness, preserved null historical identities, protected origins/history, exact pagination across 107 records, archived-client exclusion and rollback of task/revision writes after synthetic audit failures. Browser checks passed both inquiry and customer entry points, lost creation response, stale edit retention/reload, completion/history, timezone conversion, invalid DST time rejection, draft retention/navigation cancellation and mobile containment. The initial browser check found an ambiguous assignment label; it now has an explicit accessible name. Mobile output was inspected. Shared-library, API and dashboard type checks and API/dashboard builds passed, with the existing bundle-size warning. The broader CRM payload import/client mapping, explicit Won onboarding and consolidation release gates remain open.

### Customer notes preserve historical authorship and complete history

The consolidation now extends the existing customer-note store rather than introducing a parallel CRM store. Additive migration `0041_customer_note_provenance.sql` retains source instance/note/author IDs, enforces source uniqueness and allows missing local authors only for records carrying source provenance. Native notes still require the authenticated author. Existing append-only triggers remain enforced. Both client and property workspace previews use left joins, so missing historical accounts no longer hide their notes.

The customer Notes & tasks screen now loads the full paginated history, with explicit historical-author fallback, source indicator, property label and literal note text. Existing workspace previews remain bounded for compatibility. The composer retains its existing editor and property selector, but now keeps a stable requested ID and payload after uncertain writes. Concurrent identical retries create one note/audit event; mismatched actor, client, property or body returns a conflict. Saved-note retries remain valid if the property was subsequently archived, while new notes cannot target archived or unrelated properties. Drafts survive failed reads, navigation warns about unsaved work, and the disabled editor exposes its read-only state to assistive technology.

Validation: all 98 dashboard tests and migration replay passed on disposable PostgreSQL. Coverage includes native legacy callers, trusted authorship, source uniqueness/immutability, missing-author visibility in both workspace projections, exact paging across 108 notes, property ownership/archive behavior, capability revocation, client/crew/Sales exclusion and atomic rollback after a synthetic audit failure. Customer-note and CRM-task browser checks passed historical fallback, older pages, property-scoped lost-response retry, literal rendering, draft retention, navigation cancellation and mobile containment; mobile output was inspected. The browser fixture uses the DOM's actual content-editable state because its automation helper did not support the disabled custom textbox. Shared-library/API/dashboard type checks and API/dashboard builds passed with the existing bundle warning. No production data or Core payload import was used. Full CRM reconciliation/import, explicit Won onboarding and the broader release gates remain open.

### Native follow-up management for general inquiries

General Sales inquiries now have native owner, stage, next-action and due-time controls alongside their notes and tasks. A minimal read endpoint supplies the current version and eligible Sales owners. The general and commercial editors write through one shared versioned service, preserving the commercial-only legacy route and audit action. Owner eligibility is checked under transaction locks; updates and audit insertion remain atomic. No intake fields or conversion IDs are accepted from the editor, and Won does not trigger client/property/portal provisioning.

The editor preserves local drafts on stale or unconfirmed saves and blocks further writes until saved-state reload. Reload cancellation and read failure retain local edits. Due times use the displayed browser timezone, preserve an unchanged saved timestamp and reject times skipped by daylight saving. The inquiry list reflects successful saved status without a whole-workspace refresh that would disturb other drafts. No database migration was added.

Validation: all 99 dashboard tests and migration replay passed. New database-backed cases cover grants, minimal owner choices, invalid/disabled owners, general/commercial route isolation, shared-version conflicts, stage transitions without onboarding, intake preservation and rollback after audit failure. The separate commercial runner passed its five receiver/queue/HTTP tests and seven upload subtests after updating old fixtures that had assumed job-title-based access. Fixtures now grant only the capabilities needed for their intended positive cases and explicitly reject ungranted legacy managers before upload parsing; production upload code was unchanged. Browser checks passed owner/stage controls, stale/lost-response recovery, local due-time validation, failed-reload draft retention, navigation cancellation and mobile containment. An ambiguous textarea label found in browser testing now has an explicit accessible name. Mobile output was inspected. Shared-library/API/dashboard type checks and API/dashboard builds passed with the existing bundle-size warning. OpenAPI changes are limited to one path and three schemas.

General inquiry list pagination, contact/context editing, historical CRM payload import, explicit Won onboarding and the broader consolidation release gates remain open. No production data or provider connection was used.

### Complete inquiry history and native Sales filters

The native Sales inquiry view now uses a dedicated paginated query instead of the legacy 200-row array. It supports literal contact/company/location/owner search, stage, assigned/unassigned owner, general/commercial type and overdue follow-up filters. Exact microsecond timestamp/UUID cursors preserve equal-time records and remain stable across follow-up edits; cursors are bound to their filters. Commercial rows do not require an ingress receipt. The old endpoint remains compatible. Additive migration `0042_inquiry_history_index.sql` adds only an ordering index.

Loading older pages preserves existing editors and drafts. Replacement queries dispatch the existing draft-navigation guard, retain prior results on failure and temporarily disable row interactions while loading. Newly saved inquiries and changed follow-up records prompt an explicit refresh so filter membership can be reconciled without silently destroying another draft. Appending older results does not clear that refresh notice. Inquiry creation/conversion and notes/tasks/follow-up continue through their existing workflows.

Validation: 100 dashboard tests and migration replay passed on disposable PostgreSQL, including traversal of 235 records with equal and microsecond timestamps, literal wildcard search, contact/company/owner filters, overdue rules, malformed/filter-mismatched cursors, minimal projection and fresh grant revocation. Native inquiry browser checks passed failed-page retry, draft-preserving append, cancelled/confirmed replacement, combined filters and mobile containment; mobile output was inspected. Adjacent notes, tasks and follow-up browser regressions passed. Shared-library/API/dashboard type checks and API/dashboard builds passed with the existing bundle-size warning. The new test fixture initially omitted explicit lead IDs, and its query-case union required a TypeScript annotation; both were corrected before these passing checks.

The agreement-draft browser regression also passed after retaining both legacy and paginated inquiry fixture responses; its context selector still consumes the legacy bounded list and needs a separate searchable selection update.

Contact/context editing, historical CRM payload import, explicit Won onboarding and the broader consolidation release gates remain open. The full goal is still in progress, not a release candidate. No production data or provider connection was used.

### Agreement context can select the complete inquiry history

Agreement creation and context changes now share a searchable inquiry picker backed by the paginated Sales endpoint. Users can load older choices, search contact/company/location/owner text and clear the search without changing the attached inquiry. The selected record remains an explicit option outside current results, preserving its known label when available. Failed searches retain choices and selection; initial failure prevents context submission until recovery. Requests have bounded timeouts, abort on unmount and ignore superseded responses. Enter in the search field performs a search without submitting the enclosing agreement form. Saving still uses the existing server validation for related client/property/inquiry records.

Validation: dashboard type checking and production build passed (existing bundle-size warning). The expanded agreement browser regression passed older-page selection, failed-search retention/retry, no-match selection preservation, clearing search, Enter behavior and no calls to the legacy bounded lead list, alongside existing creation retry, source-template selection, saved preview, version conflicts, context edits, scope/cost calculations and capability restrictions. Mobile picker containment was checked and the rendered layout inspected. No API or schema change was needed. This closes the agreement inquiry-selector limit recorded in the preceding checkpoint; broader CRM import, contact editing, onboarding and release gates remain open. No production deployment or migration occurred.

### Native contact/inquiry corrections with preserved detail history

Sales now exposes a contact/details editor in both the complete inquiry list and the commercial detail view. It covers contact name, email, phone, company, location and message. These are inquiry corrections, not edits to linked client/property records or already-saved agreements. The existing shared inquiry version protects against concurrent follow-up/context changes. The UI keeps drafts after failures, requires saved-state reload after stale or unconfirmed writes, warns before navigation/discard and renders historical text literally. Older history pages do not replace current drafts. The general list updates saved details and asks for explicit refresh to reconcile search/filter membership.

Migration `0043_lead_detail_history.sql` preserves a baseline for existing rows and records creation/later detail snapshots through database triggers. Baselines are explicitly labeled as migration-time state, not original submission evidence. History is append-only, includes nullable historical actors and is paginated by shared inquiry version. The detail service sets transaction-local authorship, validates a strict six-field payload and commits correction/history/audit together. Audit metadata identifies changed fields without copying contact values. Native/legacy creation paths without actor context preserve null authorship. Original commercial receipts and source metadata remain separate and untouched.

Validation: all 102 dashboard tests plus migration replay passed. New coverage verifies populated legacy migration baselines, immutable earlier snapshots, pagination beyond 30 changes, simultaneous edits, conflicts with follow-up versions, no-op saves, input/field rejection, revoked capabilities, separate crew/client/customer access, minimized history projections and complete rollback after synthetic audit failure. Commercial receiver/upload regressions passed; existing tests that intentionally correct email via SQL now advance the inquiry version. Browser tests passed stale/lost-response recovery, failed reload retention, older-history draft preservation, literal history text, navigation cancellation and mobile containment. Inquiry-list, follow-up and agreement-draft browser regressions also passed. Shared-library/API/dashboard type checks and API/dashboard builds passed with the existing bundle-size warning. Mobile output was inspected.

CRM source import and reconciliation, explicit Won onboarding, complete Marketing parity, identity/capability review and the broader release gates remain open. The full goal is still in progress and is not a release candidate. No production database, deployment or provider was used.

### Full CRM payload-preservation contract and offline manifest

The migration preparation now covers every persisted field in Core leads, clients, their notes and their tasks. A new offline command builds a private manifest containing full source snapshots, deterministic content hashes, reconciliation findings and proposed native note/task projections. It derives record identity reconciliation from the same source payloads and preserves non-native client/contact/onboarding fields instead of guessing a merge. Existing-target parent snapshots never imply field overwrites or Won onboarding.

Preparation fails on incomplete/unknown source fields, duplicate source-table IDs, malformed timestamps, unsupported JSON and unsafe numeric values. Unmapped/conflicting parents, incompatible content and missing required dates remain preserved but explicitly blocked. Exact text and microsecond timestamps survive; unresolved historical people remain source IDs with null canonical projections. Source author and assignee values are not replaced by the import operator. Assignment eligibility still requires fresh database checks in the eventual importer.

Validation: all 24 migration-tool tests passed, including 10 new payload tests. A schema-contract test compares all six payload shapes with actual Core table declarations. Other cases cover every-field preservation, independent owner/account-owner fields, null/revoked/ambiguous identities, text/length/date constraints, missing/duplicate fields, record-ID matching, deterministic hashes across input order, private exclusive output and redacted failures. No application runtime or database schema changed; full app builds were not needed for this standalone Node tool. The original checkout and Blythe remained untouched.

Source extraction, reviewed mapping resolution, durable database archive/import with transactional replay, unmatched parent policy, full data reconciliation and restore/cutover rehearsals remain open. This is migration preparation, not a release candidate or a completed import. No production data was used.

### Reviewed CRM snapshots and native history can be imported transactionally

A new operational CLI rederives the payload manifest from the exact full export, requires an explicit manifest-bound review and dry-run/apply mode, checks an active Owner reviewer, and imports only reviewed existing-target records. Additive migration `0044_crm_source_archive.sql` stores every full source snapshot and mapping immutably. Parent data is archived without overwriting operational fields. Notes/tasks use the existing native stores, preserve original text/timestamps/completion/provenance and remain under the existing Sales/Customers API boundaries.

Imports serialize by source instance. New records recheck current parents, indexed commercial receipt identity, converted-client relationships and canonical users. Open assignments require current active area eligibility; completed historical assignments can retain existing inactive identities without granting access. Exact replay checks the immutable source/projection/target mapping and native provenance, preserving subsequent native task edits. Mismatched source payloads or mappings, preexisting unarchived provenance and duplicate parent mappings across batches are rejected. Native rows, revision triggers, archives and audit metadata commit or roll back together. CLI output contains counts only, and uncertain commits instruct reconciliation with the identical batch.

Validation: 107 dashboard tests plus migration replay passed on disposable PostgreSQL. Five new import tests cover dry-run rollback; simultaneous import/replay; exact snapshots and microsecond note/task dates; untouched target fields and receipts; native edits surviving replay; current Owner/assignment checks; changed receipts/conversions; immutable archives; late audit-failure rollback; provenance conflicts; cross-batch parent uniqueness; and actual CLI dry-run/apply/replay. Imported note visibility and revoked Customers access were verified through the existing HTTP APIs. The CLI test initially used a URL pathname containing encoded spaces; the fixture now converts file URLs correctly and passes. All 24 offline migration preparation/reconciliation tests also passed. Shared-library and API type checks and the API build passed. No dashboard presentation changed in this checkpoint.

The import CLI operates with privileged database credentials, not a web-session assertion. Its review file is not a cryptographic signature or release authority. Actual Core identity-link freshness must be verified with the source freeze because those links are not owned by the dashboard database. Source extraction/freezing, unmatched parent creation, full source-field functionality and archive access, full reconciliation/restoration rehearsal, Marketing parity and remaining retirement gates stay open. The full goal remains in progress, not a release candidate. All data used here was synthetic; no production import or deployment ran.

### Read-only CRM extraction feeds the reviewed import workflow

A new exporter reads all six supported Core CRM tables and minimal target/identity inventories in separate repeatable-read, read-only transactions. It checks live source table/column/type coverage, preserves exact text and microsecond dates, derives existing mappings from receipt/archive IDs and produces a private exclusive export/manifest/evidence bundle. Evidence includes snapshot identifiers, file hashes and explicit absence of identity/archive tables. It never asserts a source freeze or distributed consistency. Source timestamp interpretation is mandatory and recorded; ambiguity checks reject tested DST gaps/folds rather than silently selecting one instant.

Testing revealed the address-column naming exception, which now has an explicit mapping. It also exposed a broader numeric-preservation risk: JSONB/file numbers could round during JavaScript parsing. Export now reads JSONB text and checks numeric source tokens; preparation/import use the same parser, rejecting unrepresentable decimals, integer rounding, underflow and overflow. Source-schema additions and incompatible types fail instead of being omitted. No Core source files or production records were modified.

Validation: all 111 database/API tests and migration replay passed, including four export tests against actual Core migrations. Coverage includes read-only evidence, complete source equality, export→import→re-export/replay, private bundle modes/hashes/no overwrite, real CLI behavior, microsecond conversion, explicit timezone requirements, DST gap/fold rejection, concurrent source changes, live schema/type drift, absent identity linkage and database JSON precision rejection. All 25 offline migration tests passed, including the new numeric-token cases. No application presentation, API contract or database migration changed in this checkpoint, so no application rebuild was needed.

Production timestamp-convention and source-freeze verification, unmatched parents and incompatible payload resolution, full CRM field functionality and archive access, full import reconciliation/restoration, Marketing parity and remaining retirement gates stay open. The full goal remains in progress, not a release candidate; no production export, import or deployment ran.

### Independent CRM archive and native-history verification

A read-only post-import verifier now compares the complete source export with archived JSON, mappings, receipt identities, import audits and native notes/tasks. It recomputes stored payload hashes independently and verifies task origin plus contiguous/current revisions while preserving legitimate later edits. Snapshot evidence and private exclusive reports distinguish successful checks, completed mismatch reports and incomplete runs. This verifies preservation scope only; reconciliation exceptions and source-freeze/release authority remain separate. See [the verifier runbook](crm-import-verification.md).

The importer now rejects noncanonical uppercase target UUIDs before they can create mappings that fail later replay. Preparation, import and verification reject malformed UTF-8 instead of silently replacing bytes. Shared synthetic import fixtures keep importer and verifier tests consistent.

Full source-field functionality and archive access, unmatched-parent policy, production reconciliation and backup/restore rehearsal, Marketing parity and the remaining consolidation gates stay open. The full goal is still in progress, not a release candidate. No production data, migration, import or deployment was used.

Validation for this checkpoint: all 117 dashboard/database tests and migration replay passed, including five independent verification cases and canonical target UUID rejection. All 26 offline migration tests passed, including strict UTF-8 decoding. Verification coverage includes complete/missing imports, later native edits, receipt/audit loss, unexpected records, stray revisions, archive hash recomputation, CLI privacy/exit behavior and concurrent snapshot consistency. No application UI/API contract or database schema changed, so application builds were not repeated.

### Staff can inspect imported CRM history in the consolidated dashboard

Sales inquiry and commercial detail views now expose imported CRM history; customer Notes & tasks provides the corresponding customer history. The lazy viewer lists complete source identities with pagination, opens original source JSON as literal text and distinguishes imported snapshots from current editable records. It preserves exact stored numeric values, original IDs, empty fields and dates. Failed requests retain prior list/detail context where appropriate, retries are explicit, and neighboring note drafts survive browsing. This is historical access, not a replacement for outstanding native field workflows.

Separate server routes require current Sales or Customers capability and bind every read to its parent. Detail queries cannot cross customer/inquiry boundaries using known source identifiers. List projections omit full payloads and internal importer/review metadata. Source values are never rendered as HTML or parsed into potentially rounded JavaScript numbers. Additive 0045 indexes support parent-scoped keyset queries; no source/history rows are modified. The OpenAPI contract and generated client were extended without changing existing paths/schemas.

Validation: 118 dashboard/database tests and migration replay passed. New database coverage traverses 57 source records across instances, verifies private minimal list projections and exact microsecond/large-number detail values, rejects cross-parent/cross-area access, checks fresh grant revocation, malformed inputs and missing targets, and confirms there are no mutation routes. Shared-library/API/dashboard type checks and both application builds passed with the existing bundle-size warning. Browser checks passed lazy loading, initial/detail/older-page retry, literal values, adjacent draft preservation, customer integration and mobile containment; the mobile rendering was inspected. The browser fixture initially used a non-UUID customer route ID and was corrected before passing.

Full native source-field functionality, explicit Won onboarding, unmatched-parent policy, production data reconciliation/restore rehearsal, Marketing parity and other release gates remain open. The full consolidation goal remains in progress, not a release candidate. No production migration, import or deployment ran; the original checkout and Blythe were untouched.

Adjacent inquiry-list and customer-note browser regressions also passed after integration, including draft guards, retry, history pagination and mobile containment.

### Won inquiries explicitly create or link customers

The consolidated Sales inquiry and commercial detail views now have a customer onboarding panel for users with both Sales and Customers grants. It loads current state, requires Won, offers active existing customers or explicitly entered new-customer details, and saves with inquiry version plus an operation UUID. Unknown outcomes freeze the submitted values for an exact retry; rejected/stale requests preserve drafts but require saved-state refresh. The linked customer is shown after success. No property, portal access, accounting account or agreement is created by this step.

A lightweight route/service and additive 0046 receipt table make creation/link, version advancement and audit atomic. Original source fields and Won stage stay unchanged. Exact replay survives subsequent customer archival; changed input/actor/parent mappings conflict. Existing prospect relationships cannot contradict the chosen customer, and context editing is blocked after onboarding. The inquiry list's old property-creating conversion button is replaced. The compatibility endpoint now reuses the already-onboarded customer, rejects replacement/archived customers and preserves Won when explicitly creating a property. Prospect-property promotion remains separately gated.

Validation: 119 dashboard/database tests and migration replay passed. New tests cover dual-grant/crew/client boundaries, concurrent exact replay, stale/different requests, stage and active-customer gates, unchanged source fields, no property/access/accounting side effects, immutable receipts, context conflicts, audit-failure rollback, grant revocation and compatibility conversion preserving the linked customer/Won stage. Shared-library/API/dashboard type checks and API/dashboard builds passed with the existing bundle-size warning. Onboarding browser tests passed initial failure, non-Won gate, stale draft retention, exact lost-response retry, customer link, grant visibility and mobile containment; the mobile rendering was inspected.

This validation also corrected missing response type annotations in the preceding archive test file. That checkpoint's API type check had run before the new test was added, so its final type-check claim was too broad. The complete API test source set now passes type checking.

Operational adoption of prospect context, full agreement-to-operations/billing rehearsal, native support for remaining source CRM fields, unmatched-parent migration policy, production reconciliation/restoration, complete Marketing parity and the remaining retirement gates are still open. The full goal remains in progress, not a release candidate. No production migration, import or deployment occurred; the original checkout and Blythe remained untouched.

Adjacent inquiry-list, follow-up and imported-history browser regressions passed after onboarding integration, including draft guards, retries and mobile containment. Existing OpenAPI paths and schemas were compared semantically with the prior commit and remain unchanged.

### Marketing Website System starts with protected head-markup storage/editor

Added Owner-only Marketing → Website System → Head tag additions, a typed API/client and a narrow Core endpoint that retains the original head setting. Saved markup remains literal in the editor. The category version guards stale writes; input is bounded without truncating existing larger values. Drafts survive stale or unconfirmed writes and failed reloads. Reload/discard is explicit. Business Settings are unchanged.

Core independently checks active attested ownership, even with CMS disabled. Public-only category reads/writes reject private data and cross-category key collisions. Settings and a key-only audit record commit atomically. Legacy head writes now require canonical Owner authority and are audited; local admin alone is insufficient. Other generic settings retain their old contract. Head-setting deletion points to explicit clearing in the new editor. No schema migration was needed.

Inspection found a preexisting functional gap: the old admin editor stores `public_head_html`, but no consumer exists in Core or the public gateway. The gateway currently restricts inline scripts through CSP. Earlier commentary assumed retained public rendering; source inspection disproved that assumption. The new screen explicitly labels delivery integration as pending and does not claim saving publishes tags. Public head rendering, script/consent/CSP behavior and full Website System parity remain open; no script activation or CSP change is included.

Validation: 119 dashboard/database tests plus migration replay passed; 56 focused Core bridge/legacy-settings/cache tests passed; all 10 real-database settings tests passed, including audit-failure rollback and private/category collision checks. Core/shared-library/API/dashboard type checks and all three production builds passed with existing bundle-size warnings. Browser tests passed literal script text, stale/uncertain save recovery, draft retention across failed reloads, explicit clearing, Owner visibility and mobile containment; the rendered mobile editor was inspected. The new Core route fixture initially passed a body in the helper's prefix position and was corrected. Dashboard type checking identified the exhaustive legacy illustration map needing a `none` entry for the new route; that was added before the passing rerun.

The full consolidation remains in progress, not a release candidate. Design and other Website System modules, visual-canvas parity, remaining capability/identity and CRM work, full agreement-to-operations rehearsal, restoration/reconciliation and retirement gates remain open. The original checkout and Blythe were not modified. No production migration, setting write or deployment ran.

### Public head markup now reaches the website through its gateway

Core now exposes a minimal public projection of the Owner-authored head setting. The website gateway reads the fixed Core path without visitor credentials, validates/bounds the response, coalesces refreshes and injects literal markup only into normal public documents. Admin/API/assets and editor previews are excluded. A short memory cache has no stale-script/disk fallback: unavailable or invalid source data becomes empty once the cache expires. Existing public content remains available. The dashboard editor now describes delivery timing and script-policy restrictions.

No CSP permissions were added: inline scripts and unapproved script sources remain blocked. Raw permitted scripts are not automatically consent-managed; existing structured GA4 remains the appropriate analytics path. Global markup can conflict with page SEO or duplicate tracking, so saved production snippets must be reviewed before release. This resolves the previously discovered missing consumer without claiming arbitrary vendor execution. The [delivery runbook](website-head-delivery.md) documents exact scope, deployment ordering, cache/failure behavior and nondestructive rollback.

Validation: all 35 website server/runtime tests passed, including five new cache/payload/insertion tests and real HTTP exclusion/CSP checks. All 47 focused Core public/Owner/legacy bridge tests passed; Core and dashboard type checking passed. Website, Core and dashboard production builds passed, with existing sourcemap/bundle warnings; the website prerendered 54 routes. A local browser observed the synthetic verification meta tag in the real document head, confirmed that CSP blocked a synthetic inline script, and confirmed preview/admin exclusion while all external requests were denied. The Owner-editor browser regression also passed. Build-generated source-revision-only manifest churn was excluded from this scoped patch.

Actual stored production snippets, provider/CSP acceptance and post-release verification remain unperformed. Design and other Website System modules, visual-canvas parity, remaining capability/identity and CRM work, full agreement-to-operations rehearsal, reconciliation/restoration and retirement remain open. The full goal stays active and is not a release candidate. No production setting, script, deployment or migration was changed.
