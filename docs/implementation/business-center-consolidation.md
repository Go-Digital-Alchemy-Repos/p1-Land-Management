# Business Center consolidation implementation evidence

Status: in progress. This is not a release candidate and must not be deployed as a completed consolidation.

## Branch and source preservation

Implementation worktree: `p1-business-center-consolidation`, branch `codex/business-center-consolidation`, based on published `origin/main` at `2848855`. The original shared checkout and its content-overhaul changes remain untouched. Blythe is read-only reference material; no Blythe change is included.

The reporting implementation from `codex/admin-google-analytics` was integrated through four clean cherry-picks: `3a7c875`, `5105e17`, `c0c0737`, `0d4a7d1`. This preserves the reporting code, not a claim that Google credentials are connected or that the new destinations exist yet.

## Implemented foundation

- Shared explicit leaf capability catalogue for Workspace, Customers, Operations, Revenue, Marketing Content/Design/Reporting, and business Settings. Group selection expands into current leaves. Owner alone receives automatic known-tool access.
- Additive migration `0028_business_access.sql`; reviewed grants, profile names, optimistic version, notification subscriptions, invitation grants/revocation, and plain `member` role. Existing staff receive no automatic grants. Legacy role suggestions are review-only.
- Owner-only User Manager APIs and UI: account search, invite/resend/revoke, names, suspension, grouped permissions, MFA policy controls, session revocation and audit history. Stale edits preserve the draft and explicit reload fetches fresh server values.
- Access updates atomically version grants, audit, and invalidate sessions. Owner access cannot be edited or suspended through this manager. Client invitations retain separate client-access boundaries.
- `/me` returns capabilities. Dashboard navigation now uses explicit office grants, with profile fallback for ungranted accounts. Existing client and assigned crew portal routes remain distinct.
- Sales API follow-through: legacy leads/estimates, current estimate creation/document/recipient/send paths and commercial inbox require explicit Sales or the applicable financial read grant. Template selection and maintenance are separate. Commercial owner assignment checks active Sales grants rather than legacy titles. Direct HTTP tests prove denial for ungranted Manager/report-only accounts and access for explicitly granted members.
- Initial service migration: sales revision/conversion, commercial assessment/prospect context, agreement management/read/activation preview, billing charge review and queue require explicit grants. Billing reads remain separated from general agreement details.
- OpenAPI and generated client cover the new identity field and User Manager endpoints. Codegen now preserves its handwritten index and legacy estimate adapter.

## Validation to date

- `pnpm run typecheck:libs`: passed.
- API and dashboard typechecks passed after actor typing and generated-client integration fixes.
- `node scripts/test-dashboard.mjs`: 47/47 passed, plus migration replay, using disposable PostgreSQL and synthetic records.
- `node scripts/test-service-agreements.mjs`: lifecycle/charge/schema/review tests, worker preparation test and real HTTP test passed; no live providers.
- Dashboard production build: passed; existing large MapLibre chunk warning remains.
- Dashboard navigation tests: 3/3 passed after explicit-grant navigation change.
- User Manager browser regression passed in headless Chrome: stale draft retention, fresh reload/version, isolated Analytics grant, invitation submission, and mobile dialog containment.

The baseline property-workspace test omitted nullable structured-address and property-type fields already returned by the baseline API. Its expectation was aligned with the verified response; production behavior was not changed to satisfy that assertion.

## Required remaining work

1. Complete the API capability matrix and migrate all remaining legacy role gates, including projections, shared lookups, assignment choices, notifications and exports. Initial domain changes and navigation are not full enforcement. Replace broad frontend role-based preloading/action conditions. Preserve crew/client record scope and offline queues.
2. Complete User Manager recovery and Core form-notification selection, existing-account linkage, history/pagination and migration reconciliation. Review crew grants versus scoped field workflow before cutover.
3. Complete module-by-module visual/accessibility verification of the adopted Core admin theme, including the remaining Marketing and template screens. Shared light/dark/system styling and sampled browser verification are implemented.
4. Mount the retained CMS route modules under Marketing and build the allowlisted, user-scoped Core boundary. Separate Google Analytics and Search Console grants on all legacy and new server routes. Preserve original media/publishing/intake behavior.
5. Merge CRM notes/tasks/history and client identity through explicit source mappings, repeatable import/reconciliation, and explicit Won onboarding.
6. Implement reusable MSA/scope/cost/package templates, immutable published versions and client composition integrated into existing estimate/approval/activation workflows. Preserve legal-content and billing constraints in the accepted plan.
7. Feature inventory/parity verification, restore rehearsal, redirects/retirement tooling, production builds and final release review. No legacy removal or production cutover has occurred.

Canonical scope: `docs/proposals/p1-business-center-consolidation.md`. The active goal remains the full implementation, not just this foundation.

## Next integration notes

The reporting authority contract and separate source grants are implemented (see checkpoints below). The CMS migration is now extending that user-scoped boundary. Other Core areas still use legacy local admin/editor gates and must be migrated before retirement; local administrators must never implicitly become canonical Owners.

The web app now loads datasets from leaf grants and uses a typed `/workspace/references` endpoint for minimized selection fields. Grants are checked independently for record workspace tabs and source sections. The main client, property, schedule, recurring, project, inspection, sales and billing action controls use capabilities; agreement selectors no longer fetch full Properties records. Failed or obsolete refreshes cannot repopulate business datasets after an identity/grant change.

## Customer and operations enforcement checkpoint

- Customer/contact/property/type reads and writes require the corresponding Customers grant. Nested client/property sections query only permitted source areas; general workspace activity is Owner-only. Request attachments are excluded from generic property file listings and remain available through their own authorized tool.
- Schedule, recurring programs, project phases, inspections, expenses, billing and QuickBooks financial actions use explicit capabilities. Integration credential configuration remains Owner-only. Requests-to-work conversion requires both Requests and Schedule. Project phase billing intents require Billing.
- My Day-only ordinary members receive assignment-scoped work reads and field submission access. Office completion review requires Schedule, including inside the transition policy itself. Existing client publication and crew assignment boundaries remain.
- Workflow notification recipients are selected by tool grants; global manual delivery administration is Owner-only. Historical notification access and delivery-time revocation for already queued messages still need follow-through.
- Disposable database suite passed 47 tests plus migration replay. Added direct unauthorized HTTP checks across Customers, Operations, Revenue and minimal references, minimized record-section assertions, and My Day member assignment checks. After removing the final string-role completion-review fallback, its targeted policy suite passed 5 tests. API/dashboard typechecks and agreement lifecycle/preparation/HTTP tests passed. No live providers used.

Remaining access follow-through includes server-side assignee eligibility on every write path, notification history/delivery revocation, agreement-only editing versus billing projections, isolated-tool browser scenarios and the Core federation authority contract. Do not treat these checkpoints as complete system authorization coverage.

## Unified admin appearance checkpoint

The dashboard now uses the Core admin light/dark tokens from `platform/p1-core/client/src/index.css` through dashboard-owned `theme.css`. Earthtone feature styles use semantic surfaces, text, borders, actions and status colors. Decorative page illustrations are no longer mounted. Core/public branding files are unchanged. The light muted-text token is slightly darker than Core's source to meet 4.5:1 on the admin canvas.

The header exposes Light/Dark/System for every signed-in workspace user, independently of business-settings permission. Selection is device-local under `p1-business-center-theme`; system changes and cross-tab storage changes are observed. A same-origin pre-render initializer prevents the wrong initial mode, and the offline shell caches that initializer. Storage failure retains a working in-session preference. App manifest colors and QR-code contrast were aligned. Ordinary modal layering now clears the mobile header.

Validation: dashboard typecheck and production build passed (existing MapLibre chunk warning). `scripts/test-dashboard-theme-browser.cjs` passed in Chrome against mocked synthetic client data: light/dark/system, reload persistence, OS changes, key text/action contrast in both modes, absence of decorative backgrounds, client editing dialog, mobile containment and title hit testing. Light desktop, dark dialog and mobile screenshots were visually inspected. Existing User Manager conflict/invite/mobile browser regression passed with the new shared theme loaded.

This proves the shared theme and sampled workflows; final visual acceptance still requires the remaining Marketing/report/template screens and the broader module-by-module parity pass. Production remains unchanged.

## Marketing reporting authority and transport checkpoint

Federation introspection now negotiates `include_capabilities: true`; older callers receive the original strict DTO unchanged. Core accepts the plain `member` role and receives current leaf grants on every introspection. Authenticated requests retain that verified canonical identity separately from the linked local Core user. Local admin/editor or CRM permission alone cannot authorize reporting. Analytics and realtime require `marketing.analytics.view`; Search Console requires `marketing.search-console.view`, including the legacy report APIs.

Dashboard reporting reads now have three fixed `/api/v1/marketing/reporting/` operations: `analytics`, `realtime`, and `search-console`. They authorize the caller, create a 60-second user/session-bound grant, call the fixed Core reporting ingress, and delete the grant afterward. Core introspects the grant and enforces current canonical permissions plus the existing linked Core account's active/revoked status. No shared admin browser session or unrestricted proxy is used. The existing provider services still own queries, caches and Google credentials. Report responses are bounded and private/no-store; arbitrary upstream errors/headers are not forwarded. OpenAPI and generated report DTOs/client functions are included.

Release ordering: deploy the additive dashboard introspection extension before the upgraded Core consumer (the old dashboard rejects the new request field). Provision a separate random base64url service key in dashboard `CORE_MARKETING_SERVICE_KEY` and Core `DASHBOARD_MARKETING_SERVICE_KEY`; configure dashboard `CORE_MARKETING_ORIGIN` as the exact HTTPS Core origin, with no path/query/credentials. Existing federation confidential-client configuration and explicit identity links are also required. No keys were created, exposed, or configured by this change. Private Core service ingress is mounted after the API rate limiter and before browser Origin checks; it rejects Cookie, Origin and Fetch Metadata headers and authenticates the service key before any identity/provider call. Ordinary browser routes retain their Origin checks.

Focused Core tests exercise the actual permission middleware, independent sources, malformed/browser-shaped ingress and suspension; the disposable Core federation integration exercises linked accounts and immediate reporting grant changes/revocation. The dashboard suite includes minimized request construction, bounded/sanitized responses and direct unauthorized report reads. This checkpoint does not yet mount the reporting UI under Marketing or claim a live Google connection. Full CMS migration and reporting browser parity remain required.

Owner instruction retained: push the finished, validated branch to GitHub when the full goal is complete. No interim push or production cutover is authorized by this checkpoint.

Reporting checkpoint validation: dashboard suite 50/50 plus migration replay; Core route/auth suite 19/19; disposable Core federation suite 15/15; Core, API and dashboard typechecks passed. A pure transport test initially imported the authenticated router and caused asynchronous BetterAuth initialization to outlive the test's pool cleanup; transport/configuration were separated from route/authentication dependencies, and the standalone transport tests now pass without opening a database or triggering that warning. The generated client reflects source-specific report shapes and sanitized unavailable responses. The transport currently rejects report bodies above 8 MiB rather than silently truncating them; include large-report parity in final reporting validation.

## Marketing reporting screens checkpoint

Marketing is now a navigation group immediately above Settings. `/marketing/reporting/analytics` and `/marketing/reporting/search-console` are independently permissioned, lazy-loaded report screens using the generated dashboard reporting client. Search Console-only navigation never loads Analytics/realtime, and Analytics-only navigation never loads Search Console. Changing user/grants/source remounts the workspace; requests are aborted on navigation/range changes and failed historical refreshes clear previous report data.

The adaptation retains overview KPIs/prior-period comparisons, daily trends and metric selection, channels/source/campaigns, pages/landing pages, geography/devices/browsers, events, and realtime totals/countries/devices. Device breakdowns retain donut presentation for count metrics. Search Console includes finalized Web totals, daily trends, queries/pages/countries/devices, prior comparisons, and Pacific-day presets. Both include date presets/custom dates, refresh, loaded-row search/sort/pagination, formula-safe filtered CSV export, source identity/freshness and sampling/threshold/truncation disclosures. Missing daily values remain gaps and missing metrics show unavailable rather than invented zero. External provider links use the returned property identity. No public tracking was added.

Reused the existing project chart library version (Recharts 2.15.4) and adapted the prior report formatting functions. Reporting is a separate lazy bundle; it does not expand startup with chart code. Mobile chart-tooltip overflow found in browser testing was confined to the chart surface. The newer major Recharts version was not introduced as part of this migration; package tooling flags the existing v2 series deprecated, which remains a future dependency-maintenance consideration.

Browser verification uses synthetic intercepted API responses only: source isolation, table pagination/search, formula-safe downloaded CSV contents, independent realtime failure, historical-source failure clearing stale rows, revoked grants, dark mode and mobile containment. Screenshots were visually inspected. This is frontend/reporting implementation evidence, not proof of a live Google connection. CMS Content/Design/Website System still require their destination modules and allowlisted operations; this checkpoint does not retire `/admin/`.

Final screen checkpoint validation: dashboard suite 53/53 plus migration replay, dashboard typecheck, production build and reporting browser regression passed. The existing MapLibre bundle-size warning remains. The revocation browser assertion was corrected to await the established Profile redirect rather than the transient unavailable heading; it confirms no reporting request occurs after revoked grants are loaded. No production or GitHub push occurred.

## Retained CMS API checkpoint

Added explicit dashboard `/api/v1/marketing/cms` method/path operations for Pages (including publishing, scheduling, duplication, revisions, preview-link and relationship actions), Sections, Galleries, Menus, Sidebars, SEO/robots/redirects/audit, and Team. The operation registry in `artifacts/api-server/src/dashboard/marketing-cms.transport.ts` is the current transport contract. Record identifiers are single segments; only the original gallery search/status/sort and page force parameters are accepted. Core remains the only owner of CMS records and mutation validation. No CMS database copy or alternate publishing engine was introduced.

Each request checks canonical dashboard grants and forwards a short-lived user/session grant to the fixed Core `/api/integrations/business-center/cms` service ingress. Core independently resolves current identity, suspension and grants, then invokes the existing route handlers. The service key authenticates the dashboard process only; it cannot confer user permissions. Ordinary browser cookies, Origin and Fetch Metadata headers are rejected at ingress. Both reporting and CMS share that service authentication implementation. Request/response bodies are bounded at 8 MiB, redirects and automatic mutation retries are disabled, and upstream cookies/headers/internal failures are not exposed. Domain validation and conflict payloads are retained.

The retained CMS routes now require their exact Marketing leaf grants even through `/api/admin/cms`. Media routes likewise require Media access instead of accepting blanket Content/Design/CRM. SEO governs redirects, robots and the existing summarized cross-content audit. Public published reads are unchanged; authenticated draft preview now also requires Pages and its existing preview token. Page title/slug changes still synchronize derived menu links. Explicit removal of linked navigation items requires both Pages and Menus. Cross-tool picker references must be implemented deliberately as the editors move, rather than weakening the maintenance grants.

This checkpoint does not mount CMS editors in the dashboard. Multipart/binary Media transport, Website/Blog/Forms/Events/Careers, Design, Owner-only Website System, editor locks, minimized selector references, typed frontend clients, comprehensive OpenAPI and editor parity remain required. The initial capability list omitted the feature-gated Events/Careers areas; include them in the remaining inventory so disabled tools are preserved without enabling them in production. No legacy UI retirement or live CMS mutation has occurred.

Validation: dashboard suite 56/56 plus disposable migration replay, 26 focused Core route/upload tests, API and Core typechecks, and dashboard API production bundle passed. Tests invoke retained handlers over HTTP with synthetic storage and fresh identity fixtures, cover every exposed method/path denial on both surfaces, independent tool reads, page publication identity, linked-menu removal, revoked/suspended/crew/client/unattested Owner access, draft preview gating, transport path/query/body limits and existing upload protections. Production provider/storage operations were not invoked. Complete CMS editor browser parity and live release rehearsal remain outstanding.

## Editor reservation migration checkpoint

Editor-lock reads, listings, acquisition, heartbeat and release now require the canonical grant matching the resource: Pages, Blog, Events, Forms, Sections, Menus or Sidebars. Website documentation and email-template locks require an active, attested Owner. Local Core admin/editor roles alone no longer authorize lock operations. This check applies to retained body-scoped legacy routes as well as new path-scoped service routes. The dashboard exposes a fixed method/path entry for each resource/action; it does not let a body select a more privileged lock type. Responses are private/no-store.

The existing Core lock store, linked-user ownership, conflict responses, 30-second heartbeat and five-minute expiry remain intact. Revoked grants prevent renewal/release; expiry makes an abandoned reservation available again. These remain the existing advisory editor reservations, not a new transactional write-lock guarantee. Existing save-conflict behavior and editor UI still need their full integration verification.

Events and Careers now have distinct leaf checkboxes in the canonical permission vocabulary. No existing non-Owner receives them automatically, and no site feature flag was enabled. Their full tools remain in the destination inventory.

The dashboard service connection now supports all nine existing lock resource types. Lock operations are mounted independently of the CMS feature flag because Docs/email templates and feature-gated Events/Forms are not CMS page tools. The destination editor still needs the retained component tree, media/picker connections, route adaptation and browser parity before it can be considered migrated. No CMS screen has been declared complete by this reservation checkpoint.

Validation: 17 Core reservation/service-ingress tests and the 56-test disposable dashboard suite plus migration replay passed. Core, API, dashboard and shared-library typechecks passed, as did the dashboard API production bundle. Tests exercise actual lock handlers and service logic with synthetic storage: ownership collisions, both route formats, resource isolation, Owner attestation, revocation and expiry recovery. Initial typechecks identified the new Events/Careers leaves missing from the OpenAPI enum and stale shared build declarations; the specification/generated client and shared build were synchronized before the successful final checks. No production data, Blythe files or release settings were changed.

## Native Marketing menu editor checkpoint

`/marketing/content/menus` is now a lazy-loaded dashboard screen, protected by `marketing.content.menus`. Marketing navigation has Content and Reporting section labels above the main Settings area. The menu UI adapts the existing menu editor behavior to the dashboard's semantic theme and uses the original Core CRUD handlers and records. It supports all current and legacy location assignments, custom links, page links with synchronized or custom labels, form popups and their titles/descriptions, new-tab behavior, up to three nested levels, drag/keyboard ordering, promotion to the top level, creation and deletion. Assignment to an occupied location explicitly identifies the menu being replaced.

An independently authorized `/menu-references` response supplies only page ID/title/slug/status and form ID/name/slug. Menus-only users can populate selectors without reading full page drafts, form definitions or submissions. Typed OpenAPI menu/reference/reservation DTOs and generated API functions are used by the screen; no Core credentials or separate sign-in are present in the browser. The remaining CMS operations still need their comprehensive typed contracts as their screens move.

Existing-menu edits acquire and renew the retained reservation, recheck it before save, retain the draft on conflict and release on unmount. Internal navigation and browser unload offer unsaved-change protection; canonical access revocation overrides it and unmounts the editor. Read requests are aborted at identity/route unmount. Saving still follows the original Core menu semantics and is not claimed to add transactional optimistic concurrency. The original page builder and other CMS screens remain to be integrated; this screen does not retire `/admin/`.

Validation: native-menu browser test passed with synthetic intercepted responses for creation, nested page/form composition, promotion/reordering, minimized request sources, unsaved navigation cancellation, lock conflict draft retention, mobile containment and revoked access. The existing reporting browser regression also passed after shared navigation changes. Mobile render was inspected after waiting for the sidebar transition. Dashboard suite 56/56 plus migration replay, 10 Core CMS tests including reference projection, Core/API/dashboard/shared typechecks, and dashboard production build passed. The existing MapLibre size warning remains. No production CMS writes, deployment or GitHub push occurred.

## Native P1 Website editor checkpoint

Marketing → Content → Website now exposes the existing manifest-bounded P1 content editor natively at `/marketing/content/website`, including direct component links, catalog/field search, draft save, explicit saved-revision publication, history and restore-as-draft. Generated dashboard contracts call the retained Core workflow through six fixed service operations. Core remains the content owner; expected-revision conflicts retain local edits and do not retry writes. Both legacy and service Website routes require `marketing.content.website`; local admin/content-editor roles alone confer no access. Navigation/unload guards protect unsaved edits.

Source conflict resolved in navigation/copy: the public P1 site reads published client-site-content snapshots, including shared `site-chrome` navigation and page SEO. Generic CMS menus/SEO settings are separate retained tools; changing them must not be represented as updating primary P1 navigation/SEO. The Website editor preserves this publication boundary. Generic sitemap previews must not be labeled as the actual P1 runtime sitemap. Further generic CMS migration remains outstanding.

Preview adaptation retains source-window, exact origin, stack/route/component identity and revision checks. The public site additionally accepts the exact `https://dashboard.p1landmanagement.com` parent. Only recognized public `cmsPreview` documents allow that frame ancestor; ordinary pages retain SAMEORIGIN/self restrictions. Preview responses remain private/no-store and noindex; analytics excludes them. The iframe uses the manifest's HTTPS preview URL, scripts/same-origin sandbox and exact target origin. No production content or framing configuration was changed. Production preview requires the separately authorized public-site release; the local UI browser fixture mocks its iframe.

Validation: 33 focused Core route/workflow/preview tests, 56 dashboard tests with disposable migration replay, 29 public runtime tests and two analytics tests passed. Core/API/dashboard/public-site typechecks and dashboard/public-site builds passed. Website editor browser checks cover direct links, unsaved-navigation cancellation, stale-write recovery, exact revision payloads, publication, restore without republishing, mobile containment and revoked access. Desktop/mobile screenshots were inspected. Existing public build sourcemap warnings and MapLibre chunk-size warning remain. No full consolidation completion, live cross-origin preview claim, legacy retirement, GitHub push or production cutover.

## Media transport checkpoint

The fixed CMS operation registry now includes Media list, metadata/alt PATCH, delete, multipart upload/replacement and binary source reads. Browser authorization precedes raw upload parsing; the dashboard bounds multipart envelopes at 11 MiB and Core retains its original 10 MiB file limit, type/extension checks, image decoding/optimization, upload freeze policy and storage ownership. Fresh canonical identity is independently checked at Core ingress. No browser cookies, destinations or arbitrary headers pass through. Source streams are bounded at 11 MiB and returned private/no-store, nosniff and attachment; only PNG/JPEG/WebP/GIF retain image MIME types, other sources become octet-stream. Metadata stays JSON. No mutation retries were introduced.

This is transport groundwork for the native Media library and pickers; those interfaces, crop/replace parity, typed client/OpenAPI coverage and end-to-end real-storage rehearsal remain outstanding. No live files were uploaded, replaced or deleted. Transport tests use synthetic byte buffers; service-route positives mock the storage-producing helper/download. Existing independent file-validation and upload-freeze tests exercise the real safeguards.

During inspection, the dashboard's production CSP was found to inherit `default-src self` for frames, which would block the newly moved Website preview despite the public site's correct frame-ancestor policy. It now explicitly allows only `https://www.p1landmanagement.com` as frame source, retaining `frame-ancestors none`. A production-server integration assertion covers that header; the earlier Vite browser fixture could not detect this server-only restriction.

Validation for this checkpoint: 58 dashboard tests plus disposable migration replay passed, including production CSP and all added Media method/grant denials; 32 focused Core tests passed across service ingress, actual upload validation, freeze policy and usage discovery (13 ingress tests after the final positive fixture was added). Core and API typechecks passed. Media UI is not yet present, so no Media browser parity is claimed.

## Native Media library and Website image picker checkpoint

Marketing → Content → Media now mounts a native, independently permissioned library. It retains filename/title/alt/caption/description search, image/document and live/draft/no-known-reference filters, eight sort orders, multi-file uploads with the existing 10 MiB limit, metadata/SEO/social fields, URL copy, authenticated downloads, explicit deletion confirmation and crop/replace. Cropping supports pointer selection and keyboard-accessible percentage coordinates, bounded image coordinates, WebP output capped at 1200 pixels and explicit replacement confirmation; it adds no new package dependency. Unsaved metadata has navigation/unload protection. Failed batch uploads retain earlier successful files and reload the library.

Typed OpenAPI/generated clients now cover Media list, metadata, upload, replace, delete and binary source reads. Previews and crops use authenticated source endpoints, never arbitrary browser-selected fetch destinations. The Website editor offers the shared picker only with the independent Media grant; unsupported MIME types and image origins are unavailable for selection, and the retained server manifest validation still governs saves. A selected image only changes the local Website draft. Publication remains explicit.

Usage discovery now includes current draft and published Website snapshots, separately identified by snapshot field on the actual content record. It returns reference metadata rather than content bodies. This closes a pre-existing gap where assets referenced by the primary P1 Website could appear unused. Usage remains advisory: external references and unsaved edits are not a deletion guarantee. Existing generic content usage discovery and storage ownership remain intact.

Validation: 59 dashboard tests plus disposable migration replay passed; generated Media client tests verify paths, multipart filename/bytes, metadata and Blob responses. Focused Core usage/ingress tests passed (17 tests) and Core typecheck passed. Dashboard typecheck/build passed, retaining the existing MapLibre size warning. Native browser fixtures verify type/search filtering, metadata draft protection and save, crop multipart replacement, uploads, delete cancellation/confirmation, mobile containment, revoked access, Website picker selection, disallowed origins and removal of the picker without Media authority. Desktop/mobile screenshots were inspected with synthetic image data. Browser fixtures mock storage and provider calls; real-storage release rehearsal and full consolidation parity remain outstanding. No production assets or deployments were changed, and no GitHub push occurred.

## Native Team management and CMS rich-text editor checkpoint

Marketing → Content → Team now lists, searches, filters, creates and updates website Team profiles with the canonical Team grant. Name/title, portrait URL and alt, excerpt, rich biography and draft/published/archived state retain their Core contracts. Publication has an explicit confirmation, archival retains the record, and unsaved edits are guarded. Team profiles do not create staff accounts or grant access. Member ordering remains owned by each CMS Team section; it is not a new global staff order. Core keeps record storage, biography sanitization and activity attribution to the linked local actor.

The CMS biography editor uses the retained Core TipTap 3.20.1 engine, with visual/HTML modes, headings, emphasis, lists, quotes, alignment, links/new-tab attributes, embedded images/alignment, and undo/redo. The dashboard's older plain-text editor is unchanged. Media pickers for portraits and embedded images require separate Media access; manual URLs and stored HTML remain available without Media authority. Relative image previews resolve to the P1 public origin without rewriting stored HTML. Dashboard image CSP adds only that exact public origin; other external image URLs can be retained but are not guaranteed to preview under the restricted dashboard policy. Final cross-origin asset parity remains part of release review.

Dependency decision: reuse Core's existing MIT-licensed TipTap family rather than add a competing editor. Direct and transitive TipTap packages are pinned to the same 3.20.1 release; the first install exposed newer incompatible transitive peers, resolved with explicit family overrides. `pnpm peers check` then reported no peer issues. The editor loads with the Team chunk (approximately 388 kB uncompressed / 123 kB gzip), rather than the initial dashboard bundle.

The existing public HTML sanitizer stripped the editor's text alignment. It now preserves only `text-align: left|center|right` on paragraphs/headings. Other style properties, executable markup and unsafe URLs remain stripped; regression tests cover this narrow policy extension. Visual inspection also corrected selected-toolbar contrast.

Validation: native browser checks cover rich formatting/HTML round-trip, portrait selection/alt, canceled unsaved discard, publish confirmation, archive/reopen, independent Media denial, revoked Team access, and mobile containment. Desktop/mobile views were inspected. The initial development-browser run hit a Vite dependency-optimization reload error; a fresh run after optimization passed without page errors. Focused Core Team contract/rendering/service tests passed (25), followed by 11 sanitizer/security tests after the alignment change. Dashboard/Core typechecks passed. All fixtures use synthetic records/assets and mocked browser API responses; no public Team records were changed. Full consolidation completion and GitHub push remain outstanding.

Final checkpoint checks: 59 dashboard tests and disposable migration replay passed after the image CSP assertion; the dashboard production build passed with the existing MapLibre warning. The Team browser fixture also verifies embedded-image alignment and preservation of its original relative URL through HTML/visual modes.

## Blog service and typed contract checkpoint

The dashboard CMS allowlist now exposes all retained Blog post CRUD, taxonomy CRUD, comment moderation and comment-settings operations under `marketing.content.blog`. The same leaf is enforced inside the retained Blog router and at its legacy admin mount; broad Content or local admin roles do not bypass it. Blog remains behind its existing independent `blogEnabled` flag and is mounted before the general CMS feature gate. No disabled feature was enabled. Scheduling still rejects past dates, forces scheduled posts to unpublished with no published timestamp, and clears scheduling when publishing. Comment filters accept only one known scalar status, and static references/comment routes are registered before post-ID routes.

A Blog-only reference endpoint returns sidebar IDs and names without sidebar content or maintenance authority. Existing post author names remain content fields, not staff-account lookups. Taxonomy reads retain their existing legacy-name reconciliation behavior. An explicit `parentId: null` now detaches a category; omitted parent fields retain the old parent. Previously null-coalescing prevented detachment. This distinction is covered by a retained-handler test.

OpenAPI/generated clients cover posts (including SEO, focal points, article/podcast/external content fields, categories/tags, sidebar and publication dates), category/tag administration, moderation records/status/filtering, and all comment settings. Partial update contracts preserve explicit nulls. Core remains the owner of records, validation, scheduled publication and public comments. The native Blog editor/settings/moderation screens and their end-to-end parity are still outstanding; this checkpoint does not make generic Core Blog posts the primary P1 public site's content source or change its manifest publication workflow.

Validation: 18 focused Core service-route tests passed, covering every Blog operation denial on service and retained routes, publication scheduling, feature isolation, minimized selectors, moderation, taxonomy conflicts and parent detachment. Core/API/shared typechecks passed. No live posts, categories, comments, provider settings or production deployments were changed.

Final dashboard checks: 61 tests plus disposable migration replay passed, including generated Blog-client verification of scheduling nulls, category detachment and moderation query serialization. Semantic comparison of the OpenAPI document confirmed that existing schemas and paths are unchanged; the new Blog definitions are additive.

## Native Blog workspace checkpoint

Marketing → Content → Blog now mounts a lazy native workspace behind the Blog capability. It includes searchable post lists, draft/published/scheduled filters, post create/edit/delete, article/podcast/external fields, author, excerpt, cover and focal-point preview, categories/tags, sidebar selection, SEO/social fields, noindex and explicit publication/scheduling. Existing posts acquire and heartbeat the retained advisory editor reservation; saves verify ownership. Unsaved edits use the dashboard navigation/unload guard. Reservation writes are still advisory, not transaction-enforced locking.

The shared retained TipTap editor adds Blog heading 1, code, code block, divider, emoji and published-gallery shortcode controls while preserving visual/HTML modes. Blog references now return published gallery IDs/titles alongside sidebar IDs/names; draft galleries and gallery bodies remain excluded. Media selection retains its separate grant. Arbitrary external images remain subject to the existing restricted preview CSP.

Separate Blog panels expose category/tag names, slugs, hierarchy, order and deletion; comment body/note editing, moderation states and deletion; and every retained comment setting. Updating one comment preserves unsaved drafts in other cards. Category parent changes reject descendant cycles server-side, in addition to filtering invalid parent choices in the UI. This closes an existing unsafe hierarchy edge case. Taxonomy deletion retains Core's reference cleanup behavior.

Validation: 61 dashboard tests and disposable migration replay passed. All 19 focused Core service tests passed, including descendant-cycle rejection and minimized published-gallery selectors. Dashboard and Core typechecks passed; the dashboard production build passed with the existing MapLibre size warning. Synthetic browser checks passed for new post creation, HTML/gallery shortcode persistence, future scheduling, explicit category detachment, independent comment draft preservation, moderation, settings and mobile containment. Browser fixtures mock API calls and do not change live records. The new post editor still needs broader visual/accessibility and failure/revocation parity review; generic Blog public-source reconciliation and the rest of the consolidation remain outstanding. No GitHub push or production deployment occurred.

## Native SEO workspace checkpoint

Marketing → Content → SEO now provides native global defaults, social/organization fields, robots override/reset, redirect create/edit/activate/deactivate/delete, and the retained CMS metadata audit. The SEO leaf controls navigation and all server operations; image pickers require the separate Media leaf. Defaults and robots saves preserve their separate contracts so saving defaults cannot accidentally clear a custom robots override. Unsaved edits have navigation/unload protection. Enabling global noindex and applying/resetting robots content require explicit confirmation.

The Sitemap and robots panel links to the actual P1 public sitemap/robots endpoints. It deliberately does not reproduce the legacy client-side sitemap prediction, which combined generic CMS records and unrelated static routes. The panel explains the source distinction; the audit remains a metadata-only report over retained CMS pages, Blog posts and Events. Primary P1 page SEO continues through the Website manifest publication flow. Public-source reconciliation and broader cross-module edit links remain in the final parity work.

Additive OpenAPI/generated contracts cover settings, robots, redirects and audit projections. No persistent schema or public publication behavior changed. Validation: 61 dashboard tests and disposable migration replay passed; 20 focused Core bridge tests passed, including custom robots normalization/reset and audit body minimization with independent page denial. Dashboard/shared typechecks and production build passed; existing MapLibre chunk warning remains. Synthetic browser checks cover defaults saves, dirty-navigation cancellation, independent Media denial, robots override/reset, redirect creation/deactivation/deletion, minimized audit calls, mobile containment and revoked SEO access. Mobile output was visually inspected. No live settings or redirects were changed; no GitHub push or production deployment occurred.

## Native Sidebars checkpoint

Marketing → Content → Sidebars now provides native listing/search, create/edit/delete, description/default selection, all eight retained widget types, widget titles/settings, removal and keyboard-accessible ordering. Form/newsletter selectors preserve their original eligibility rules. Existing widget settings remain intact when editing neighboring values. Custom HTML stays editable text; the dashboard does not execute it. Saving the default uses the retained Core behavior and storage.

Existing sidebar edits acquire/heartbeat/verify the retained advisory reservation; a conflict leaves the user's draft visible and recoverable. Unsaved navigation is guarded. The reservation remains advisory rather than a transactional concurrency guarantee, matching the existing editor's contract. Final cutover concurrency/restore rehearsal remains outstanding.

A Sidebar-specific form reference endpoint returns only ID/name/slug/kind under the Sidebar capability. It does not require broader Forms access or expose rules, recipient settings or submissions. Typed sidebar CRUD/reference/reservation clients and the dashboard bridge allowlist are additive; Core still owns validation and persistence.

Validation: 61 dashboard tests with disposable migration replay, 21 Core bridge tests, Dashboard/Core/shared typechecks and the production dashboard build passed. The existing MapLibre size warning remains. Synthetic browser checks exercise widget fields/order, HTML persistence, default selection, exclusion of application forms, unsaved-navigation cancellation, reservation conflict/recovery with draft preservation, mobile containment, deletion and revoked access. Mobile output was visually inspected. No live sidebars or forms changed. Full consolidation and final GitHub push remain outstanding; no production deployment occurred.

## Gallery contract and Sidebar reference preservation checkpoint

Additive gallery OpenAPI/generated clients now describe list filters, create/read/update/delete, duplicate/publish/unpublish, five layouts, all retained display settings and item metadata (image/media, alt/title/caption, links/CTA, tags and ordering). The native Gallery workspace remains outstanding. Retained gallery validation now rejects slugs that normalize to empty or separators before querying or writing storage; valid slugs and public routes are unchanged.

Sidebar form selectors now keep a saved reference visible when changing widget type makes that form ineligible for new selection. Previously the underlying reference survived but its select appeared empty. The fix uses the same eligibility predicate for choices and saved-reference fallback, preserving explicit user control.

Validation: Sidebar browser regression passed, including switching a Contact form widget to Newsletter and back without losing the saved reference. Dashboard/Core/shared typechecks passed. All 22 focused Core route tests passed, including punctuation/separator-only slug rejection. An initial test invocation from the repository root failed the existing Website manifest lookup because that suite depends on the Core working directory; rerunning from Core passed. No native gallery UI or end-to-end gallery parity is claimed at this checkpoint. No live records, deployment or GitHub push changed.

## Native Gallery editing checkpoint

Marketing → Content → Galleries now exposes searchable/filterable/sortable galleries, draft creation, all five layouts and retained display settings, item URLs/Media selection, alt/title/caption/link/CTA/tags, keyboard ordering and removal, draft/published/archived state, saved-record publish/unpublish/duplicate and deletion. Item order is serialized explicitly. Changing an image URL detaches its old Media ID. Media selection requires independent Media access. Publication and deletion require confirmation; unsaved navigation is guarded, and saved-record actions are unavailable until the draft is saved. Core remains the storage/publication owner.

Validation: native browser checks passed for layout settings, metadata/order preservation, dirty guard, independent Media denial, publish/unpublish, duplication, deletion and mobile containment. Synthetic image data and mocked API calls were used; mobile output was visually inspected. Dashboard typecheck and production build passed, with the existing MapLibre warning. All 61 dashboard tests plus disposable migration replay passed.

This is an editing checkpoint, not full Gallery parity: the retained full GalleryRenderer preview and bulk image-upload interaction remain to be ported and verified, along with permission-revocation/failure coverage and cross-origin preview review. No live gallery or Media records were changed. Full consolidation, GitHub push and production release remain outstanding.

## Gallery draft preview and bulk upload checkpoint

The Gallery workspace now includes a local draft preview for grid, masonry, carousel, slider and featured layouts. It adapts the retained renderer's settings and behavior without fetching public content: responsive column counts, image limits/ratio/crop/radius, caption/title position, hover/slide effects, arrow colors, thumbnail/slide selection and lightbox navigation. The lightbox uses a native dialog with focus handling and Escape/arrow-key controls; reduced-motion preferences disable animation. CTA labels expose their destination as text without navigating away from unsaved edits. Custom class names are retained, but public-only CSS and arbitrary external image origins still need final cross-site parity review.

Gallery editors with the separate Media capability can upload multiple PNG/JPEG/WebP/GIF images through the existing bounded Media endpoint. Successful assets append to the current draft with Media ID and metadata; a later failure retains earlier successful assets and explains that behavior. Uploads use an AbortController canceled on editor unmount. No upload publishes or saves the gallery automatically.

Validation: expanded synthetic browser checks passed for slider navigation, keyboard lightbox navigation/Escape, partial bulk-upload failure preserving the first asset, and the existing gallery editing/publication/mobile checks. Draft preview output was visually inspected. Dashboard typecheck and production build passed with the existing MapLibre warning. The preview is an adaptation of the retained renderer, not a claim of pixel-identical public rendering; full visual/asset parity and remaining consolidation gates remain outstanding. No live uploads, publication, GitHub push or deployment occurred.

## Shared block catalog and Sections contract checkpoint

Moved the four data-only block-registry modules into Core's shared/cms-builder directory. The retained admin paths re-export the canonical modules, preserving consumers and tests. Byte-for-byte comparison confirmed the moved definitions/defaults/helpers are unchanged. This provides one source of truth for retained and Business Center block editors instead of a second registry.

A Sections-scoped builder endpoint now returns that catalog and legacy aliases plus minimized page/form/gallery/team selectors. Only published galleries/team profiles are offered, and bodies, biographies, gallery items, form fields/recipient settings and submissions are excluded. Sections permission does not grant access to the underlying editors. Typed contracts cover section CRUD, unchanged arbitrary block payload preservation, starter refresh result counts, builder property definitions and advisory reservations. Starter refresh retains its original behavior, including refreshing/removing system starter records; no refresh was executed against live data. The native visual Sections editor is still outstanding.

Validation: 23 service tests and 2 registry tests passed; retained Section editor and all-block inspector regression tests passed (2). Core/Dashboard/API/shared typechecks passed. Core client/server production build passed with existing PostCSS and chunk-size warnings. Semantic OpenAPI comparison confirmed previously existing paths/schemas are unchanged. An initial root-level `npm run check` had no matching script; checks were then run from the correct Core directory. No production settings/content, GitHub push or deployment changed.

## Native Section property editor checkpoint

Marketing → Content → Sections now mounts a native list/search/category workspace and retained section CRUD with advisory reservations. The editor consumes the canonical block catalog, adds defaults by deep copy, preserves legacy type aliases, orders/duplicates/removes blocks and edits nested array items, rich text, basic fields, page/form/gallery/team references and Media-gated images. Unknown blocks and properties remain in the payload when neighboring values change. Saved references absent from current selector choices remain visible. Rich-text editing respects the reservation/busy state as well as disabled native controls.

Starter refresh is exposed with an explicit explanation/confirmation of its retained overwrite/removal behavior; no starter refresh was run on live data. Saving Sections does not publish pages. The visual page-builder canvas, full block rendering previews, conditional button/link field behavior and comprehensive all-block interaction parity are still outstanding; this checkpoint is property editing, not completion of that migration.

Validation: synthetic browser checks passed for nested-property editing, unknown data/block preservation, missing form references, ordering, deep duplication with new IDs, canceled dirty navigation and mobile containment. Mobile output was inspected. Dashboard typecheck and build passed. The initial dashboard chunk now slightly exceeds 500 kB uncompressed (about 149 kB gzip), alongside the existing larger MapLibre chunk; final bundle review remains required. No live section changes, GitHub push or deployment occurred.

Checkpoint regression suite: all 61 dashboard tests and disposable migration replay passed.

## Shared conditional block fields checkpoint

Extracted the retained button-action and rich-text field rules into a data-only shared helper consumed by both editors. The dashboard now interprets legacy `url` actions as internal/custom links without rewriting their stored value, offers page references for internal button links, switches link/form-modal fields according to the selected action, applies the retained dynamic labels, and uses rich-text editing for subtitle/subheading/answer fields. Hidden alternative values remain in the block payload. Nested array inspectors use the same rules.

Validation: the expanded Sections browser regression passed, including legacy action display, internal page selection, switching to a form modal and preserving the hidden saved URL. Five focused shared-helper/retained-editor tests passed. The first retained-editor run found an inspector-grouping dependency on the moved controller helper; exporting/importing that helper restored the existing inspector, and Core typecheck plus all focused regressions then passed. Dashboard typecheck and build passed with existing size warnings. The full visual canvas, preview, fallback editor details and remaining consolidation requirements are still outstanding. No public content or deployment changed.

## Legacy block compatibility editor checkpoint

Moved the retained fallback block-definition inference into a shared helper, preserving its old import/export entry point. Native Sections now offers the same compatibility controls for unknown block types instead of leaving those records entirely read-only. Text, booleans, numbers, image/link fields and long/HTML text use the retained inference rules. Unsupported objects, arrays and null values remain in the payload unchanged; this does not invent a new block schema.

Validation: the Sections browser fixture now edits a legacy scalar field and verifies the unknown nested object survives alongside all prior ordering/reference/duplication checks. Shared inference and retained all-block inspector tests passed (2). Core/Dashboard typechecks and dashboard production build passed with existing chunk warnings. A first attempt to write the test used a root-relative path while in Core and created no file; the test was then written at the correct path and explicitly run. Native canvas/full rendering and remaining consolidation work are still outstanding. No production writes or GitHub push occurred.

## Builder preview transport checkpoint

Added a shared, versioned draft-preview envelope for the planned Core-rendered Marketing canvas. Acceptance requires an exact canonical HTTP(S) origin, the expected window identity, a per-frame UUID channel and an increasing revision. Draft blocks retain unknown fields and legacy properties. Validation rejects duplicate block IDs, unsafe object keys, cycles, getters, non-JSON values, excessive nesting/node counts, oversized sparse arrays and payloads over 1 MiB; previews are limited to 200 blocks. No storage read, save or publication authority is conferred by this message.

Validation: two focused protocol tests passed using the installed Core Vitest, and Core TypeScript checking passed. An earlier root invocation used an ephemeral Vitest version; the installed Core runner was subsequently used successfully. This checkpoint does not expose a preview endpoint or connect a renderer. The retained renderer has HTML insertion points and dynamic components, while the normal Core app starts account/analytics integrations. Renderer integration therefore still needs sanitization, interaction isolation, explicit frame-origin policy and runtime verification before enabling the canvas. No production headers, live data, GitHub push or deployment changed.

## Shared preview sanitization checkpoint

Moved the existing public rich-HTML sanitizer into a shared module, preserving the server import entry point and its policy unchanged. The retained builder preview now sanitizes a memoized rendering copy before passing blocks to the public renderer. The preview adapter also covers nested `subtitle` values rendered by SectionHeading. The original editor data, unknown properties and save payload remain unchanged. This prepares the same HTML policy for the consolidated preview without introducing a second sanitizer implementation or dependency.

Validation: the sanitizer/draft-preservation test, retained preview-document test and eight existing server security regressions passed. Core TypeScript checking and the client/server production build passed. A repeatable synthetic Chrome test bundles the sanitizer for the browser, inserts its output, checks that scripts/event attributes and unsupported positioning styles are removed, verifies supported formatting and original draft preservation, and confirms no page errors or click-triggered script execution. The existing PostCSS and chunk warnings remain; the Core vendor chunk is now approximately 1.07 MB uncompressed/362 kB gzip, requiring the planned final bundle review.

This is HTML sanitization, not complete preview isolation. The cross-origin receiver, frame headers, origin configuration, disabled publication/submission/navigation behavior and full native canvas remain outstanding. No live content, production configuration, deployment or GitHub push changed.

## Core preview receiver checkpoint

Added a renderer-only preview receiver that uses the retained PublicPageRenderer and shared sanitization. It listens before issuing an exact-origin ready handshake, accepts only the configured parent window/channel and increasing revisions, clears drafts when the session changes, and removes its listener on unmount. A revision-keyed error boundary lets a subsequent corrected draft recover from a malformed block. The rendered subtree is inert, with click/submit capture guards, so it cannot be activated through normal pointer or keyboard interaction.

Added the dedicated preview fetch policy for the forthcoming isolated entry point. It allows only enumerated public-content GET endpoints, strips caller headers/cookies, omits credentials, rejects redirects and cross-origin requests, and renders account lookups as an anonymous local result without requesting the Core account. Explicit fetch calls in retained dynamic components require this runtime policy in addition to QueryClient defaults.

Validation: five receiver, message-boundary and request-policy tests passed; the receiver test renders the actual retained raw-HTML block and checks sanitization, inert state, stale messages and channel replacement. The first fixture used an unsupported block name and was corrected to the actual registry key; the corrected test passed. Core TypeScript checking passed. The route/bootstrap configuration, network-policy installation, CSP/frame headers, dashboard frame sender and real-browser end-to-end verification are still outstanding. Neither the receiver nor fetch policy is exposed as a live route at this checkpoint. No production data/configuration, GitHub push or deployment changed.

## Isolated Core preview route checkpoint

Core now has a dedicated `/cms-preview/builder` document with an isolated client entry. It mounts the retained renderer, public branding and a separate query cache after installing the anonymous public-read policy. It never mounts the normal App/setup/auth/analytics/consent managers. Trusted parent configuration is injected by the server; the session channel stays in the fragment. Standalone or malformed sessions show an unavailable message.

The route is disabled by default. Enabling `CORE_BUILDER_PREVIEW_ENABLED=true` requires valid existing federation configuration; the frame ancestor is exclusively the validated `DASHBOARD_FEDERATION_ISSUER`. Request Origin/Host/query values cannot change it. Only this document receives its frame policy: private/no-store, noindex, no referrer, sandboxed scripts, no nested frames, no forms, and restricted script/connect origins. Existing admin framing remains unchanged. Static production and Vite development document handlers inject the same configuration.

Release preparation must route `/cms-preview/builder` to Core through the existing gateway, preserve Core asset/public-API routing, and enable it only after the dashboard sender/origin configuration and final browser review are complete. This code does not change gateway or production configuration. The new path is outside the legacy `/admin/` control area; existing compiled Core asset URLs remain under their current prefix until the retirement rollout.

Validation: 17 focused receiver/request-policy/security-header tests passed, including disabled/misconfigured behavior, forged-origin rejection and unchanged admin framing. Core typechecking and production build passed with existing PostCSS/chunk warnings. The new repeatable Chrome fixture serves the built artifacts across two synthetic local origins using the actual header middleware, completes the handshake, renders sanitized drafts and the dynamic recordings block, rejects stale revisions and submission fetches, and verifies public reads omit cookies and never call account/setup/analytics/purchase endpoints. No production writes, deployment or GitHub push occurred. The dashboard frame sender and remaining consolidation work are outstanding.

## Native Sections preview integration checkpoint

The Sections editor now opens the isolated Core renderer with desktop (1280), tablet (834) and mobile (430) viewports. It sends current unsaved blocks only after the exact frame/origin/channel/version handshake; updates carry increasing revisions, and retry creates a fresh session. Closing or navigating away removes listeners/timers. Payload failures and connection timeouts leave the editor draft intact and display an actionable status. Missing preview configuration does not block editing or saving. Frame scrolling stays inside the panel on narrow screens.

The Sections-scoped builder projection now exposes an optional nullable preview URL only when Core's isolated route is enabled, using the validated Core APP_URL. The OpenAPI/client contract is additive. Dashboard frame-src retains the public Website origin and adds only the fully configured exact CMS connection origin. Wildcard hosts are rejected for preview framing and federation issuer/origin configuration. Deployment must align Core APP_URL with the reachable origin permitted by the dashboard's CORE_MARKETING_ORIGIN and route the isolated preview path before enabling it. No production configuration changed.

Validation: 62 dashboard tests and disposable migration replay passed; 24 Core bridge tests and two preview-policy tests passed; Core/Dashboard/API/shared typechecks and the dashboard production build passed. Existing chunk warnings remain. The expanded synthetic Sections browser check covers the handshake, live edits, device widths, a fresh retry session, existing save/draft/legacy-field behavior and mobile containment with the preview open. Mobile output was inspected. This sender fixture complements the prior built-Core cross-origin browser test; production gateway/cutover verification remains a release gate. The visual canvas and broader page-builder parity, remaining modules, migration and full consolidation validation are still outstanding. No GitHub push or deployment occurred.

## Saved section insertion checkpoint

The native Sections editor now includes a searchable/category-filtered saved-section library and a position selector shared by new-block and saved-section insertion. Inserting a section copies its block data with fresh IDs and preserves unknown/nested properties; subsequent changes do not link back to the saved source. The library uses the existing Sections capability/API, cancels loading on unmount, reports load errors without changing the draft and disables insertion while the reservation or editor state prevents writes. Searching with Enter does not submit the outer editor form.

Extracted the retained dynamic-system-starter exclusion and block-copy operation into shared helpers used by both editors. Dynamic classification comes from each editor's canonical catalog rather than bundling another registry in the dashboard. The retained editor now also deep-copies nested source data when assigning fresh IDs. Inspection confirmed undo/redo belongs to the existing rich-text editor and is already present; no speculative document-history feature was added.

Validation: four shared-helper/retained-editor tests passed, Core/Dashboard typechecks and dashboard production build passed with existing chunk warnings. Expanded synthetic browser coverage passed for source filtering, insertion before the first block, fresh IDs, retained unknown/nested data, source preservation and existing preview/save/mobile behavior. The initial browser run exposed an ambiguous select label; explicit accessible labels corrected it and the rerun passed. The saved-library UI was visually inspected. Full canvas interaction and remaining consolidation requirements remain outstanding. No schema, production data, GitHub push or deployment changed.

## Generic Pages contract and builder-reference checkpoint

Added typed dashboard contracts/clients for generic CMS page list/create/read/update/delete, duplication, publish/unpublish/schedule, linked-menu inspection/removal, preview links, revision listing/restoration and page reservations. Arbitrary retained JSON content remains unknown/nullable rather than being coerced into a new block schema. Existing OpenAPI paths and schemas were compared semantically and remain unchanged.

The new Pages-scoped builder endpoint returns the canonical block catalog and minimized page/form/gallery/team/sidebar references. It shares the same projection service as Sections, while sidebar references are included only for Pages. No form fields/submissions, gallery items, biographies or sidebar widgets are disclosed. Reading these selectors does not grant access to the related management endpoints. CMS slug validation now reserves the isolated preview path and checks reserved prefixes after leading slashes, closing the rooted-path bypass for existing reserved routes.

Implementation note for the native Pages UI: these generic CMS records are distinct from primary Website manifest drafts. Saving a published generic page updates the same content record, and revision restoration updates title/content while retaining the current status and other metadata. The UI must make published-save/restore effects explicit; it must not imply that revisions restore SEO/template/sidebar metadata or create a separate unpublished working copy. Existing preview links and public routing need the final gateway/source reconciliation before retirement.

Validation: 26 Core bridge tests passed, including minimized Pages selectors, independent capability denial and reserved-slug rejection. The first slug fixture mistakenly supplied its payload as request headers; it was corrected to send JSON and then passed. All 62 dashboard tests and disposable migration replay passed. Core/Dashboard/API/shared typechecks passed. This checkpoint supplies contracts and data access; the native Pages workspace is still outstanding. No schema migration, live page publication, GitHub push or deployment occurred.

## Native generic CMS Pages checkpoint

Marketing → Content → CMS Pages now mounts under its independent Pages grant. The native workspace lists/searches/filters pages and provides draft creation, title/slug/type/layout/sidebar and SEO metadata editing, the shared block property editor, draft previews, duplication, publishing, scheduling, moving to draft, draft archival, deletion, linked-menu review/removal and revision restoration. Saved-record actions are disabled while edits are dirty. Published saves and content restores explicitly confirm their immediate effect; deletion and unpublication fetch fresh menu references and require confirmation before sending a force override. Menu removal requires the separate Menus grant. Media and saved-section insertion retain their independent grants.

Extracted the existing native block controls into CmsBlockEditor, consumed by both Pages and Sections. Reordering, deep duplication, compatibility fields, unknown/nested data preservation and saved-section insertion use the same implementation. Pages preserve unknown top-level content fields when updating blocks and keep unsupported legacy content shapes unchanged during metadata edits; legacy conversion is explicitly unavailable rather than silently clearing that content. The existing advisory reservation lifecycle verifies before writes and retains the draft on conflict. Capability changes remount the workspace.

Validation: native Pages browser checks passed for create/edit, retained fields and missing sidebar references, dirty-navigation cancellation, reservation conflict/recovery, published-save confirmation, publication/scheduling, menu-force review, revision restoration, duplication/deletion, archive/unarchive, mobile containment and revoked access. Separate Sections browser regression passed after extracting shared controls. Mobile Pages output was visually inspected. Dashboard typecheck/build and all 62 dashboard tests with disposable migration replay passed. Initial validation caught a missing view decoration mapping and stale section-specific preview copy; both were corrected and checks rerun. Existing bundle warnings remain (initial dashboard chunk about 504 kB uncompressed/149 kB gzip).

This is the native editing/publication checkpoint, not full Pages parity. Legacy format conversion, retained page starter-template selection, shareable preview-link/public-route reconciliation, all visual-canvas interactions and broad release/restore checks remain outstanding along with the rest of the consolidation plan. No live pages, production release, GitHub push or deployment changed.

## CMS page starter layouts checkpoint

CMS Pages now offers all five retained starter layouts through a lazy-loaded picker. Applying a starter changes only the unsaved block collection, preserves page metadata and unknown top-level content fields, asks before replacing existing blocks, respects the editor reservation, and participates in the existing discard/navigation guards. Starter copy, metrics, testimonials and links are explicitly identified as examples requiring verification before publication. No example was published.

Moved the existing template definitions and landing generator into a canonical shared module with a compatibility re-export for the retained admin. Template block creation now deep-copies registry defaults so nested edits cannot mutate later starter instances or shared defaults. The dashboard downloads that catalog only when the picker opens (about 62 kB uncompressed/14 kB gzip); the initial chunk remains about 504 kB uncompressed/150 kB gzip, with the existing bundle warnings.

Validation: the shared starter test passed for retained-export identity, actual block counts, known block types, fresh IDs and nested data independence. Core and Dashboard typechecks passed, and the dashboard production build passed. Expanded synthetic Pages browser checks passed for replacement cancellation, no implicit writes, discard, starter persistence, independent grants and mobile containment, alongside the existing publication/reservation/revision checks. The first browser run identified an ambiguous select label; an explicit accessible label fixed it. The mobile starter view was inspected. No migration, production data change, push or deployment occurred.

Further inspection confirmed the retained page parser does not actually convert arbitrary legacy content: it accepts block arrays, merges adjacent legacy join blocks, and otherwise returns empty content. The native editor deliberately preserves unsupported data rather than reproducing that clearing behavior. Any conversion requires an inventoried source format and lossless mapping. Landing generator UI, visual-canvas parity, public-route reconciliation and the broader consolidation requirements remain outstanding. GitHub push remains deferred until the entire goal is complete and validated, as requested.

## Landing page generator checkpoint

CMS Pages now includes the retained landing-page composition workflow in the lazy-loaded starter workspace: campaign goal/headline/introduction, audience selection, recommended or individually selected blocks, call-to-action text/link, and draft preview before applying. The native UI uses the shared generator and the existing isolated BuilderPreview instead of introducing another renderer. Changing a goal refreshes its recommended selections; chosen block order is shown, and normal page block controls remain available after applying.

Headline and explicit CTA values are required before progressing, audience and block selections gate later steps, and Enter in generator inputs cannot submit the enclosing page form. Applying uses the same replacement confirmation, draft-only update, metadata preservation, discard guard and reservation restrictions as starter layouts. No server write occurs until the normal Save action. Existing audience/template copy is identified as examples; none was published or asserted as verified P1 proof.

Validation: two shared tests pass, including all offered generator goals/block types, chosen order, fresh IDs and headline/CTA propagation. Expanded synthetic Pages browser checks pass for required-step gating, Enter without saving, replacement cancellation, applying without saving, saved generated content, unchanged page title, mobile containment and existing Pages behavior. Mobile generator output was inspected. Dashboard typecheck and production build pass with existing bundle warnings. No schema, production data, push or deployment changed. Full visual-canvas parity, legacy/source reconciliation, remaining CMS and User Manager modules, CRM/agreement integration and final migration/release validation remain part of the active goal.

## Save block as reusable section checkpoint

The shared native block editor now supports saving an individual block as a reusable section from both Pages and Sections. This control requires the independent Sections capability and uses the existing capability-checked creation endpoint. Opening the panel snapshots the block with independent nested data and a fresh ID, preserving unknown properties. Name, description and category are editable; saving creates a new section without saving or changing the source editor. The saved-section browser is closed after creation so its next open fetches fresh records.

Failures keep the snapshot and entered metadata available for retry. Empty names and pending/disabled states prevent submission, and Enter in panel inputs cannot submit the enclosing editor form. Opening the panel focuses its name field. Existing editor reservations still disable these controls when editing is unavailable.

Validation: expanded synthetic Sections browser checks pass for no implicit source write, empty-name prevention, Enter handling, failed-save retry, copied nested data and independent IDs, alongside existing editor checks. Pages browser regression passes and verifies that a Pages-only user has no save-as-section control. Dashboard typecheck and production build pass; 26 Core bridge tests and two shared section-copy tests pass. No schema, production data, GitHub push or deployment changed. The broader consolidation goal and remaining builder/module/migration work remain active.

## User Manager password recovery checkpoint

Added an Owner-only password-recovery action to the native User Manager and its typed API contract. The server revalidates Owner access, locks the target profile, requires an active existing account, resolves the saved email itself, and invokes the same Better Auth reset flow already used by dashboard sign-in. The callback is fixed to the configured dashboard origin. It records `account.password_recovery.requested` without the recovery link/token and enforces a one-minute per-account limit under the target lock. The response contains only success, never a password or recovery token. MFA requirements and permissions are unchanged; session revocation remains the explicit existing action.

The UI confirms the saved recipient, keeps unsaved profile/access edits intact, disables recovery for inactive saved accounts, and displays delivery-queue confirmation inside the open editor. No real email was sent during implementation. Password tokens and email outbox records continue to be owned by the canonical authentication flow; its database work is separate from the surrounding management audit transaction. A database failure after auth queues an email could therefore require audit reconciliation; the recovery action does not claim distributed atomic delivery.

Validation: API, Dashboard and shared-library typechecks pass, generated clients are current, and dashboard production build passes with existing chunk warnings. Synthetic User Manager browser checks pass for cancellation, sending without saving the profile draft, visible confirmation, existing version-conflict behavior, invitations and mobile containment. The disposable dashboard integration suite exercises authorization, inactive/missing accounts, ignored recipient/redirect overrides, canonical callback, unchanged MFA policy, token-free response/audit and resend limiting. Remaining User Manager notification/linking and broader consolidation work remain active. No production configuration, data, GitHub push or deployment changed.

## Crew and client User Manager boundary checkpoint

Reconciled an authorization inconsistency: office route guards already excluded crew, but the shared capability helper and User Manager accepted/displayed office grants for crew. The shared helper now denies office capabilities for crew and client accounts, and both actor/session projections return empty office grant lists for those account types. Assigned-work and client/property authorization remain in their existing dedicated paths. Legacy crew review suggestions no longer propose ineffective office grants.

Invitation validation and account updates reject office grants or form subscriptions on crew accounts, matching the existing client restriction. The User Manager hides office checkboxes for both account types and explains their access boundaries. Previously stored incompatible grants are preserved in storage and displayed for explicit cleanup; clearing changes only the draft until Save. No migration silently deletes historical grants or subscriptions.

Validation: all 62 dashboard integration tests and disposable migration replay passed, including attempts to assign crew office grants and a crew session containing incompatible stored grants. All five capability tests pass, covering every known capability for crew/client denial and empty crew suggestions. Expanded User Manager browser checks pass for account-type switching, hidden office controls, existing incompatible grants and draft-only cleanup. API, Dashboard and shared-library typechecks and dashboard production build pass with existing chunk warnings. Validation found and corrected a test response typing issue and an ambiguous account-type selector label. No production data, push or deployment changed. Remaining User Manager form notification/catalog delivery and the full consolidation scope remain active.

## User Manager notification catalog contract checkpoint

Added a read-only Owner-only website form catalog through the existing bounded CMS service bridge and generated dashboard client. Core requires an active, attested canonical Owner after service authentication and fresh-grant resolution; local admin status or a Forms grant alone is insufficient. The response contains only ID, name, slug, active status and system status. Inactive forms remain identifiable for explaining saved subscriptions. Fields, submissions, integration settings and recipient addresses are excluded. This account-administration selector remains available independently of CMS editor feature visibility.

Delivery investigation found that the retained forms service currently snapshots admin recipients at submission time, uses private configured recipients for P1 public forms, and embeds `/admin` links in queued effects. Dashboard subscription storage therefore cannot be presented as effective delivery control yet. The next integration must reconcile the existing recipient sources, validate selected forms, recheck current access/subscription state at delivery, update links and preserve existing queued/history records through migration. No subscription checkbox UI or delivery behavior was changed in this checkpoint.

Validation: 27 Core CMS bridge tests pass, including member denial, unattested/inactive Owner denial and exact minimized catalog output. Seven dashboard transport tests pass, including the explicit GET-only Owner allowlist entry. Core/API/shared-library typechecks pass, generated clients are current and diff whitespace checks pass. A test-edit command initially used the wrong working directory; it made no change, was corrected, and the intended test was added and passed. No production data, configuration, push or deployment changed. The complete consolidation objective remains active.

## Canonical form recipient eligibility service checkpoint

Added confidential service endpoints under the existing federation ingress for paginated notification subject IDs and one-at-a-time current recipient resolution. They reuse the existing authenticated service boundary, strict purpose/body validation, browser-header rejection, rate limiting and bounded JSON transport. Lists contain at most 20 account IDs and a continuation cursor; email addresses are returned only when resolving one currently eligible account. The Core client rejects duplicate subject IDs, inconsistent cursors, oversized result sets and mismatched recipient identities.

Canonical eligibility requires an active verified office account, explicit form subscription, Forms access (or Owner), and enrollment when MFA is required. Crew/client accounts are excluded. Recipient resolution reads the current email and repeats all eligibility checks; a stored account ID is not a lasting permission grant. These services are the delivery integration contract only: existing effect queues and sender behavior are not switched yet. The worker, subscription UI/validation, private-recipient reconciliation and legacy queue migration remain to be connected before claiming effective consolidated notifications.

Validation: all 62 dashboard integration tests and disposable migration replay pass. Added cases cover canonical service lookup, disabled accounts, removed grants/subscriptions, unmet MFA enrollment, crew exclusion and two-page enumeration with no duplicates. Seven Core federation-client tests pass, including malformed and mismatched notification responses. API and Core typechecks pass. No production email, data migration, configuration, push or deployment occurred. The full consolidation goal remains active.

## Canonical form notification worker checkpoint

The form-effect queue now supports a canonical `dashboard_form_notification` payload with form/account IDs and the submitted notification content, without a frozen recipient email. Its worker resolves current eligibility/email through the authenticated dashboard service immediately before delivery and uses the canonical dashboard Forms URL. Removed/ineligible recipients complete as explicitly skipped; unavailable federation, lookup failures and provider failures enter the existing bounded retry/dead-letter flow. There is no legacy-address fallback. Provider delivery remains at least once, as with existing effects.

Queue deduplication keys distinguish canonical account IDs, preserving one job per recipient per submission and existing idempotent submission behavior. This extends the JSON payload contract without a destructive schema migration. Existing `admin_notification` jobs retain their current behavior pending the explicit legacy-recipient/queue migration; no live queue was rewritten. Submission fan-out, subscription UI/validation, native Forms navigation and rollout reconciliation are still required before enabling the consolidated delivery path.

Validation: 11 delivery/worker tests pass, covering current email selection, canonical link generation, revoked-recipient skips, lookup/provider retry and token-fenced completion. All 14 form-effects database tests pass against a new disposable local PostgreSQL instance, including canonical recipient separation and replay deduplication; the instance was removed afterward. Core typecheck passes after correcting a missing schema barrel export. A test-edit command initially targeted the wrong working directory and made no change; the corrected edits and intended tests passed. No production email, migration, configuration, GitHub push or deployment occurred. The full consolidation goal remains active.
