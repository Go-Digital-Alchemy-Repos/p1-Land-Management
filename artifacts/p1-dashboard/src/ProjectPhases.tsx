import React, { useEffect, useMemo, useState } from "react";

type Api = (path: string, body?: unknown, method?: "POST" | "PATCH") => Promise<any>;
type Phase = { id: string; title: string; scope: string; status: string; position: number; version: number; prerequisites?: { label: string; done: boolean }[]; planned_start?: string | null; planned_end?: string | null; published_at?: string | null; published_summary?: string | null };
const next: Record<string, string> = { planned: "ready", ready: "in_progress", in_progress: "manager_review", manager_review: "accepted", accepted: "archived", blocked: "ready", cancelled: "archived" };
const label = (status: string) => status.replaceAll("_", " ");

export function ProjectPhases({ projects, estimates, role, api, refresh, onError }: { projects: any[]; estimates: any[]; role: string | null | undefined; api: Api; refresh: () => Promise<void>; onError: (message: string) => void }) {
  const [projectId, setProjectId] = useState(""); const [phases, setPhases] = useState<Phase[]>([]); const [busy, setBusy] = useState(false); const [showCreate, setShowCreate] = useState(false); const [title, setTitle] = useState(""); const [scope, setScope] = useState(""); const [billingPhase, setBillingPhase] = useState<string | null>(null); const [estimateId, setEstimateId] = useState(""); const [amount, setAmount] = useState(""); const [billingTitle, setBillingTitle] = useState(""); const [overrides, setOverrides] = useState<Record<string, string>>({});
  const selected = useMemo(() => projects.find((project) => project.id === projectId), [projects, projectId]);
  const canManage = role === "owner" || role === "manager"; const canBill = canManage || role === "finance";
  const load = async (value = projectId) => { if (!value) { setPhases([]); return; } try { setPhases(await api(`/projects/${value}/phases`)); } catch (error) { onError((error as Error).message); } };
  useEffect(() => { if (!projectId && projects[0]) setProjectId(projects[0].id); }, [projectId, projects]);
  useEffect(() => { void load(); }, [projectId]);
  async function run(work: () => Promise<void>) { setBusy(true); try { await work(); await load(); await refresh(); } catch (error) { onError((error as Error).message); } finally { setBusy(false); } }
  return <section className="panel project-phases"><div className="section-heading"><div><p className="eyebrow">Project delivery</p><h2>Phases and billing review</h2><p className="muted">History is permanent. Billing preparation creates a reviewable draft only.</p></div>{canManage && <button className="secondary" disabled={!projectId || busy} onClick={() => setShowCreate((value) => !value)}>Add phase</button>}</div>
    {!projects.length ? <p className="muted">Create a project to plan phases, prerequisites, review and billing intent.</p> : <><label className="project-phase-select">Project<select value={projectId} onChange={(event) => setProjectId(event.target.value)}>{projects.map((project) => <option key={project.id} value={project.id}>{project.name} · {project.property_name}</option>)}</select></label>
      {showCreate && <div className="phase-form"><label>Phase name<input value={title} onChange={(event) => setTitle(event.target.value)} maxLength={500} /></label><label>Scope<textarea value={scope} onChange={(event) => setScope(event.target.value)} maxLength={10000} /></label><button className="primary" disabled={busy || !title.trim()} onClick={() => void run(async () => { await api(`/projects/${projectId}/phases`, { title, scope, prerequisites: [] }); setTitle(""); setScope(""); setShowCreate(false); })}>Create phase</button></div>}
      <div className="phase-list">{phases.map((phase) => { const target = next[phase.status]; const needsOverride = Boolean(phase.prerequisites?.some((item) => !item.done) && ["ready", "in_progress"].includes(target)); return <article className="phase-card" key={phase.id}><div><p className="eyebrow">Phase {phase.position + 1} · {label(phase.status)}</p><h3>{phase.title}</h3>{phase.scope && <p>{phase.scope}</p>}{phase.planned_start && <p className="muted">Planned {phase.planned_start}{phase.planned_end ? ` to ${phase.planned_end}` : ""}</p>}{phase.prerequisites?.some((item) => !item.done) && <p className="warning">Prerequisites need review before dispatch.</p>}{phase.published_at && <p className="muted">Client update published.</p>}</div>{canManage && needsOverride && <label className="phase-override">Override reason required to move this phase<input value={overrides[phase.id] || ""} onChange={(event) => setOverrides((current) => ({ ...current, [phase.id]: event.target.value }))} maxLength={10000} /></label>}<div className="phase-actions">{canManage && target && <button className="secondary" disabled={busy || (needsOverride && !overrides[phase.id]?.trim())} onClick={() => void run(() => api(`/project-phases/${phase.id}/transitions`, { expectedVersion: phase.version, status: target, reason: `Moved phase to ${label(target)}`, overrideReason: overrides[phase.id]?.trim() || undefined }))}>Move to {label(target)}</button>}{canManage && ["manager_review", "accepted"].includes(phase.status) && <button className="secondary" disabled={busy} onClick={() => void run(() => api(`/project-phases/${phase.id}/publish`, { expectedVersion: phase.version, summary: phase.published_summary || `P1 has reviewed the ${phase.title} phase.` }))}>{phase.published_at ? "Update client note" : "Publish client note"}</button>}{canBill && phase.status === "accepted" && <button className="secondary" disabled={busy} onClick={() => { setBillingPhase(phase.id); setBillingTitle(`${selected?.name || "Project"} · ${phase.title}`); }}>Prepare progress draft</button>}</div>{billingPhase === phase.id && <div className="phase-form billing-form"><label>Approved estimate<select value={estimateId} onChange={(event) => setEstimateId(event.target.value)}><option value="">Select estimate</option>{estimates.filter((estimate) => estimate.property_id === selected?.property_id && estimate.status === "approved").map((estimate) => <option value={estimate.id} key={estimate.id}>{estimate.title}</option>)}</select></label><label>Draft title<input value={billingTitle} onChange={(event) => setBillingTitle(event.target.value)} /></label><label>Amount (USD)<input type="number" min="0.01" step="0.01" value={amount} onChange={(event) => setAmount(event.target.value)} /></label><button className="primary" disabled={busy} onClick={() => { const cents = Math.round(Number(amount) * 100); if (!estimateId || !billingTitle.trim() || !Number.isSafeInteger(cents) || cents <= 0) { onError("Select an approved estimate and enter a positive draft amount."); return; } void run(async () => { await api(`/project-phases/${phase.id}/billing-intents`, { operationId: crypto.randomUUID(), expectedPhaseVersion: phase.version, estimateId, title: billingTitle, amountCents: cents, kind: "progress" }); setBillingPhase(null); setEstimateId(""); setAmount(""); setBillingTitle(""); }); }}>Prepare draft</button></div>}</article>; })}{!phases.length && <p className="muted">No normalized phases have been added. Existing legacy project notes remain unchanged.</p>}</div></>}
  </section>;
}
